# AI Memory — Current State Only (compact, active path)

> This file holds only current state + open items. Append new state here;
> prune entries once superseded instead of growing this file.

**Owns:** Current state, open items, verified facts, oddities. **Not owns:** Rules/architecture/events/contracts → `AGENTS.md`, `AI_ENTRY.md`, `architecture.md`, `module_contracts.md`, `event_catalog.md`.

**Previous update (render_market CI fix, 2026-09-23):** CI `render_market::market_indicator_toolbar_buttons_queue_wires` failed (eye wire missing). Bisect proved trigger: new accent GRID/CROSS toolbar buttons collide with the test's accent-pixel scan contract (₹-only passes, zero-size passes). ROOT-CAUSE FIX (test untouched): toolbar restored to v1.25.0 pixels; scale/grid/cross settings moved into the INDICATORS popup CHART section (stable ids, category-routed `chart-opt-picked`; Rust actions/state/projection/wiring preserved + new popup test). VERIFIED: render_market 9/9 (+--nocapture), workspace EXIT 0 (22 suites: 388 core + 159 shell + 9 render), my files rustfmt-clean, clippy 0 in shell/view crates (60+ pre-existing vayren-core lints + fmt drift in untouched files; CI runs neither). Unrelated distinguished: disk-full (freed scratch), pythonw-fallback works. Local only, no commit/push.
**Previous update (build-perf audit, 2026-09-23):** MEASURED build bottleneck (no code/config changed, tree clean). Cargo is NOT the bottleneck — the serial LLVM tail is: shell lib = 294k-line Slint codegen → 490s debug / 1463s release (83% serial codegen under `lto=true,codegen-units=1`); 8 crates statically re-embed shell so a 2.9s core change amplifies to 184s wall (63x); per-binary MSVC link ~25s debug, minutes in LTO; full PDBs 3.4GB; target/ 24.9GB; registry re-download 1.17GB every CI run (zero cache); release view DLL 15.5MB vs debug 33-48MB each. PROVEN workflow wins (kept, zero behavior change): `cargo check -p` warm 4.8s vs 184s workspace (-97% feedback), `-p` selection 102s vs 184s (-45%). REJECTED as speed plays: invocation consolidation (~8s, ~1%), feature-stripping (default backend REQUIRED). EXPERIMENTAL (unmeasured, needs validation): release `lto=thin`+CGU16, dev `debug=1`, CI registry/pip cache + job split, lld/sccache/AV-exclusion. 2x dev YES (workflow); 5x/10x NO without architecture change (shell-lib split/dynamic views). Full evidence in task report; raw logs under `%TEMP%\opencode\` (noop/touch/link/slint-gen/clean-shell2/rel-view/increm/shellonly/coretest).
**Previous update (stergy-lab-scroll-select, 2026-09-22):** FOLLOW-UP on the Lab scroll work. (1) VERTICAL SCROLL ROOT CAUSE: the whole workspace page now lives in a `ScrollView` whose `viewport-height` is bound explicitly to `Math.max(self.height, wsdoc.min-height)` — a bare wrapper `Rectangle` has no implicit size so the default viewport stayed window-tall and nothing could move. PROVEN with real `WindowEvent::PointerScrolled` in `tests/lab_scroll.rs`: >5% pixels move, saturates at the document bottom (uniform lab-bg spacer band), round-trips to the top frame. (2) "STOCK WON'T SELECT" ROOT CAUSE: the right-hand RESEARCH OBJECTS column is taller than the DEFINE card beside it, so the layout squeezed its nested `ScrollView` (was `max-height:160px` only), clipping the 2nd strategy (SMA) under the "+ New strategy" button — the row was ~15px and clicks hit the button. FIX: give that list an explicit `height`/`viewport-height` sized to its rows (46px each + 8px gaps, capped at 160px, scrolls beyond). Pixel-verified: both rows now render as full 45px bands with a clean gap (`tests/lab_select.rs` geometry regression). SELECTION WIRING was already correct (`invoke_lab_library_picked(1)`→SMA, shell.rs test) — the bug was purely the clipped hit target. NOTE: the off-screen `MinimalSoftwareWindow` harness does NOT fire `TouchArea.clicked` from synthetic `PointerPressed/Released` (verified: even the shell nav rail ignores them) — only `PointerScrolled` works there — so click-selection can't be proven headlessly; it is covered by the geometry test + the real running app. (3) SMOOTHNESS: the running app was the unoptimised debug build (chunky repaint); launched the `--release` optimised binary for the user to feel the difference. Slint wheel step size is fixed and out-of-scope to change. VERIFIED: `cargo test -p vayren-shell` exit 0 (all render matrix + live-render + lab_scroll + lab_select PASS). Release exe rebuilt with the fix. Local only, no commit/push. Changed: `ui/lab.slint` (page + list viewport heights), new `tests/lab_scroll.rs` + `tests/lab_select.rs`, this file.
**Previous update (stergy-lab-premium-refinement, 2026-09-22):** CONTINUED the existing Strategy Lab layer (no restart, no architecture change) — premium UX pass on `rust/vayren-shell/ui/lab.slint` + palette/presenter plumbing. DATE-RANGE PICKER: new `LabDateRangePicker` (296x344 overlay anchored to the DATE RANGE control through `absolute-position` deltas, mutual exclusion with the popup menus) — START->END two-step pick, hover range preview, other-month days dimmed+unclickable, Cancel/Apply, presets Last 1M/3M/6M/1Y/YTD, immediate inline validation, selection never destroyed (out-of-order click swaps instead of clearing). §22 single authority: ISO parse + validation + human formatting + preset math live ONLY in `lab.rs` (`parse_iso_days`/`human_from_days`/`sub_months`/`date_presets_for`, Hinnant civil<->epoch); Slint gets integer day anchors (`today-days`/`dates-start-days`/`dates-end-days`) and never re-parses strings; commit contract untouched — `dates-committed(iso,iso)` -> queued `dates:a:b`. RUN button: idle/hover/press/running/complete/error states (`run-hover`/`run-press`/`run-done-hover`/`run-done-press`). Pixel-inspection fixes (off-screen `MinimalSoftwareWindow` frames): 8-digit hex alpha tints had wrong channel order (painted opaque cyan), native std-widgets followed the light OS palette (`Palette.color-scheme = dark` at the screen root), popups anchored to the wrong origin (`map-to-window` is not Slint 1.17 -> `absolute-position`), invalid language forms removed (`var`->`let`, `%`->`Math.mod`, no `.substring()`/`.to-int()`, no `parent.parent`), 32-bit `usize`/`u32` indexing mixes. LAYOUT: workspace capped at `max-w` 1280px was left-pinned inside a wider screen (~540px dead at 1920) -> now centred. §19: the 04 READ receipt shows `empty-reason` (Rust already projected it, Slint ignored it) plus an inline "+ New strategy" action when no strategy exists. GOVERNANCE: `LabPalette` re-declared generic role names (bg/text/muted/faint/teal/red/amber) with lab-specific values -> `validate_authority` duplicate-token FAIL; renamed to `lab-*`, colour values untouched, 87 refs updated. VERIFIED: `cargo test -p vayren-shell -p vayren-strategy-lab-view` exit 0 (144 lib tests incl. new `civil_date_math_matches_known_epoch_days` + `date_range_projection_formats_validates_and_never_destroys`, 3 render tests, 0 failures); authority + language-ownership validators PASS (231 files). NOT pixel-verified after the final centring/empty-state edits (user chose fast-track over the render matrix) — run the Lab screen to eyeball those two. Pre-existing dead-code warning `research_state.rs:2840 st_symbols` (other file, untouched). FORENSICS BUG FIXED (required task-end `report` was crashing): `scripts/forensics/intervals.build_intervals` let anchors/pairs spill past a closed session window when a later turn wrote marks against the already-closed task, so `covered 68566ms != elapsed 35825ms` raised `ValueError: partition mismatch`; anchor/span/run-pair timestamps are now clamped to the window end and the new regression test `test_markers_written_after_the_window_closed_do_not_inflate_partition` fails without the clamp and passes with it (15/15 forensics tests, `report` renders `PARTITION CHECK diff 0ms`). Also `validate_architecture_gate.py` FAILED once Lab-touched files entered the git change-set: its `local_modules` first-party set was built from `scripts/**/*.py` file stems only, so the pre-existing `from forensics import ...` in the forensics tests read as an undeclared third-party import; package dirs under `scripts/` (excluding `tests`) are now first-party too, gate PASS. GATE RE-RUN: ruff check + format clean (231 files), all 6 validators PASS, forensics 15/15, `cargo test -p vayren-shell -p vayren-strategy-lab-view` still 144+3 PASS. Python partitions (run individually): 02_data 113 pass + 1 pre-existing ambient fyers fail, 09_broker 55 pass, scripts/tests 389 pass + 4 `test_validate_scope` bench cases returning `status UNKNOWN`/empty scope (ground-truth/index-dependent, not Lab-related), 00_app/01_core/05_strategy test dirs contain only `__init__.py`. NOTE machine-level env pollution found while running the aggregate suite: an editable-install `.pth` in global site-packages (`_editable_impl_vayren_core.pth`) points at the OTHER checkout `Desktop/vayren-core-main`, so a single cross-chapter pytest run dies with `import file mismatch` on `broker.tests.*` — run partitions per chapter, or remove that stray editable install. Temp render harness + `.tmp_lab_renders/` deleted. Changed: 7 Rust files (lab.slint/lab.rs/palette.slint/shell.rs/app.slint/view lib.rs/lab_host.slint) + 3 script files (forensics/intervals.py, forensics/tests/test_forensics.py, validate_architecture_gate.py) + this file. Local only, no commit/push.
**Previous update (no-console permanent fix, 2026-09-22):** BLACK WINDOW part 2: after the exe went GUI-subsystem, the spawned `python.exe` (console-subsystem child) popped its OWN console. Permanent fix: bridge tries `pythonw` first, falls back to `python` + `CREATE_NO_WINDOW` on spawn AND handshake failure (a stub can spawn yet never speak); backend logging degrades to `NullHandler` when stderr is broken instead of crashing startup. Verified live: app window up, backend connected, zero conhost anywhere. Note: on this machine the bridge uses `python` fallback (Store-alias PATH lacks `pythonw` for Explorer launches) — hidden flag covers it. 158/158 tests; exe rebuilt.
**Previous update (backend-resolution hardening, 2026-09-22):** PRODUCTION BUG: app showed empty chart ("No data available") despite new binary — the global pip editable pointer had flipped to another checkout (`VAYREN-BROKER-LAYER-ADD`), so `python -m app.headless` imported ANCIENT backend code (no `market_data_service`, old honest-empty snapshots without `notice`). Immediate fix: reinstalled editable from vayren-core-main. Permanent fix: `PythonBackend::spawn` now sets `PYTHONPATH` from the exe's own repo chapters (derived from `current_exe`, existing `PYTHONPATH` appended) — the backend always runs the code next to the binary, immune to global pip state. Proven by sabotage test (stale pointer + foreign cwd → still 527 symbols, real bars, OBR+3 built-ins). +1 bridge test (`chapter_paths`); 158/158 shell tests; rustfmt clean; exe rebuilt.
**Previous update (chart perf + TV parity, 2026-09-22):** CHART PERFORMANCE: measured first (new `rust/vayren-shell/src/perf.rs` headless benches) — Rust engine was already sub-ms (project 0.11ms, hover 0.17ms, pan/zoom 0.19ms @60k bars debug), so the lag was Slint scene churn (4,800 nodes rebuilt per hover) + UI-thread-blocking backend fetches. Fixed with tiered apply: `apply_market_hover` (8 scalars, 0 models — 0.51ms→0.02ms, 21x), `apply_market_viewport` (geometry lists only), full apply reserved for data changes; `project_hover` extracted as single source of truth. Async fetch bus (`main.rs`: worker threads + mpsc + 50ms UI-thread pump, per-kind seq latest-wins, mutex-serialized bridge) for symbol/timeframe/lab-select/lab-run — old bars stay visible until atomic swap. TradingView parity (engine-owned, own implementation): `ChartScaleMode` Regular/Percent/Log with honest fallbacks + `%`/`log` toolbar button, GRID and CROSS visibility toggles + toolbar buttons, cursor-anchored wheel zoom + visible-only autoscale verified (pre-existing, kept). Slint additions are bindings/flags only (no second pipeline). VERIFIED: 157/157 shell tests (incl. 5 perf + 5 scale tests), ruff clean, authority/routes PASS, rebuilt exe + live launch (window handle up, backend connected, clean shutdown, no strays). Honest limits: no real raster-FPS measurement headless (Rust frame work 0.02–0.5ms debug); no bars/line/area chart types; no indexed-to-100; rapid clicks can queue worker threads (each exits after response).
**Previous update (chart+lab end-to-end fix, 2026-09-22):** CHART + STRATEGY LAB now run on real `D:\ZerodhaTradingData` (527 symbols, base 15m). New canonical read path `00_app/app/services/market_data_service.py` (MarketDataService: discovery + normalized Bar + kernel timeframe ladder/availability/fetch-plan + kernel aggregation + quotes + date ranges; SQLite read-only, per-file failure never aborts discovery) shared by Chart snapshots and Lab/backtest. New `00_app/app/services/backtest_service.py` (mirrors Rust `execute_bars` via `backtest.native_positions` fills/exits + `native_metrics` report; buy/sell/compare; fail-closed actionable errors). New built-ins `strategy/builtins.py` + `strategies/ema.py`/`rsi.py` (SMA/EMA/RSI, marked built-in). `headless.py` rewired: real market/lab snapshots, `select_lab_strategy` + `run_backtest` commands, startup discovery logging, specific notices. Root-cause repairs: `strategy/manifest.py` no longer imports deleted `core` authorities (structural dict — `import strategy` works again); `pip install -e .` refreshed to this repo (1.24.0); headless bootstraps chapter `sys.path`. Rust: `main.rs` data-dir legacy fallback, `python_bridge` SelectLabStrategy/RunBacktest commands, `MarketState.notice` with symbol+timeframe empty message, `wire_lab` select/run backend round-trips via `LabRunRequest::gather`. Contracts: `module_contracts.md` manifest row updated. `run_tests.py` PARTS += `00_app/app/tests`, `05_strategy/strategy/tests`.
VERIFIED: 20/20 new Python tests; cargo 388/388 core + 147/147 shell; full protocol smoke (527 symbols, RELIANCE 15m/1h bars, lab 3 built-ins + 527 universe, real SMA backtest 49 trades); ruff clean repo-wide; pyright 0 in new files (39 pre-existing elsewhere); imports/structure/language/authority/routes validators PASS. Pre-existing/environmental, unchanged: FYERS ambient fail, arch-gate + scope-bench FAIL only on unborn-HEAD (no commits in this checkout), `make check`'s `scripts/context.py` target + cargo-fmt drift. Deleted one stale validation-cache FAIL entry (pre-fix gate_coverage premise). Local only, no commit/push.
**Last update (chart-price-scale-and-historical-viewport, 2026-09-24):** VAYREN CHART — PRICE SCALE & FULL HISTORICAL DATA VIEWPORT FIX.
1. Problem 1 Fixed (Right Price Scale):
   - Added subtle 1px vertical hairline boundary (`VayrenPalette.hairline`) between candle plot and dedicated price axis strip.
   - Vertically centered price tick labels on horizontal grid lines (`y: Math.max(2px, Math.min(t.pos * parent.grid-h - parent.tick-h / 2, parent.grid-h - parent.tick-h - 2px)); height: 16px;`) with 8px margin from right container edge.
   - Replaced flawed `mouse-x > plot.width - root.price-strip` click interception inside the candle plot with a dedicated `TouchArea` in the price scale strip (handling price dragging, scrolling price zoom, and double-click reset).
   - Ensured active crosshair price badge sits strictly on the right price axis, aligned with horizontal crosshair without candle overlap.
2. Problem 2 Fixed (Historical Candles & Viewport Navigation):
   - Diagnosed data pipeline: SQLite store has full multi-year history (62,824 bars on 15m; 2,524 bars on 1D from 2016 to 2026). Database fetch, timestamp conversion, and sorting were verified intact.
   - Identified root cause of narrow window / 2026 lock: `MAX_VISIBLE_BARS` was hard-capped at 1,800. In 15m (25 bars/day), 1,800 bars is only ~72 trading days (~3.5 months, mid-May to mid-August 2026). Zooming out stopped dead at 1,800 bars. On Daily (`1D`), 1,800 bars cut off 724 bars (~3 years, 2016–2018).
   - Raised `MAX_VISIBLE_BARS` to 10,000 in `rust/vayren-shell/src/market.rs`. All 2,524 daily bars (10 years) can now be displayed at once. Intraday zooming out expands up to 10,000 bars (~1.5 years).
   - Added dedicated time axis `TouchArea` with mouse-wheel and left-click drag support to pan smoothly across the full historical range back to 2016 (`first = 0`).
   - Added `ev.modifiers.shift` support to the main plot so Shift + mouse wheel pans horizontally along time.
3. Verification:
   - 164/164 Rust unit & performance tests passed (`cargo test -p vayren-shell --lib`).
   - Integration tests passed (`cargo test -p vayren-shell --test live_render_snapshot`).
   - 55/55 broker tests passed (`pytest 09_broker/broker/tests`).
   - Governance validators passed (`validate_authority.py`, `validate_language_ownership.py`).
   - Zero git push performed, `stash@{0}` intact.

**Previous update (fyers-auth-fix-and-broker-icons, 2026-09-23):** FYERS API V3 AUTHENTICATION FIX & BROKER UI POLISH.
1. Root cause diagnosed & fixed: FYERS `POST /api/v3/token` endpoint returns HTTP 308 (Permanent Redirect) with the authorization code inside the response body (`{"Url":"http://.../vayren/fyers-callback?s=ok&code=200&auth_code=..."}`). `_UrllibTransport._read_json()` in `02_data/data/provider/fyers/live_auth.py` previously aborted on HTTP 308 without reading the body. Updated `_read_json()` to decode the JSON body on HTTP 308 if `Url`/`url` is present. Auth code extraction and token exchange now succeed completely.
2. Auto-Authentication Priority (`02_data/data/provider/factory.py`): In `_auto_authenticate()`, prioritized the fast, headless API-based `FyersAutoAuthEngine` (vagator API) before Chrome Selenium. When the full auto-login triple is present, authentication succeeds without opening Chrome or triggering anti-bot challenges. Selenium remains as fallback.
3. Credential Persistence & Reconnect UX (`00_app/app/headless.py`, `rust/vayren-shell/src/broker_connection.rs`, `shell.rs`, `main.rs`): Added `saved: bool` to `CredentialFieldView` in both Rust and Slint. Headless checks the OS vault and flags stored fields so the UI displays saved values (`••••••••`) on load. Unchanged sentinels are not overwritten during connection requests. Added `submit_check()` on startup in `headless.py` so valid sessions are automatically validated and the broker shows `CONNECTED` on application launch without requiring re-entry.
4. Official Broker SVG Logos & UI Polish (`rust/vayren-shell/ui/`): Added pixel-perfect `zerodha.svg` and `fyers.svg` matching the user reference image (`media_1790134198935.png`). Added native `key-pressed` clipboard handling for Ctrl+C, Ctrl+V (`\u{16}`), Ctrl+X, Ctrl+A directly on `TextInput` and streamlined `[PASTE]` button to call `field-input.paste()` directly. Added "Account Connected & Verified" active status banner, capsule status pills, and prominent red `DISCONNECT BROKER` button.
5. Sidebar Status Synchronization (`rust/vayren-shell/src/shell.rs`, `main.rs`): Fixed UX confusion where the sidebar row for the selected broker remained `Connected` while the main card was in the `Authenticating` state. In `shell.rs`, the selected broker's sidebar status label, tone, and connected boolean are now derived directly from `workspace.state` (showing amber `Authenticating` during authentication, and green `Connected` only when verification succeeds).
6. Permanent Fix for Hang & Clear Real-Time Status View (`02_data/data/provider/fyers/`, `00_app/app/headless.py`, `rust/vayren-shell/`):
   - Diagnosed root cause of hang: The OS credential store had an accidental multiline prompt saved as `redirect_uri`, triggering FYERS HTTP 400 `redirectUrl mismatch`. `_read_json` previously suppressed the error body, causing an opaque fallback to Selenium (CAPTCHA) and `headless.py` waiting 120s in a loop.
   - Sanitization & Normalization (`credentials.py`, `headless.py`): Guaranteed `redirect_uri` always validates as a single-line HTTP/HTTPS URL, automatically falling back to `DEFAULT_REDIRECT_URL` (`http://127.0.0.1:9475/vayren/fyers-callback`). OS vault cleaned.
   - Fast Failure & Transparent Venue Errors (`live_auth.py`, `auto_auth.py`, `factory.py`): Decoded HTTPError JSON response body in `_read_json` to extract exact venue messages (`redirectUrl mismatch`), surfaced the message through `ensure_session`, and avoided doomed Selenium fallbacks on fatal credential/venue errors.
   - Clear Live UI Feedback (`ui/broker_connection.slint`, `broker_connection.rs`, `shell.rs`): Added active live authenticating feedback badge (`root.status-message`), dedicated right-panel Connection Status Feedback card showing real-time state/reason, and eliminated duplicate RETRY buttons on `is-failed`.
7. Verification: Live FYERS connection verified (`authenticated as XV29244`); 67/67 pytest tests passed (12/12 selenium + 55/55 broker); 9/9 broker_connection Rust tests passed; all governance validators (`validate_language_ownership.py`, `validate_authority.py`, `validate_routes.py`) passed (0 errors). Native binary compiled: `rust/target/debug/vayren-shell.exe`.

**Previous update (fyers-connect-end-to-end-fix, 2026-09-23):** VAYREN FYERS — END-TO-END BROKER CONNECTION & AUTHENTICATION WIRING.
1. Root cause diagnosed: Slint "Connect to Fyers" button invoked `connect-requested`, which forwarded to `ui.on_broker_connect_requested` in `rust/vayren-shell/src/main.rs`, but `main.rs` only logged a stub `println!`; `python_bridge.rs` lacked `ConnectBroker`/`DisconnectBroker` commands in `BackendCommand`; and `00_app/app/headless.py` lacked `connect_broker`/`disconnect_broker` command handlers in its command loop. As a result, clicking "Connect to Fyers" never initiated authentication or reached `BrokerManager`.
2. Python Headless Backend Wiring (`00_app/app/headless.py`): Added `connect_broker` and `disconnect_broker` handlers. On `connect_broker`, non-empty UI credentials merge over existing OS vault stored credentials (defaulting `redirect_uri` to `http://127.0.0.1:9475/vayren/fyers-callback` if absent), `broker_manager.configure` saves them securely, and the backend awaits background worker thread settlement (or triggers `start_login` when manual login is required) before emitting the updated `system_snapshot`.
3. Rust Bridge & IPC Protocol (`rust/vayren-shell/src/python_bridge.rs`): Added `ConnectBroker { broker_id, credentials }` and `DisconnectBroker { broker_id }` to `BackendCommand` enum with full serialization unit tests.
4. Native Shell Asynchronous UI Wiring (`rust/vayren-shell/src/main.rs`): Upgraded `PythonBackend` handle to `Arc<PythonBackend>` for thread safety; wired `ui.on_broker_connect_requested`, `ui.on_broker_login_requested` (retry), and `ui.on_broker_disconnect_requested` to execute backend IPC in a background thread (`std::thread::spawn`), immediately updating Slint to the amber `Authenticating` state with step 2 active, and dispatching the final `BrokerWorkspace` projection back to the UI thread via `slint::invoke_from_event_loop`.
5. Real End-to-End Verification: Tested `connect_broker` through `app.headless` with real FYERS credentials. Confirmed that the existing `FyersSeleniumAuthEngine` launched Chrome, opened the live FYERS login page, successfully detected and handled the broker challenge ("FYERS shows a security check (captcha) at the login page"), and returned honest `LOGIN_REQUIRED` status with the exact broker reason.
6. Validation: 141/141 Rust tests passed; 12/12 FYERS Selenium auth tests passed; all validators (`validate_language_ownership.py`, `validate_routes.py`, `validate_authority.py`) passed (0 errors). Zero credentials logged. Desktop binary rebuilt at `rust/target/debug/vayren-shell.exe`.

**Previous update (phase8-self-opt, 2026-09-22):** MEASUREMENT-DRIVEN SELF-OPTIMIZING LOOP (consumes frozen P1-P7; never edits production source; learned data only suggests/accelerates). New `scripts/self_opt.py` (`--telemetry/--baseline/--candidates/--evaluate/--accept/--reject/--rollback/--benchmark/--report`): compact local telemetry (`telemetry/v1`, no source bytes) → `BASELINE_V1` over 10 golden tasks (strategy/broker/market/backtest/UI/AI/data/cross-module/cross-language/failure) → evidence-gated candidates (problem/evidence/change/benefit/safety/threshold/rollback; 3-observation minimum) → A/B vs baseline → versioned accept/reject/rollback in `.repo_index/optimization/` (append-only history, atomic writes, corrupt-state quarantine, stale-baseline exclusion). Learned params are learnable-keys-only (packet level/cache reuse/scope-memo/decision-memo); canonical route/ownership/contracts/validation verified byte-for-byte on every acceptance. Safety: prechecks never execute doomed work; scope/decision memos re-verify freshness/evidence on every hit; current evidence always beats history. No compatibility changes to P1-P7.
MEASURED: baseline 378,955 context bytes/47 tool calls/0 searches/10-11.8s; optimized 267,617 bytes (-29.4%), 10/10 suite PASS, 0 searches; CTX-L1 53,219→37,060 bytes; scope-memo 1,011.05→5.95ms; decision-memo 0.07→0.01ms; synthetic classify 0.4/0.1ms, decide 0.1ms, precheck 3.3ms. Live: 11 accepted (9× context-level + scope-memo + decision-memo), 1 rollback event verified (re-accept restores gain), rejections via insufficient-evidence/unsafe paths. False recoveries 0; no correctness/safety regression.
VERIFIED: 28/28 new + 54/54 P7 + 41/41 P6 + 161 P1-P5 + 103 other scripts/tests + 182 domains/forensics PASS (569 total + 1 pre-existing FYERS ambient fail); ruff clean; pyright 0 new (48 = baseline); all governance validators + graph/index checks PASS; graph 3,636 entities/0 unresolved; index READY. Pre-existing issues unchanged (FYERS ambient, missing scripts/context.py, cargo-fmt drift, pyright-48). Only intended files changed. Local only, no commit/push.
**Previous update (phase7-failure-intel, 2026-09-22):** FAILURE INTELLIGENCE (consumes frozen P1-P6; no redesign, no source repair). New `scripts/failure_intel.py` (`--command/--result/--task --precheck/--bench/--recover/--revalidate/--json`): FAILURE→normalize (A-R stable struct, bounded raw tail)→classify (command+stderr+rc+scope+graph/index evidence; env/baseline signatures before code blame; signature cache only fills unmatched)→attribute (fault-vs-tool ownership)→decide (exactly one NEXT_ACTION + budget + deterministic escalation)→bounded execute/verify→final (RECOVERED/RETRYING/ESCALATED/BLOCKED/STOPPED/ENVIRONMENT_FAILURE/CODE_FAILURE/UNKNOWN). Precheck skips only deterministic-doomed commands (unavailable/stale manifest+index/missing target+missing binary/proven FYERS ambient). Retry budget exactly 1 on stale/missing/transient only; loop fingerprint stops repeats (`RECOVERY_LOOP_DETECTED`); escalation L2/L3 uncertain deps, L4 cross-module/language/public, L5 mismatch/inconsistency/graph-authority, never downgrade. No compatibility changes to P1-P6.
MEASURED: synthetic classify cold 0.4ms/warm 0.1ms, decide 0.1ms, precheck 3.3ms, escalation 0.2ms, signature cold/hit 0.0ms; live slowest recovery scope-recompute 4.05s, FYERS classify 2.33s, timeout 2.03s, cargo-fmt 1.86s, stale-index refresh 1.16s, escalated scope 0.54s, packet refresh 0.44s, transient retry 0.04s; benchmark 15/15 expected; false recoveries 0; retries bounded (1 execution max per path); escalations deterministic.
VERIFIED: 52/52 new + 41/41 P6 + 33/33 P5 + 40/40 P4 + 43/43 P3 + 25/25 P2 + 20/20 P1 PASS (254 total); ruff clean; pyright 0 new (48 = baseline); all governance validators + graph/index checks PASS; graph 3,554 entities/5,482 rels/0 unresolved; index READY. Known pre-existing issues unchanged (FYERS ambient failure, missing scripts/context.py, cargo-fmt baseline drift, pyright-48). Only intended files changed. Local only, no commit/push.
**Previous update (phase6-valid-scope, 2026-09-21):** INCREMENTAL + CACHED VALIDATION (consumes frozen graph/index/routes/surface/packets/manifests; no redesign, no codegen). New `scripts/validate_scope.py` (`--task/--manifest/--file/--symbol/--module/--route/--run/--full/--json/--no-cache/--stats`): manifest hashes vs tree + pre/post index-manifest compare (catches out-of-manifest edits, new/deleted files, canonical inputs) → impact (modules/symbols added-removed-moved vs manifest regions/public/dep-edge diffs via pre-refresh filemeta/callers/dependents/canonical/languages/incomplete-Rust flags) → deterministic scope L0(syntax: per-file ruff+pyright; Rust proven by cargo compile, no per-file fmt gate exists)..L5 (L1 direct tests/cargo-filters, L2 module partitions+route suites, L3 routes/authority/imports, L4 full governance+scripts tests, L5 full gate; per-file pytest pruned under covering partitions) with reasons+escalations (unexpected/tooling→L5, ownership/routes/validator→L4, docs→L3, cross-language/multi-module→L4, incomplete→+1, test-layer fallbacks) → persistent result cache `.repo_index/validations/` (key: command+scope-level+relevant-file bytes incl. live whole-tree digest for tree-scanning validators+canonical+validator-script shas+toolchain, NO timestamps; PASS/FAIL/UNAVAILABLE cached, TIMEOUT/ENV never reused, atomic tmp+replace, per-key locks with stale expiry) → run minimum set. STALE on pre-run race; UNKNOWN on unfreshable ground truth; UNEXPECTED_CHANGE_SURFACE escalates to L5 (canonical/tooling edits use dedicated rules, not unexpected). Compatibility changes (explicit): execute_surface.run_validation += cwd kwarg (cargo runs under rust/ like Makefile/CI, default unchanged); context_packet key/fingerprint re-read post-resolve + stat-guarded hits (found STALE-keyed cache pollution via Phase-6 testing); patch_surface += route_listed flag (additive). Caught-by-own-tests fixes: template `<kernel>` cargo filtered; conftest excluded from suites; partition-level L2 (22 cmds/254s → 5 cmds/104s cold); manifest-rebuild-never-blesses (stale manifest = change to scope); CRLF-safe TreeEdit; nesting guard env var; uuid probes vs cross-run cache.
MEASURED: cold incremental broker 5 cmds 103.8s 0/5→FAILED only on ambient fyers; warm second run 22/22? (partition shape) 12.4s all-hit; full-py baseline ~180s/14 cmds; packet 218ms cold; scope-only <1s; cache-hit rate 100% on repeats; incremental≡full on private+public broker changes (0 mismatches, broker PASS, full sane modulo documented pyright-48/fyers); cargo filters 17s; 7 goldens all READY end-to-end (manifest→scope→read→gate). No speedup claims beyond measured numbers; cargo-lib equivalence explicitly unmeasured (FULL_PY boundary).
VERIFIED: 41/41 new + 33/33 P5 + 40/40 P4 + 43/43 P3 + 25/25 P2 + 20/20 P1 PASS (493+1 pre-existing); ruff clean; pyright 0 new (48 = baseline); all 8 governance checks PASS; graph 3,433 entities/0 unresolved; index READY. Same pre-existing unrelated issues (context.py target, cargo fmt, fyers env). Only intended files changed. Local only, no commit/push.
**Previous update (phase5-exec-surface, 2026-09-21):** ZERO-DISCOVERY EXECUTION SURFACE (READ-only layer over Phase-4 packets; no codegen, no patch application, no autonomous behavior). New `scripts/execute_surface.py` (`--task/--file/--symbol/--module/--route/--domain/--modify/--read-only/--json/--no-cache/--read [PATH]/--tests/--validate [--run]/--check-edit PATH [--symbol-check]/--check-manifest/--plan/--stats`): TASK→packet→execution manifest (`execution-manifest/v1`: READ files+regions, MODIFY must/may, FORBIDDEN patterns+files, TESTS with symbols, VALIDATE with per-command availability, target hashes, packet/index fingerprints). States READY/READ_ONLY/STALE/BLOCKED/UNKNOWN/NEEDS_CLARIFICATION/TEST_TARGET_MISSING/VALIDATION_UNAVAILABLE/EXECUTION_COMPLETE (never collapsed). Enforcement: must/may allow (freshness-verified), must-not/forbidden/arbitrary-path (incl. traversal)/unknown-path BLOCKED with reason+owner+allowed; symbol-level guard (line+sha, moved/deleted diagnosed via fresh index); sha mismatch → STALE concurrent-modification (no overwrite); read-only manifests block writes; command verification (known runners, scripts exist, binaries on PATH, `<...>` templates marked, never invented); `--validate --run` executes literal commands with timeout → EXECUTION_COMPLETE + per-command rc (broker golden: pytest 55 passed + validators green, template skipped). Manifest cache keyed packet-key+schema+ro-flag; deterministic bytes. Caught-by-own-tests fixes: read_only unkeyed (READY served for ro request), test over-strictness, cache-collision-free keys, structural no-search test, history-dependent cache test isolated.
MEASURED: packet cold ~218ms; manifest cold 8.6ms / warm 0.8ms / hit 0.69ms; exact read 2.8ms; test/validation resolution µs; first-edit-ready (warm) 3.6ms; goldens cold first-ready 2.5–37ms. Zero-search: structural test (no match_route/load_routes/rglob/glob/build_graph/resolve in module; single build_packet + single subprocess sites) + warm-path spies (refresh/build_graph never fire); searches=0, rediscovery=0 by construction; freshness stat-walk reported separately (not hidden). Goldens 7/7 READY with exact must/may/tests/cmds + read READY + gate ALLOW. No speedup claims beyond measured numbers.
VERIFIED: 33/33 new + 40/40 Phase-4 + 43/43 Phase-3 + 25/25 Phase-2 + 20/20 Phase-1 PASS; ruff clean; pyright 0 new (48 = baseline); all 8 governance checks PASS; full pytest 452 + same 1 pre-existing env fail. Same pre-existing unrelated issues (context.py target, cargo fmt). Only intended files changed. Local only, no commit/push.
**Previous update (phase4-context-packets, 2026-09-21):** EDIT-READY CONTEXT PACKETS (consumes frozen patch-surface/graph/index; no new authority, no search, no codegen). New `scripts/context_packet.py` (`"<task>" --json/--level/--context-lines/--max-bytes/--no-cache/--stats` + file/symbol/module/route/domain/modify passthrough): TASK→patch-surface→leveled packet; levels cumulative L0 routing/L1 target/L2 impact/L3 safety, auto-escalated from validation scope (internal→L1, contract→L1+L2, ownership/full→L1+L2+L3). Exact snippets (py AST extents, rs/slint brace-match, default ±6 lines, 120-line cap + CONTEXT_TRUNCATED); route/intent packets carry bodies only for route-listed symbols (empty symbols-map → top-10 connected fallback cap), explicit intents full bodies; caller snippets capped (5, materiality-ranked) + refs capped (25 + omitted count); test symbols via call-edges ranked by hits; contract = ref pointer + declared-direction deps + forbidden + boundary invariant (no prose copies); IMPACT_INCOMPLETE flag for untracked Rust/Slint callers; plan READ/MODIFY/TEST/VALIDATE/FORBIDDEN from surface metadata; budget tracked (bytes/chars/lines/files/symbols/snippets) + --max-bytes intelligent strip with markers; cache `.repo_index/packets/` keyed task+level+lines+bytes+intent-params+routes-sha+index-fingerprint (pre-resolve check, atomic store, 100-file eviction, no timestamps in canonical bytes); freshness via manifest-checked reads + refresh-retry else STALE; blocked/unknown/ambiguous preserved editable-never. Caught-by-own-tests fixes: cache-key intent collision, _span nested-snippet bug, over-trimmed plan/validation, ref-array bloat (slim refs), caller alphabetical→hit-ranked, fallback blowup cap.
MEASURED: cold 210-223ms (surface ~85 + extraction ~135), warm hit 15ms (was 98 before pre-resolve check), freshness ~80-145ms (Windows stat walk), route+index 23µs; goldens A→C bytes/lines: strategy 51K/1448→39K/163, broker 62K/1727→52K/209, market 210K/6543→48K/124, backtest 289K/9032→67K/142, ui 422K/10846→66K/~150, ai 37K/1030→36K, download 17K/555→28K/204 (tiny-route scaffolding floor, reported honest); zero-discovery audit complete on all goldens (modulo acknowledged-missing-tests). No token claims (bytes/lines proxies).
VERIFIED: 40/40 new + 43/43 Phase-3 + 25/25 Phase-2 + 20/20 Phase-1 PASS; ruff clean; pyright 0 new (48 = baseline); all 8 governance checks PASS; full pytest 419 + same 1 pre-existing env fail. Same pre-existing unrelated issues (context.py target, cargo fmt). Only intended files changed. Local only, no commit/push.
**Previous update (phase3-patch-surface, 2026-09-21):** INTENT→PATCH SURFACE (consumes frozen routing/graph/index; no new authority, no search fallback). New `scripts/patch_surface.py` (`--task/--file/--symbol/--module/--route/--domain/--modify/--json/--stats`): priority explicit file>symbol>module>exact route>intent match>UNKNOWN/NEEDS_CLARIFICATION; ambiguity = substring tie under different owners (agreement with route.py pinned); targets with current lines/callers/tests/owner from warm index; impact (callers/caller-files/dependents+actual/affected-modules bounded/affected-interfaces); tests symbol>file>module>route>validators; MUST/MAY/MUST-NOT (must-not = route forbidden globs expanded against graph); ownership gate (forbidden/ext/retention-cover-aware Python↔Rust both directions → BLOCKED with owner/language/reason/allowed); scope ladder internal<contract<ownership<full (RUST+SLINT = one authority; full = multi-language×affected>3); validation = route commands verbatim + scope governance extras (all existing); evidence (route/graph/index fingerprints + contract). New `scripts/tests/test_patch_surface.py` (43 tests: §17 matrix + 7 golden tasks across strategy/broker/market/backtest/UI/AI/data with exact route/target/symbol/tests/scope/forbidden + command-existence anti-invention test).
MEASURED: A route-only 33µs; B route+graph 320µs; C route+index 23µs; D packet cold 185ms / warm ~169ms (freshness stat-walk ~89ms dominates on Windows; build ~10-20ms) / unknown 142ms — within target, no optimization added. Example: "Add validation to StrategyRegistry registration" → NEEDS_CLARIFICATION (broker-ubl vs strategy-change tie, correct per no-guess rule).
VERIFIED: 43/43 new + 20/20 Phase-1 + 25/25 Phase-2 PASS; ruff clean; pyright 0 new (48 = baseline); all 8 governance checks PASS; full pytest 379 + same 1 pre-existing env fail. Same pre-existing unrelated issues (context.py target, cargo fmt). Only intended files changed. Local only, no commit/push.
**Previous update (phase2-warm-index, 2026-09-21):** ALWAYS-WARM INDEX over frozen Phase-1 graph (graph schema/query API unchanged; only additive helper extraction + optional `root` params). New `scripts/repo_index.py`: `.repo_index/` (gitignored) holds `state.json` (fingerprint: index/schema/generator-sha/inputs_hash/manifest/graph hashes) + `manifest.json` (per-file sha/size/mtime incl. canonical+generator inputs) + `filemeta.json` (per-file module/dotted/alias-targets/imports) + `lookup.pkl` (protocol 4, lists/dicts only, O(1) maps with run_query-identical shapes). Change→stat-probe→rehash→incremental patch (affected files/modules/callers/tests/deps/drift, global called_by/depended_on_by/drift recompute) or FULL rebuild when canonical/structural/unsafe→validate→atomic publish (tmp+os.replace, state last+verified); lock-dir mutual exclusion with stale expiry; states READY/STALE/REBUILD_REQUIRED/INVALID/UPDATING(lock); fail-closed (refresh failure keeps old files, query errors, stale never served). PROVEN: incremental≡full byte-identical (live tree + sandbox test). New `scripts/tests/test_repo_index.py` (25 sandbox tests: full §17 matrix — create/noop/warm/add/modify/delete/rename/symbol add-remove-move/dep/caller/test/ownership/language/contract/route changes, interrupted/corrupt/stale/generator-failure/concurrent, determinism, malformed-source, ghost-import). MEASURED (live): full rebuild 4.34s; incremental add 1.13s / delete 0.59s; cold load JSON 33.7ms vs pickle 20.0ms; warm symbol lookup 643µs→1µs (others ~equal, already O(1)); post-change ensure_fresh ~0.3s (Windows stat walk). `.gitignore` += `.repo_index/`.
VERIFIED: 25/25 new + 20/20 Phase-1 graph tests PASS; graph validator PASS (3,148 entities/4,242 rels/0 unresolved); ruff clean; pyright 0 new (total 48 = baseline); structure/imports/language(219 files)/arch-gate/authority/routes/context-engine PASS; full pytest 336 + same 1 pre-existing env fail. Same pre-existing unrelated issues as Phase 1 (scripts/context.py target, cargo fmt). Only intended files changed. Local only, no commit/push.
**Previous update (phase1-repo-graph, 2026-09-21):** REPOSITORY INTELLIGENCE GRAPH (derived index only; canonical sources untouched). New `90_brain/repo_graph.json` (1.59MB, schema v1): 23 modules (incl. 6 view crates + submodules ai/provider/adaptive/native/services), 306 files, 2657 symbols (py AST + rust/slint top-level), 67 dependencies (declared from DOMAIN_DEPS+routes with sources, actual from import-scan with file evidence), 39 drift entries, 40 tests mapped, 0 unresolved. New `scripts/repo_graph.py` (deterministic --build + 8 query kinds) + `scripts/validate_repo_graph.py` (schema/refs/canonical-conflict incl. inputs_hash staleness) + `scripts/tests/test_repo_graph.py` (20 tests: strategy/market/rust/slint/broker symbols, deps, drift w/ evidence, dependents, tests, owner, contract, negative x3, determinism, artifact-match). REAL DRIFT FOUND (reported, not rewritten): `05_strategy/strategy/research/dataset.py` imports `backtest.execution` (strategy→backtest forbidden). MEASURED: cold gen ~4.3s; lookups 1-900us warm (symbol 882us), 15ms cold load; vs broad search 2.0-3.3s/306 files (Q1 130x wall, 306x fewer files — infra numbers only, no speedup claim).
VERIFIED: graph validator PASS; 20/20 new tests PASS (~65s); ruff clean; pyright 0 in new files (repo total 48 = baseline); structure/imports/language/arch-gate/authority/routes/context-engine checks PASS; full pytest 311 + same 1 pre-existing env fail (fyers credential leak, untouched code). PRE-EXISTING, UNRELATED, NOT FIXED: `make check`/`CI` reference `scripts/context.py` (renamed to context_engine.py in phase7 — that target fails); `cargo fmt --check` fails on old shell/system-view files (newer rustfmt). New files only (4 untracked), zero modifications to existing sources. Local only, no commit/push.
**Previous update (arch-compact, 2026-09-21):** Compacted `90_brain/architecture.md` 167→98 lines (9,838→6,022 bytes): kept all required content (module map, dep graph, forbidden deps, layers, runtime, boundaries, validators, evolution); rewrote stale parts to current truth (deleted bootstrap/EventBus/04_chart/loader refs, fixed structure/make-check rows, added verified desktop-shortcut path); merged §5 flow into §4.3 (old §§6,7 → §§5,6; only external section ref 4.2 preserved). VERIFIED: 109 tests, 6 validators + self-check PASS, ruff clean. Local only, no commit/push.
**Previous update (triple-cleanup, 2026-09-21):** (1) Deleted root `conftest.py` (docstring-only, zero dependents; forensics README disambiguated to its own conftest). (2) Compacted `module_contracts.md` 419→153 lines: kept all §§/APIs/deps/invariants/security/interfaces/change-rules (dropped §5.7-era prose, duplicate dep lines, stale dead-model entries verified GONE: SymbolQuote/ChartModel/ExecutionIntent/BrokerOrder/Fill/RiskPolicy/etc.; §8 now lists only live shapes). (3) Compacted `language_retention.json` 39,639→20,129 bytes: dropped unread `classes{}` + 132 dead `migrated{}` + unread per-entry `domain` + 3 dead EXEMPT entries (Python-domain, never consulted); all 31 required entries byte-identical; test_desktop updated to schema lock. VERIFIED: full pytest 291 + same 1 env fail; ruff clean; 6 validators PASS; pyright 48 = baseline. Local only, no commit/push.
**Previous update (constitution-cleanup, 2026-09-21):** Deleted `ARCHITECTURE_CONSTITUTION.md` (166 lines) after merging ACTIVE ownership (explicit Rust/Python/Slint lists + 4 fixed rules) into `AI_ENTRY.md` §1; migrated 54 refs (31 code-comment pointers via script + 3 manual mojibake cases, 8 doc authority lines, policy/retention notes, validator docstrings, task_routes contract, test ACTIVE_DOCS). Migration history/retired sections NOT transferred (git history preserves them). Zero live refs remain (4 changelog lines preserved as evidence). VERIFIED: 164 tests, 6 validators + self-check PASS, ruff clean, cargo check core+shell (1 pre-existing warning). Local only, no commit/push.
**Previous update (contrib-cleanup, 2026-09-21):** Deleted `CONTRIBUTING.md` (68 lines) after merging its only unique item (MODEL→EVENT→SERVICE→TEST order, 6 lines) into `AI_ENTRY.md` §4; fixed AGENTS.md commit header + test_desktop ACTIVE_DOCS. VERIFIED: 109 tests, 6 validators + self-check PASS, ruff clean. Local only, no commit/push.
**Previous update (ui-doc-cleanup, 2026-09-21):** Deleted `UI_DESIGN_SYSTEM.md` (306 lines) after migrating all 22 refs: 14 code comments compacted inline (ownership -> constitution §3, states/banner/components/palette rules kept as 1-liners, stale lab_theme.py + wrong §21/§29 refs dropped); task_routes ui-screen contract -> palette.slint; AI_ENTRY row -> tokens pointer; test_desktop ACTIVE_DOCS entry removed. Zero live refs remain (2 changelog lines preserved as evidence). VERIFIED: 164 tests, 6 validators + context self-check PASS, ruff clean, cargo check (1 pre-existing warning) + lib 388/388. Local only, no commit/push.
**Previous update (phase7b-final, 2026-09-21):** FINAL ENGINE+DELTA PASS on revised spec (frozen Phases 1-6 intact; no second router/policy; no new language). DESKTOP: shortcut was pythonw+launch_native.py (spec-forbidden) -> repointed to `rust/target/debug/vayren-shell.exe --data-dir D:\ZerodhaTradingData --strategy-dir D:\VAYREN_STRATEGIES` (pinned legacy dirs; bare exe would diverge to ~/.vayren); verified by 25s live launch (shell alive + exactly one python backend, clean exit, no strays) + read-back; `make dev` keeps launcher (dev path only). CLEANUP VERDICTS (reference-scanned, zero deletions): retention.json ACTIVE MACHINE INPUT (31 retained + 3 exempt drive both validators; 132 migrated records); ai_history HISTORICAL ARCHIVE (0 consumers by design); broker_layer_design HISTORICAL+referenced (4 refs); CONSTITUTION ACTIVE AUTHORITY; UI_DESIGN_SYSTEM ACTIVE (17 code refs); CONTRIBUTING ACTIVE (thin+convention). Removed 1 stale migrated record (research_service.py still live, needs no entry). ENGINE DELTAS: --context-lines N (default 0), boundary auto-escalation on multi-language modify sets, --validate mode. TESTS: test_desktop.py (binary/flags/spawn/lnk incl. live Windows lnk test/doc-refs/retention-health) + 4 engine tests. FIXED OWN BUGS: over-escalation on multi-language routes; README corpus row; test lint. SPEED: router ~1KB -> context L1 ~0.4-0.8KB with file:lines (4 tasks live).
VERIFIED: full pytest 291 (281+10) + same 1 env fail; ruff clean; 7 checks (6 validators + context self-check) PASS; pyright 48 = baseline; cargo 388/388. Local only, no commit/push.
**Previous update (phase7-context-engine, 2026-09-21):** CONTEXT+EXECUTION ENGINE (no arch/lang changes; Phase-4 context + Phase-5 router preserved, no second router/docs bloat). New `scripts/context_engine.py` (task->route reuse->L0-L3 packet->plan/safety/scope/cache/CLI): symbol file:line resolution + targeted caller grep, change-surface from route metadata, deterministic escalation (internal/contract/ownership/full), safety prechecks (exists/forbidden/language -> EXECUTION BLOCKED), scope drift (planned vs git actual -> ok/escalate/block), content-hash cache (.context_cache/, gitignored), §13 handoff + §19 failure formats, --check self-validation. New `scripts/tests/test_context.py` (22 tests: known/alias/unknown/malformed, symbols+lines+callers, deps, change-surface, escalation, forbidden/missing/language blocks, drift x3, plan, cache key+hit, self-check). Wired: `context-check` in make check + CI; AI_ENTRY +1 line. FIXED OWN BUGS: generic module name `context` collided with speed/context in one pytest process (collection ImportError) + pyright ambiguity -> renamed context_engine.py (both green); pub-fn symbol regex; determinism compare excluded timing. SPEED (4 tasks, live): route handoff ~1KB -> context L1 ~0.4-0.8KB WITH file:lines+plan; 0 searches; 1 call + code reads both sides.
VERIFIED: full pytest 281 (259+22) + same 1 env fail; ruff clean; 6 validators (5 + context self-check) PASS; pyright 48 = baseline; cargo 388/388. NOTE: concurrent agent edited `00_app/app/headless.py` mid-phase (lazy fail-closed imports; `import app.headless` works again) — not my change, not reverted, not adopted; re-certified final tree identical (281+1, pyright 48). Local only, no commit/push.
**Previous update (phase6-enforcement, 2026-09-21):** SELF-ENFORCING INVARIANTS (no arch/docs bloat, no new policy source). Deleted verified-dead `rust/vayren-shell/ui/chart_view.slint` (2.7KB PoC, zero references) — unblocked clean UI-token rule. New `scripts/validate_authority.py` (8 rule groups, pure functions, ARCHITECTURE FAIL format): single-registry (3 canonical classes + no shadow maps + canonical .register writers + holder-only construction), single-authority (12 symbols, canonical-vs-duplicate output), selection single-writer (BrokerSelection allowlist + zero direct .provider writes), public contracts (all __all__ resolve incl. 50+ sites = zero stale exports tree-wide; REQUIRED_ALL on 4 surfaces), bridge purity (import roots stdlib/core/own-domain + 1 documented exception; no IO/threading/network), UI tokens (hex only in palette.slint; token names unique), contract drift (route symbols in ancestor __all__ for strategy/broker; route language vs policy per-file). New `scripts/tests/test_authority.py` (24 tests). Wired into make check + CI after arch-gate. Perf: validators total ~10s (authority 3.7s single-pass; routes 4.0s->0.4s via git-aware scan). Caught-and-fixed own issues: overbroad contract rule (Rust symbols), owner prose->paths, drift-check None handling, 3 pyright errors (->0, repo total back at 48 baseline).
VERIFIED: full pytest 259 (235+24) + same 1 env fail; ruff clean; 6 validators PASS; pyright 48 = baseline; cargo 388/388. Local only, no commit/push.
**Previous update (phase5-routing, 2026-09-21):** MACHINE ROUTING (no arch/lang changes; Phase-4 context preserved). New `90_brain/task_routes.json` (17 routes, ~14KB): task/domain/owner/language/files/deps/tests/validation/forbidden/symbols/change-surface, all paths+symbols verified, multi-language boundaries explicit. New `scripts/route.py` (deterministic exact-key > exact-alias > longest-alias-substring > UNKNOWN + controlled discovery; text/JSON output) + `scripts/validate_routes.py` (unique keys/aliases, language set, owner/files/tests/validation-runner/forbidden-glob/symbol resolution, extension-drift; stale = CI FAIL) + `scripts/tests/test_router.py` (9 tests). Wired into `make check` (validate-routes) + CI; AI_ENTRY +2-line router pointer. Caught-by-own-validator fixes: owner prose->owner_paths, bridge-aware drift, alias collisions, validation-token parsing, tie-break specificity (market-screen case). LESSON: never pass .json to `ruff format` (0.15 formats JSON and can corrupt hand-written quirks; validator ERROR net caught it pre-commit). SPEED (3 tasks, live): router handoff ~0.9-1.3KB, 0 searches, 1 step + code reads (vs Phase-4: entry+grep+reads ~2-3 steps, 3 searches total).
VERIFIED: full pytest 235 (226+9) + same 1 env fail; ruff clean; 5 validators PASS (gate: 8 files); pyright 48 = baseline; cargo 388/388. Local only, no commit/push.
**Previous update (phase4-ai-context, 2026-09-21):** KNOWLEDGE COMPRESSION (no arch/lang changes; two gate FP fixes). BEFORE startup corpus 307,689 bytes (~77k tok, ai_memory alone 226KB). AFTER startup = `AI_ENTRY.md` (4.6KB: purpose + language map + 19-row domain routing + rules + validation) + compact `ai_memory.md` (~11KB) = ~15KB (~3.8k tok, ~95% cut). New `90_brain/ai_history.md` (216KB verbatim archive, never active). Compressed: constitution 484->166 lines (migration procedure -> archive sections, ownership verbatim); module_contracts (dependency graph/event-table/stale-perf/agent-rules -> pointers; fixed Manifests row + stale section refs); event_catalog (story/example -> 1 paragraph); architecture (graph block -> pointer); CONTRIBUTING (dup tables -> pointers); broker_layer_design (supersede banner, body preserved). AGENTS.md: fast-path route (`AI_ENTRY.md -> module -> code`) + current structure block (04_chart/bootstrap/import-example staleness fixed); rules intact. AFTER navigations (3 tasks, live): all routes resolve in-entry (order_state gap found + fixed while measuring). FP FIXES: gate treated repo-local sibling imports as third-party -> `_is_first_party_root` seam + test. Entry routes, never duplicates (machine JSONs canonical).
VERIFIED: full pytest 226 (217+9 gov tests incl. new FP test) + same 1 env fail; ruff clean; 4 validators PASS; pyright 48 = baseline; cargo 388/388. Local only, no commit/push.
**Previous update (phase3-lang-governance, 2026-09-21):** LANGUAGE GOVERNANCE (no redesign, no migration, no new systems, no new policy files). BASELINE = Phase-2 end-state (fast checks + 217+1 pytest + Rust 388/388 all re-recorded). ENFORCEMENT AUDIT: lang-validator already enforces per-file retention/NO_REINTRODUCE/MIGRATED-gone/stale-retention/Rust-hygiene; arch-gate already enforces undeclared-imports + unretained-Rust-domain new files — but TWO gaps: (1) gate scanned `.py` only → any new language invisible; (2) gate `_required_language` had a `prefix + sub` concat bug so excluded_subpaths never matched (would false-flag future AI/test files as unretained-Rust-domain). MINIMAL EXTENSIONS (existing validators only): arch-gate += `ALLOWED_LANGUAGE_EXTENSIONS {.py,.rs,.slint}` + `UNAUTHORIZED_LANGUAGE_EXTENSIONS` (24 prog extensions) + pure `check_new_file_extension` wired into the change scan (diff-scoped, zero FP on current tree); concat bug fixed to exact `_classify_file` semantics (excluded → None); lang-validator += `core_native_importer_allowed` (only `*/native_*.py`, `01_core/core/native/`, `scripts/` may import core.native — current 19 importers verified exact) + `rust_module_forbidden` seam, both enforced repo-wide. Bridge rule-tables deliberately NOT shape-gated (current bridges carry kernel-verdict projections like `_STATE_CODES`/`MESSAGES` — naive literal ban = mass false positives; purity stays NO_REINTRODUCE + importer-allowlist + review). NEGATIVE TESTS: new `scripts/tests/test_language_governance.py` (8 tests, tmp fixtures only): unauthorized ext → FAIL; allowed/non-language → PASS; Rust-domain .py → RUST; excluded AI/tests → never inherit; real-policy RUST/PYTHON pins; reintroduced TRANSITIONS → detected; Rust absorbing strategy/research → forbidden, kernels → allowed; rogue core.native importer → detected. Two test-expectation bugs of mine fixed during red (ai→PYTHON precedence, rust/-tree governed Rust-side). CI: unchanged and sufficient (gate + lang-validator + run_tests covering scripts/tests). DOCS: zero changes — constitution owns, JSONs enforce, AGENTS references; no competing language rules found. Multi-language domains (provider/AI-adjacent/native-bridge) already explicit ordered rules — preserved, not migrated.
VERIFIED: full pytest 225 (217+8 new) + same 1 env fail; ruff check+format clean; 4 validators PASS (gate checked 4+ changed files incl. new test); pyright 48 = baseline (validator/test edits contribute 0); cargo test 388/388. Local only, no commit/push.
**Previous update (phase2-ownership-lock, 2026-09-21):** VERIFY → NORMALIZE → LOCK (no redesign, no new systems, no language-governance change). BASELINE = Phase-1 end-state (17 dirty paths preserved). DISCOVERY from code+graph+validators (docs treated as secondary): actual import graph matches allowed deps (app→broker/core/execution/market/risk/strategy; data→broker/core; backtest→core/strategy; all bridges→core.native.loader only); all 136 FFI exports live in `rust/vayren-core/src/lib.rs` (single surface); risk_engine calls risk kernel in-process (no Rs↔Rs dup); old registries retired (single broker.default_registry, grep-verified); Rust event_bus/registry consumed by 5 *_events modules (live). DOMAINS mapped (18): composition/app, core-bus/registry (Rs), AI-guardrails (Py), native-loader (Py), event-marker (Py), provider-SDK (Py, 12 test files), download-config (Py), download-flow (Rs, unwired), market-store (Rs, unwired), market-vocab Bar (Py), backtest (Rs, unwired), risk (Rs, unwired), execution (Rs, unwired), execution-AI (Py, importable), strategy (Py, package entry broken), broker-UBL (Py, 5 test files, healthiest), native-UI (Rs+Slint), tooling (Py). DUPLICATES: none (any pairing; bridges marshal-delegate-return; credentials storage vs reference-vocabulary layered; protocols layered). CONFIRMED VIOLATIONS in live code: none (validators clean). AMBIGUOUS (reported, Phase-4 docs): 5 docstring-only `__init__` vs "`__all__` is the contract"; provider "derived" invariant with no writer; risk/native_engine's deleted `risk.models` import. PRE-EXISTING DEBT (classified, not redesigned): strategy manifest/dataset, ai/*, headless+3 services dangling imports (break `import strategy`, `import app.headless` exactly as at HEAD); unwired Rust twins (download_engine/backtest_runner/live_session+kernels, no drivers); provider writer absent. INTENTIONAL EXCEPTIONS: policy prefixes for deleted dirs + NO_REINTRODUCE entries for deleted files (anti-reintroduction tripwires); live_session.rs documented twin; TYPE_CHECKING exemptions. LOCK: `ownership_policy.json` + `language_retention.json` canonized as-is (complete: 206 files mapped, 0 stale retained entries) — no new files. MISSING ENFORCEMENT (reported for Phase 3/6): runtime single-registry, no-duplicate-authority (semantic), selection single-writer, `__all__`-presence, UI token discipline, bridge-purity (review-only). NORMALIZED (trivial only): settings.py Bootstrap docstring → composition root (2 comment lines). DOC CONTRADICTIONS for Phase 4 (not rewritten): AGENTS.md bootstrap-subscribe rule (file deleted, zero bus.subscribe in Python), architecture.md 04_chart rows/flows, event_catalog publisher tables, module_contracts §6 interface parties.
VERIFIED: ruff clean; 4 validators PASS; targeted pytest 40/40 (settings+gate); `cargo test -p vayren-core --lib` 388/388; pyright settings.py 0 errors (repo total unchanged at 48 pre-existing). Docstring-only change → full pytest re-run not required (no product code touched). Local only, no commit/push.
**Previous update (phase1-cleanup, 2026-09-21):** VERIFIED-ONLY CLEANUP (no redesign, no ownership change, no new systems). BASELINE first at d0cc3f9 (clean tree): pytest 217+1 pre-existing env fail (fyers credential leak), ruff clean, 4 validators PASS, pyright 53 pre-existing errors (v1.21.0 dangling imports), headless.py + 3 services import-broken (pre-existing), run_tests.py CI driver broken on 8/12 partitions. REMOVED (7 files, all repo-wide reference-checked): `scripts/seed_sample_db.py` (legacy `candles`-schema fabricator, zero refs, G5 violation), `01_core/core/tests/helpers.py` (imports 4 deleted modules, zero refs), `scripts/benchmark_runs.jsonl` + `benchmark_scoreboard.md` + `benchmark_corpus.md` (stale Sep measurements on obsolete tree; corpus specs reference deleted 04_chart/backtest-UI/--describe), `scripts/speed_journal.jsonl` + `speed_marks.jsonl` (stale streams; both harnesses handle missing files). FIXED STALE WIRING: `run_tests.py` PARTS pruned to 4 live-test dirs (CI driver green-path again); stale refs corrected in `scripts/README.md`, `.vscode/launch.json` (dead `python -m app` config removed), `CONTRIBUTING.md`, `05_strategy/README.md`, `00_app/README.md`, `09_broker/README.md`, `forensics/README.md`; `.gitignore` += `.vayren/` + empty `.vayren/strategy_versions/strat-fix` residue removed from disk. KEPT deliberately: benchmark.py + speed/ (harness + test-pinned), forensics (workflow), broker_layer_design.md (3 consumers), UI_DESIGN_SYSTEM.md (17 rs/slint consumers), ai_memory.md (workflow-mandated), headless.py + services (future-rewire composition knowledge), indicators.py (strategy-owned per policy, not a Rust duplicate), skeleton (test-pinned), .ico (external shortcut target), empty tests/__init__ (structure-pinned). DUPLICATES: none (bridges transport-only by inspection). OWNERSHIP VIOLATIONS: none (validator 206 files PASS).
VERIFIED AFTER: pytest 217 + same 1 env fail; ruff check+format clean; 4 validators PASS (arch gate checked the edited run_tests.py); pyright 53→48 (helpers.py's 5 gone, rest identical pre-existing); broker/factory/selection-service imports OK; `from strategy import ...` still fails exactly as at HEAD (pre-existing, manifest.py untouched). Uncommitted, local only, no push.
**Previous update (100pct-ownership-purity-aggressive, 2026-09-21):** DESTRUCTIVE PURITY PASS (user-ordered, capability loss accepted). Deleted 132 prod Python files (10,430 lines) + 118 test files (16,321 lines): all of Core bus/registry/contracts/system/logger/observable/events-appstarted, data storage/scanner/calendar/symbols/lock/models/reporter/manifest/events/downloader/worker, market database/repository/loader/timeframe/events/manifest (+symbol_quote), backtest runner/worker/execution/validation/engine/models/events/manifest, risk engine/session/killswitch/models/manifest, execution engine/journal/modes/planner/replay/broker-venues/market-data-providers/portfolio/runtime/models/manifest. KEPT: Strategy/AI/Research/provider-SDK/broker-contract/app-composition (byte-identical), native_* bridges (transport only), zero-logic shapes needed by kept code (core.Event, market.Bar, execution market-event dataclasses, OrderState enum, data settings/throttle). BRIDGE SCRUB: `native_order_state` no longer materializes the Rust transition table — `transition_allowed` calls `vy_order_transition_allowed` per check (existing export). Retention: deleted entries → `migrated`; structure/imports/arch validators updated + PASS; `module_contracts` §§5.2/5.4/5.7/5.8/5.9 + event_catalog purity-noted; pyproject testpaths pruned.
VERIFIED: ruff clean; 4 validators PASS; `cargo test -p vayren-core --lib` 388/388; pytest survivors green (provider/settings/broker-contract/strategy-core suites); pyright 53 errors = dangling imports in kept-but-unwired files (00_app services, ai/*, strategy/manifest, research/dataset) + 2 pre-existing — accepted breakage, nothing restored. Full suite: kept tests pass; 1 pre-existing env fail (fyers credential leak, stash-proven). Diff ≈ 275 files, +316/−34,176. Local only, no commit/push.
KNOWN STATE: backtest/live/download/market-load capabilities have no wired Python runner (Rust twins exist unwired: download_engine/backtest_runner/live_session + kernels). Shell `dl:*` wires + native app entry have no backend consumer. Rewiring = future work, explicitly out of scope here.
**Previous update (remove-local-preview-badge, 2026-09-25):** Removed `LOCAL PREVIEW` pill from Strategy Lab topbar (`rust/vayren-shell/ui/lab.slint` ~L922-937) per user request — amber `pvtx` Rectangle deleted, layout keeps stretch + V avatar.
VERIFIED: grep `LOCAL PREVIEW|pvtx` 0 hits; `cargo check -p vayren-shell` PASS (4m); `cargo build -p vayren-shell` PASS (6m14s, exe 93MB fresh) so shortcut gets fix on restart. Local only, no commit/push.
**Last update (dead-code-removal, 2026-09-25):** Zero-reference dead-code scan (3-pass: graph `called_by` + repo-wide name-reference + manual verify of every candidate; false positives like `*HostWindow`/`*Harness` Slint comps used from Rust, `ERR_AUTHENTICATION_FAILED` used in a test, `builtins.*` used by headless — all excluded) then removed all strict dead symbols — 22 symbols / ~208 lines: `ObrSellV10` + whole `05_strategy/strategy/strategies/obr.py` (builtins registers only sma/ema/rsi; README line fixed, `obr` task-route alias kept as generic keyword), `ResearchWorker` (`00_app/app/services/research_service.py:191-234`, only `ResearchService` is wired), `unavailable_provider` (`02_data/data/provider/factory.py:49-80`), `restore_version_source`+`verify_version_ir` (`05_strategy/strategy/version.py:473-515`), `pending_events` (`00_app/app/observable.py:209-211`), `protected_snapshot` (`scripts/self_opt.py`), `_fingerprint_paths`+`ensure_warm_index` (`scripts/vayren_intel.py`), 6× `ERR_*` consts (`rust/vayren-core/src/download.rs:734-739` — Python has its own live copies in `contract.py`), `CHECK_COUNT` (`rust/vayren-core/src/risk.rs:46`), `NavItem` (`ui/app.slint`), `DlKV` (`ui/market.slint`). Cascade-cleaned now-unused imports: `Signal`+`WorkerThread` (research_service), `ERR_PROVIDER_UNAVAILABLE`+`ProviderError` (factory), `hashlib`+`repo_index` (vayren_intel). NOT removed: 8 ML Protocol classes in `08_execution/execution/ml_interfaces.py`+`regime.py` (~35 lines, zero refs but declared advisory contracts per AI_ENTRY "execution-AI advisory only, importable") — user decision.
VERIFIED: ruff check+format clean repo-wide; `cargo check --workspace` PASS (8m41s, only 1 pre-existing `unused_mut` in vayren-strategy-lab-view); `cargo test -p vayren-core --lib` 388/388; targeted pytest 185 passed (02_data+05_strategy 118, self_opt+vayren_intel 52, 00_app 15) + 1 pre-existing FYERS ambient credential fail (OS-vault real app id, unrelated); 6 validators PASS (structure/imports/language-242/authority/routes + arch-gate via cargo). Editable pip install absent on this box (pre-existing) → pytest run with chapter PYTHONPATH. Local only, no commit/push.
**Previous update (remove-status-bar, 2026-09-25):** Removed bottom status bar (`VAYREN • Native Shell` + `SCREEN: ...`) from all screens (`rust/vayren-shell/ui/app.slint` ~L1080-1113) per user request — 24px Rectangle deleted, all screens now edge-to-edge. Properties `screen-title` (still used for PendingCard) and `visual-check-label` (still set by main.rs) kept to avoid Rust breakage.
VERIFIED: `cargo check -p vayren-shell` PASS (~2m). Local only, no commit/push.
**Previous update (intel-day1, 2026-09-25):** LIVING ENGINEERING-INTELLIGENCE DAY 1 (foundation, no rewrites, no profile/arch changes). New `scripts/vayren_intel.py` (tooling route, Python) reusing frozen infra (repo_index/graph, benchmark.impact_plan, self_opt/failure_intel patterns — zero duplicates): SCAN→…→UPDATE_MODEL lifecycle state, Day-1 baseline (242 py/58k loc, 74 rs/55k loc, 19 slint/14k loc @cb8ec45; 8 crates, 604 locked pkgs, 7 Slint build.rs, release lto=true/cgu=1), knowledge store with UNKNOWN…REJECTED states, experiment log (failures kept), daily report + ASCII-safe progress timeline + evidence-driven goal generator, all in `.repo_index/intel/`. 53-MIN PATH (code-verified): CI build-test runs build_rust.py --test (7 release + shell + workspace tests) then run_tests.py unconditionally re-runs build_rust.py; dominant cost = LTO+Slint serial tail (historical audit), NOT the duplicate. EXPERIMENTS KEPT: (1) run_tests.py --skip-build with fail-closed mtime gate (58ms probe; saves cargo-startup overhead only — honest seconds, not minutes) + CI uses it; (2) benchmark.py rust-core/rust-ui impact domains + cargo key — tiny Rust/Slint edits now get LEVEL 2 targeted `cargo test -p` instead of full-gate fallback (§48/§49; §50 workspace path intact). NEGATIVE KNOWLEDGE: first ad-hoc scan 2x counts REJECTED (git ls-files cross-check), real-cargo-in-unit-tests VERIFIED harmful (orphan lock pile-ups; tests mock cargo), contaminated 289s baseline REJECTED (quiet remeasure 4.0s). Next goal (self-generated): instrument cache-ratios UNKNOWN.
VERIFIED: 60 impact-scope tests green; ruff/format/pyright 0 on touched files; 6 validators PASS (structure/imports/language-243/authority/routes/arch-gate/context). Full scripts/tests: 421 passed, 4 failed — all environmental/pre-existing (3 validate_scope bench run=True need the unbuilt native cdylib here; 1 known FYERS ambient). `90_brain/repo_graph.json` auto-regenerated by ensure_fresh (+76 entities for new files; artifact-match test green). Local only, no commit/push.
**Last update (intel-day2, 2026-09-25):** CI RUST BOTTLENECK — MEASURED, NOT GUESSED. Full `build_rust.py --test` baseline phase-by-phase on this box (cold target, warm registry, 16GB RAM, cargo 1.98.1): P1 core release 25s; P2 portfolio 1247s; P3 live 1298s; P4 system 1879s; P5 strategy-lab 1782s; P6 research 356s; P7 market 338s; P8 shell debug 2s (warm); P9 workspace test 97s warm (test exec ~35s). Release total ~113 min here. RANKING: (1) separate `-p` invocations rebuild shell+Slint+LTO per view (P2-P5 = 4 shell rebuilds; P6/P7 shared) — A/B PROOF: batched system+lab = 1533s/1 shell build vs 3661s/2 builds separate (2128s saved) → KEPT: build_rust.py now issues ONE 6-view release invocation (same artifacts/profile/assertions) + AST tests; (2) 6 view release DLLs have NO in-repo consumer (no dlopen/ctypes/packaging refs; Python needs only vayren_core, loader accepts debug) — DEFERRED (test-policy call): lean path = core release + shell debug + workspace test; (3) LTO+CGU1 serial tail + 0.5GB-free paging — no change (release correctness). VERDICTS: cold CI ≤10min NOT achievable by scheduling alone (cold dev compile still ~12-18min projected); warm CI ≤5min achievable via Day-1 skip-build + batching + lean path. TRANSIENTS REJECTED: P3 rc=101 (mid-build worktree mutation 08:12-08:22, source unknown — 7 files rewritten with HEAD-identical content; 315-vs-324 setter codegen proved it) and P9 E0786 (OOM/concurrency) — both pass on clean rerun. HEAD CORRECTNESS: workspace green except 1 pre-existing render_market pan test (380px vs 0, Sep-24 price-strip era, product scope). Pyright repo total 135 (48 baseline stale; 0 new from Day-2, 1 pre-existing in build_rust). Next: lean-path cold-dev measurement + thin-LTO A/B. Local only, no commit/push.
**Addendum (concurrent worktree, same day):** AFTER the baseline, `git status` showed 15+ files modified 08:12–09:52 by a second party (00_app, 02_data, 05_strategy incl. deleted obr.py, 09_broker, rust core/shell sources incl. market.slint +292, self_opt.py). My files verified to contain only my hunks (diff-reviewed). IMPACT: (a) P4–P9 absolute times are approximate (built during concurrent edits); structural findings stand (reuse counts, batch A/B direction, no-consumer proofs from stable files). (b) The render_market failure attribution is DOWNGRADED to confounded — needs clean-HEAD retest (could be their uncommitted market.slint). (c) rs/slint loc deltas in the Day-2 report (+578/+186) are THEIR edits, not mine. Did not touch their files; no commit/push.
**Previous update (tv-axis-volume-icons, 2026-09-25):** TRADINGVIEW PRICE-AXIS + VOLUME + ICON PARITY on the Market screen (Rust-owned chart; Slint bindings only, no second pipeline). (1) GLYPH BUG FIX: `▾/↩/⋯/▸` painted tofu on this machine — `SmallButton` gained an `icon` prop rendering vector art (drop-chevron Path, circular-arrow reset, 3-dot menu; IndicatorIcon precedent); sites fixed in `market.slint` (watchlist/sort/overflow chevrons, reset, more, context-menu "Reset chart view") + `live.slint` (SESSION ^/v, CONFIGURE BROKER >) + `system-view` health/log toggles. (2) PRICE AXIS: `nice_step` (1/2/5×10^n) + `nice_price_ticks` (integer-grid walk, Percent steps in %, Log steps in real-price space, axis/cursor share `fmt_scale`) replace span-fraction ticks; grid follows one-for-one. (3) LAST-PRICE: permanent line + direction-colored badge (`last_price*` view fields, full+viewport apply, hover path still 8 scalars) pinned at the axis edge when panned out. (4) VOLUME: bars use translucent direction hues (new `vol-up/vol-down` palette tokens), axis shows visible-window max (faint) + latest-bar volume pinned at its height in bull/bear color.
VERIFIED: `cargo test -p vayren-shell --lib` 173/173 (4 new: nice_step/round-ticks/badges/empty); authority + 21 pytest (desktop/app) PASS; debug exe rebuilt (shortcut picks it up on restart). Known coexisting: ●/▶/■/✓ glyphs elsewhere left untouched (render OK here). Local only, no commit/push.
**Previous update (tv-icon-centering-fix, 2026-09-25):** FOLLOW-UP: user screenshot showed the new vector icons floating at the TOP of their buttons (chevron above "All Stocks"/"Sort by" text, reset/dots riding high). Root cause: Slint `Path` has no `vertical-alignment` — inside the button's HorizontalLayout it pins to y=0 while Text stretches + centers. Fix: every icon branch (chevron/reset/dots) now sits in a `VerticalLayout { alignment: center }`, and the label Text renders only when non-empty (`if root.label != ""`) so icon-only buttons lose the stray 4px gap too. Rest of the screenshot verified correct against TV (nice round axis, red last-price line+badge 1,162.10, gray hover badge, vol max + hover/last volume labels).
VERIFIED: `cargo build -p vayren-shell` PASS (exe fresh; restart to see). Local only, no commit/push.
**Previous update (chart-stage2-polish, 2026-09-25):** SECOND-STAGE TERMINAL POLISH (no rebuild-from-scratch; same viewport transform everywhere — `norm()`/fraction mapping is the single contract). ROOT CAUSES, not magic numbers: (1) §3 MYSTERY 932.68 LINE = stale `strategy_rays/markers/trade_markers/focused` surviving `set_bars` series changes + snapshots-without-keys (replaced only when keys present) — cleared on symbol/timeframe change in `set_bars` (same-series append keeps); legitimate rays now carry hollow axis tags (`RayLevel`, same transform, line's own color) so no persistent line is ever unlabeled. (2) §4 CROSSHAIR OFFSET = raw `y_frac` stored (line/badge diverged at pane edges) — clamped once at store; vertical snap verified exact inverse (`window_size()==count==plot_slots`); Percent/Log badges already frame-derived. (3) §8 VOL FLOOD = hover-volume tag pinned at zone-middle + last/max near-duplicates — tag now sits at the hovered bar's own height (`hover_vol_pos`, 9th hover scalar, zero models), last-volume hides within 12% of max. (4) §9 TIME DUPES = intraday time-only labels repeat across days — adjacent-duplicate collapse. POLISH: dashed subtle last-price run via overlay segments (solid divider removed); adaptive axis precision (`step_decimals`: ≥1→2dp, 0.005→3dp, shared by ticks/badge/hover/rays); compact adaptive crosshair time (`17 Jul 2026 09:45` intraday, `17 Jul 2026` daily+, badge 168→140px, Sakamoto DOW deleted); vertical gridlines from `time_ticks`; header flow layout replaces magic 200px offset (bright symbol + accent timeframe + muted OHLC). VERIFIED-ONLY (§10/11/13/15/16): 7-item timeframe row is data-driven (backend 15m-base has no 1m/5m/1D/1W here — architecture, not UX); header spacer is intentional transparent fill; watchlist fixed 220/200px; candles/vol already share slot X + visible-max scale. Out of scope kept honest: no intermediate volume ticks (15% zone too short; TV shows max only), no 1280/1440/1920 live-resize proof (transforms are resolution-independent fractions).
VERIFIED: `cargo test -p vayren-shell --lib` 180/180 (11 new); rustfmt clean on touched files; authority PASS; 21 pytest PASS; debug exe rebuilt. Local only, no commit/push.
**Last update (intel-day3, 2026-09-25):** AGGRESSIVE-BUT-SAFE BOTTLENECK ATTACK, all measured. ENV: 12 cores, 8.4GB free at start, 81GB disk, cargo/rustc 1.98.1; foreign tree stable all Day (their mtimes unchanged). THIN-LTO A/B (isolated cold target dirs, research-view, `CARGO_PROFILE_RELEASE_LTO` env, Cargo.toml untouched): fat 1116s vs thin 969s (-13% upper bound — FS-cache warmth confounds part; shell codegen flat 505→518s, dep codegen down); DLL byte-identical size (20,132,864 both); peak RSS coarse 7.19→6.32GB; research-view lib tests 3/3. VERDICT: modest, no runtime proof on shipped DLLs → profile UNCHANGED (DEFER to owner). BATCHING VERIFIED: single 6-view invocation, shell-once, all 7 DLLs present (15–19MB), AST tests green, 6-view no-op 0.71s. LEAN PATH MEASURED: cold-dev `cargo test --workspace` = 617s OOM-abort + 483s retry ≈ 18 min (23/24 targets green; only confounded render_market pan fails). OOM ROOT-CAUSED: paging file too small (os error 1455) under 12-job parallel cold build → E0786 mmap cascade (also explains Day-2 P9-run-1). FINGERPRINT MYSTERY SOLVED: P2–P5 rebuilds were correct mtime invalidation during the concurrent-edit window; quiet tree shares — no cargo bug, no fix. CI INSTRUMENTATION ADDED (behavior-preserving `time` + pre/post cache snapshots; yaml-valid; unclaimed until CI runs). FINAL: cold ≤10min IMPOSSIBLE by scheduling alone (lean-cold ≈18 min here); warm ≤5min ACHIEVABLE (skip-build + batching + lean). Lean saves ~85% vs full path here. Tree left cold-target (documented). Local only, no commit/push.
**Last update (intel-day5-lean-ci, 2026-09-25):** LEAN PATH ON REAL GITHUB ACTIONS (user-approved push of as-is branch `intel-lean-test-path`, main untouched, no PR). Run 36114806209: wall 9.4min, FAILED at render_market — failure follows the other party's uncommitted market.slint (committed-HEAD CI green twice), lean change exonerated; quality+validators green. MEASURED (not projected): lean `build_rust.py --lean-test` step = 458s (time: real 7m37.865s) vs 1018–1172s full at same SHA = **560–714s saved on CI**. Cache: full primary-key match, 1238MB/20s restore, target restored trimmed to 4.7G (`cache-workspace-crates: false` confirmed in config — workspace crates always rebuild), save skipped on failure by design. Python partitions unmeasured (build step failed first). No coverage removed (full workspace test ran; view DLLs absent from log as designed). Lean flags locally validated first (19 unit tests + AST pinning + end-to-end partitions; local failures all attributed: mispointed editable-install fixed, machine-vault fyers, their `_phase6_probe` collision in registry.py). Branch left pushed (unmerged); main clean of this work. No further push.
**Last update (intel-day5-render-fix, 2026-09-25):** CI render_market FAILURE ROOT-CAUSED + FIXED LOCALLY (no commit/push). CI log hides the test name (build_rust prints only 3000-char tail — logging gap noted). Reproduced locally at CI commit: `market_one_side_free_pan...` got 380 candle-toned px, expected 0. Row-band probe PROVED: exactly 2 adjacent rows (y=320-321), bear-red only, full-span — the intentional permanent last-price dashed overlay (~50% duty), NOT ghost candles/markers/grid. Classification: real behavior change (their feature, by design) vs stale expectation (test predates overlay). FIX (1 file, +55/-3, test-only): new `parked_right_space_tone` counter masks only rows inside a ≤3-row toned window with ≥25% row coverage; ghost bodies always span many rows so intent fully preserved. Non-vacuity PROVEN by temp live-candle control (>100, deleted after; probe file also deleted). VERIFIED: render 9/9, market-view pkg green, full lean path rc=0 in 516s, rustfmt clean, authority pass. Lean/6-skip/workspace/profile untouched. CI re-run BLOCKED on push approval (fix uncommitted). Worktree: render_market.rs + ai_memory.md dirty; all else in pushed branch.
**Last update (intel-day6-shardfix2, 2026-09-25):** SHARD CI RUN (36134689178) FAILED differently — pure shard GREEN 111s first try (mechanics proven); stateful shard failed LINKING shell bin (`rust-lld: unable to find -lfontconfig/-lfreetype`) — my new job lacked build-test's apt step (pkg-config libfontconfig1-dev); fixed with 4-line yaml mirror (pure needs none — no native link). Awaiting push approval for re-run. No main push without approval.
**Last update (intel-day6-shardfix, 2026-09-25):** SHARD CI RUN (36130354617) FAILED differently — pure shard GREEN 111s (mechanics work); build-test core-lib fail UNATTRIBUTED (truncated tail; zero Rust delta vs green run ⇒ environmental flake; local 5/5 green; added failure-summary printer to build_rust so the name surfaces next time); stateful shard 3 fails ROOT-CAUSED: (a) stale_manifest probe assumes warm index (cold shard runner ⇒ REBUILD_REQUIRED) — E1 proven locally, fixed via session autouse warm fixture (E1 re-run passes cold), (b) nested `pytest scripts/tests -q` needs shell binary (test_desktop) absent on shard runner — E2 proven locally (566s fail), fixed by adding shell debug build to stateful job, (c) my 2 platform tests + probe debris already fixed; public bench was their cascade. My earlier confusion resolved: NO nested-TreeEdit collision (env guard skips nested scope tests). Local proof: 8/8 build_rust, 4/4 scope, 55/55 failure_intel, broker 55/55, all validators, artifact-match green. Worktree for push: ci.yml, build_rust.py, test_build_rust.py, test_failure_intel.py, test_shard_contract.py, repo_graph.json, ai_memory.md. No main push without approval.
**Last update (intel-day6-sharding, 2026-09-25):** scripts/tests SHARDING AS INFORMATION-GAIN EXPERIMENT. Phase-1: 19 files/436 tests; per-file local timings (validate_scope 241s + self_opt 93s = 75%, rest 122s, 456s total green); only 2 live-tree mutators (validate_scope, failure_intel — TreeEdit+restore), rest tmp/sandbox/mock/read-only; bench tests re-execute gate commands recursively (caching defeated by novel-probe design). Phase-3/4 LOCAL EXPERIMENT: 2-way same-checkout concurrency (mutators vs readers) → 294s wall BUT 5 race failures (incl. no-repository-writes guard firing on TreeEdit mutation) vs serial green → REJECTED with proof. Verdict: separate-runners sharding is the only safe parallel form. IMPLEMENTED locally (no push): run_tests.py --exclude + 2 CI jobs (stateful: scope+failure_intel; pure: other 16) + test_desktop step in build-test + test_shard_contract.py pinning exact-once coverage (stdlib-only yaml subset parser after arch-gate rejected pyyaml import) + regenerated repo_graph.json. PROJECTS 21.5→~13min (needs CI proof). All validators green; ruff/pyright clean; 20/20 directly-affected tests green; full local --exclude run validated (only known vault-fyers failure). CI proof + Phase-5 blocked on push approval.
**Last update (intel-day5-ci-green, 2026-09-25):** FULL GATE GREEN ON LEAN PATH (run 36123465842, branch, user-approved pushes only). Wall **21.5min** vs 29.6–33.8min baseline = **8–12min CI-measured saving**. quality 32s ✓, validators 24s ✓, lean build real **8m28.948s** (core ~22s, shell dbg ~141s, workspace test ~345s — render_market 9/9 green on Linux CI), Python real **9m44.288s** (`[SKIP] rust build` worked with lean artifacts; app 1.3s, data 70.1s, strategy/broker/forensics ~1.5s, **scripts/tests 511.0s**), gate ✓. Cache: full primary-key match, 1238MB/20s restore, target 4.7G→27G, save ~0s. Fixes CI-proven: platform test names + probe-debris removal + overlay-aware counter. Lean/6-skip/workspace/profile/coverage all intact. ≤10min NOT reached — remaining bottleneck scripts/tests 511s; next: shard it. No main push.
**Last update (intel-day5-ci-fix, 2026-09-25):** LEAN CI RUN (36114806209) FAILED with 6 Python failures — all root-caused, none hidden. (1-2) MY bug: test_build_rust `_fake_tree` hardcoded Windows artifact names → `main()` returned 1 on Linux (`assert 1 == 0`); fixed via driver's own name helpers (8/8 green). (3) test_bench_public_py_change: cascade of (1-2) — resolves with them. (4-6) private/cross_module/unexpected_file UNKNOWN: committed `_phase6_probe` debris in registry.py (byte-identical to test PROBE_PRIVATE; killed-run leftover swept in by my snapshot commit) collides with test appends → duplicate symbol → graph invalid; REMOVED (3 lines, zero callers) → all 4 scope tests pass locally (506s). PROVEN on CI: render fix green (partitions ran ⇒ cargo rc=0), lean build 458s stands, quality+validators green. Validation: broker 55/55, pyright 0 changed, all validators pass. CI re-run needs push of registry.py + test_build_rust.py (+ regenerated repo_graph.json).
**Last update (intel-day4-ci-evidence, 2026-09-25):** REAL GITHUB CI EVIDENCE (no push — read existing runs via gh). Two success runs at local HEAD SHA cb8ec45: wall 33.8min (36008184611) and 29.6min (36086537542) — the ~53min figure is STALE. Step split: setup ~4min (incl. free-disk 92–160s), rust-cache restore 17–34s (partial-key hit once, primary-key hit once; save 0–22s; target/ 28G trimmed → workspace crates always rebuild), build_rust.py --test 17–19.5min (core ~25s, portfolio 358–412s deps+shell ONCE, 5 views 20–80s each sharing shell, shell debug 110–131s, cargo test workspace ~5.5min), run_tests.py 9–9.8min (dup rebuild = SECONDS same-runner; partitions: app ~1s, data 69–77s, strategy/broker/forensics ~2s total, **scripts/tests 456–515s**). CORRECTIONS to Day-2/3: batching CI benefit ~nil (CI already shares shell; local rebuilds were pressure artifacts — batching kept as dev-loop win); --skip-build saves ~1min on CI (kept, harmless). LEAN VERIFIED against coverage: cargo self-contained; only 00_app (+scripts/tests via scope-bench) needs vayren_core (release kept, 25s); desktop needs shell bin; packaging needs nothing → lean CI projects ~19–23min (not ≤10; needs + scripts/tests sharding + deeper levers). NOT within target → next experiment: lean-path CI validation run (requires owner push; Day-3 instrumentation will timestamp it). Coverage intact. No commit/push.
