# AI Memory — Abhi Kya State Hai

**Owns:** Current state, open items, verified facts, oddities. **Not owns:** Rules/architecture/events/contracts → `AGENTS.md`, `ARCHITECTURE_CONSTITUTION.md`, `architecture.md`, `module_contracts.md`, `event_catalog.md`.
**Last update (desktop-broker-ui-integration, 2026-09-19):** DESKTOP NEW BROKER UI VERIFIED. Root cause tha stale deployment: Desktop app `rust/target/release/vayren_system_view.dll` (09/17) load karta hai, lekin NEW `BrokerConnectionWorkspace` embedding (`system_host.slint` + `lib.rs` projection) 09/19 ka tha — app purana `BrokerPanel` (CONNECT/CONFIGURE) dikhata raha. Fix: `cargo build --release -p vayren-system-view` (22m52s) → DLL 11:31:29. Saath me embedding-glue hardening (sirf `apply_geometry`: `ScaleFactorChanged` ab `set_size(PhysicalSize)` SE PEHLE dispatch — Slint adapter physical→logical conversion current scale se karta hai, `set_scale_factor` geometry refresh nahi karta; DPR 1.5 host par galat order 1.5x oversize + right-side clip karta). Koi .slint/auth/trading/strategy change nahi. VERIFIED: offscreen production-DLL probe (FYERS snapshot: Zerodha-Connected + Fyers-selected LOGIN_REQUIRED) — sidebar, FYERS selected, compact Not-Connected pill + PAPER badge, App ID/Secret/Client ID/TOTP/PIN, Connect to Fyers CTA, 5-step progress, no legacy screen; `cargo test -p vayren-system-view --lib` 7/7; fmt diffs sirf pre-existing concurrent-stream hunks me (mere hunk clean). Dhyaan: screen-CAPTURE hamesha physical pixels me karo (1295×1.5=1942) — logical-size bitmap right/bottom kaat deta hai (yeh pehle "missing pills" ka galat alarm tha). Local only, no commit/push.
**Last update (fyers-selenium-auth, 2026-09-19):** FYERS SELENIUM-FIRST AUTH — user requirement (Zerodha-concept parity, NO manual auth_code/token/URL copy-paste). Naya `02_data/data/provider/selenium_driver.py` (broker-agnostic: lazy selenium, Selenium-Manager-first chromedriver chain, headless Chrome + anti-automation posture, explicit-wait + redirect-param capture, secret-free auth log, BROWSER_UNAVAILABLE/PAGE_CHANGED/CHALLENGE/LOGIN_FAILED/TIMEOUT taxonomy). Naya `02_data/data/provider/fyers/selenium_auth.py` (`FyersSeleniumAuthEngine.ensure_session`: stored probe → triple check → Chrome drives REAL `generate-authcode` pages (live-probed: `#fy_client_id` → six OTP boxes `#first..#sixth` + `#confirmOtpSubmit` → `#verifyPinForm #first..#fourth` + `#verifyPinSubmit` → first-use approve click/host-scoped → `auth_code`+`state` redirect → SAME `validate-authcode` exchange/store/`profile` validate; NO password field — FYERS flow has none; CAPTCHA/recaptcha + SMS-OTP mode + unclickable approve → CHALLENGE with exact user action, no bypass). `factory.py`: `_auto_authenticate` Selenium-first (infra-missing → API `FyersAutoAuthEngine` fallback, logged; auth failures never masked), `interactive_login` → Selenium wrapper (valid-session short-circuit kills the post-auto manual-browser double-open; triple → Chrome; else legacy manual unchanged). `broker_manager.py`: `_job_login` passes `config=` ONLY when the callable accepts it (`_accepts_keyword`) — Zerodha `wait_for_login_token` keeps its exact call (signature-asserted in tests). Zerodha files ZERO touch (git diff empty). UI/.slint ZERO touch.
VERIFIED: NEW 12 fake-driver unit tests + 1 REAL headless-Chrome (v153) browser test vs loopback mock FYERS pages (server-recorded genuine submissions: fy_id + 6-digit TOTP + 4-digit PIN → MOCK-AUTH exchanged → CONNECTED); fyers suites 35/35, manager 24/24, 09_broker 215/215; ruff check+format clean (7 files), pyright new files 0 errors (factory/manager only pre-existing cross-chapter import-resolution + one pre-existing `object is not iterable`); imports/language-ownership (625 files)/arch-gate/structure/migration validators ALL PASS. PRE-EXISTING (stash-proven, not mine): `test_provider.py::test_core_engine_imports_are_broker_free` (subprocess lacks chapter PYTHONPATH). NOT DONE (needs operator): real FYERS credential run — vagator/API field names untouched by this change, but live TOTP/PIN + first-use approve + any FYERS bot-friction (headless) can only be proven with real credentials; `VAYREN_SELENIUM_HEADED=1` exists for that debugging. Local only, no commit/push.
**Last update (backtest-validation-rust-slice, 2026-09-18):** BACKTEST VALIDATION MIGRATION — aakhri Rust backlog item (`06_backtest/backtest/validation.py`, 28 lines). Naya `rust/vayren-core/src/backtest_validation.rs`: 7-check bitmask decision (`validate_form`, messages byte-identical incl. em-dash, cap if/elif exclusivity, NaN behavior Python-jaisa) + 6 parity tests. `lib.rs` me `vy_backtest_validate_form` C-ABI (sirf ints/f64, fail-closed all-set). Naya `06_backtest/backtest/native_validation.py` bridge (risk-kernel pattern: pack_env me string/set marshalling, check_mask FFI). `validation.py` ab sirf message renderer hai (signature same, koi independent check nahi). `language_retention.json` me bridge entry + validation reason update (TEMPORARILY_RETAINED barkarar, modes.py pattern).
VERIFIED: `cargo test -p vayren-core --lib` 254/254 (6 naye), `rustfmt` clean (meri files), pytest backtest 54/54 (Rust delegation se, VAYREN_NATIVE_LIB=debug cdylib), edge battery (whitespace-timeframe truthiness preserved, NaN no-error, AttributeError path same, message order exact), ruff/pyright/imports/structure/language-ownership (621 files) PASS. **ACTUAL MIGRATION REMAINING = 0.** Dhyaan: `python -c`/pytest seedhe chalane par `backtest` package `VAYREN-UI` checkout se shadow hota hai (global PYTHONPATH) — verify hamesha repo chapter-dirs wale PYTHONPATH + fresh cdylib ke saath karo. Release cdylib rebuild baaki hai (production flow: `python scripts/build_rust.py`). Local only, no commit/push.
**Last update (execution-migration-phase-2-3, 2026-09-18):** EXECUTION MODULE MIGRATION (Phases 2 & 3: Paper Simulation, Order Fill Folding, and Pre-Live Readiness Authority). (1) `rust/vayren-core/src/lib.rs` added C-ABI exports: `vy_exec_paper_calculate_fill` (deterministic fill price, slippage, affordable size clamp, notional, and commission accounting), `vy_exec_order_apply_fill` (weighted average fill price and cumulative quantity folding), and `vy_exec_check_live_readiness_basic` (pre-live gates evaluation). (2) `08_execution/execution/native_execution.py` added typed ctypes bindings `native_paper_calculate_fill`, `native_order_apply_fill`, and `native_check_live_readiness_basic`. (3) `08_execution/execution/broker/paper.py` delegated `PaperBroker.settle` fill math to native Rust kernel. (4) `08_execution/execution/engine.py` delegated `ExecutionEngine.apply_fill` order-level fill folding to native Rust kernel. (5) `08_execution/execution/runtime/session.py` delegated `check_live_readiness` gate checks to native Rust authority.
VERIFIED: `cargo test -p vayren-core` 219/219 passed; `pytest 08_execution/execution/tests` 143/143 passed in 3.00s; `validate_language_ownership.py` (620 files), `validate_imports.py`, `validate_architecture_gate.py` all PASSED; `cargo fmt` clean; `ruff check 08_execution` and `ruff format` 0 errors. Local only, no commit/push.
**Last update (execution-migration-phase-1, 2026-09-18):** EXECUTION MODULE MIGRATION (Phase 1: Portfolio Ledger Snapshot & Reconciliation Authority). (1) `rust/vayren-core/src/lib.rs` added and exported C-ABI functions: `vy_exec_reconcile_funds` (equity tolerance matching against broker truth), `vy_exec_verdict_blocks_live` (verdict live-blocking check), and `vy_exec_ledger_snapshot` (account equity, available capital, and day PnL folding over position marks in Rust). (2) `08_execution/execution/native_execution.py` configured strict ctypes `argtypes` and `restype` signatures for all 7 execution C-ABI exports, preventing x86_64 ABI float register corruption; added Python wrappers `native_reconcile_funds`, `native_verdict_blocks_live`, and `native_ledger_snapshot`. (3) `08_execution/execution/portfolio/ledger.py` delegated `snapshot()` to `native_ledger_snapshot`. (4) `08_execution/execution/portfolio/reconcile.py` delegated `reconcile_funds()` and `ReconciliationVerdict.blocks_live` to native Rust authority. (5) Removed dead duplicate state `_ARM_TRANSITIONS` from `modes.py` and `TRANSITIONS` from `runtime/lifecycle.py`, solidifying single source of truth in Rust.
VERIFIED: `cargo test -p vayren-core` 219/219 passed; `pytest 08_execution/execution/tests` 143/143 passed in 2.81s; `validate_language_ownership.py` (620 files), `validate_imports.py`, `validate_architecture_gate.py` all PASSED; `ruff check 08_execution` and `ruff format` 0 errors. Local only, no commit/push.
**Last update (execution-core-rust-bridge, 2026-09-18):** EXECUTION CORE RUST BRIDGE — Constitution §5/§11 compliant slice migration. (1) `rust/vayren-core/src/kill_switch.rs` + `Cargo.toml`: `serde_json` dependency completely removed and replaced with std-only string parsing/formatting; `vayren-core` restored to 100% dependency-free std-only per policy, passing `validate_language_ownership.py`. (2) `rust/vayren-core/src/lib.rs` added C-ABI exports: `vy_exec_arm_transition`, `vy_exec_lifecycle_transition_allowed`, `vy_exec_planner_plan`, `vy_exec_ledger_apply_fill`. (3) New Python bridge `08_execution/execution/native_execution.py` wrapping these exports with typed ctypes signatures. (4) Delegated `modes.py` (`arm_transition`), `runtime/lifecycle.py` (`StrategyLifecycle.transition`), `planner.py` (`OrderPlanner.plan`), and `portfolio/ledger.py` (`PositionLedger.apply_fill`) to the native Rust execution authority. (5) `90_brain/language_retention.json` updated with `native_execution.py` entry and `migrated` tracking.

VERIFIED: `cargo test -p vayren-core` 219/219 passed; `pytest 08_execution/execution/tests` 143/143 passed in 1.98s (was 3.81s, ~2x speedup); `validate_language_ownership.py` (620 files), `validate_imports.py`, `validate_architecture_gate.py` all PASSED; `ruff check 08_execution` and `ruff format` 0 errors. Local only, no commit/push.
**Last update (indicator-3-action-toolbar, 2026-09-17):** INDICATOR TOOLBAR 3-ACTION FIX (eye/settings/delete, source+more removed). Root causes fixed: (1) settings did NOTHING (`_on_indicator_settings` sirf log) → ab real: native Slint `IndicatorSettingsPopup` (SpinBox rows, SAVE/RESET/CANCEL) + retained-Qt fallback dialog; SAVE → wire `indicator:params:NAME:{json}` → `widget.set_indicator_settings` → `_run_strategy_plots` merges stored params over strategy `param_defaults` (logic untouched) → chart immediately updates. (2) delete inconsistent (Rust OBR ko hidden-listed rakhta تھا, Qt `False` flag) → ab dono full-remove; snapshot `indicators` PANEL ROWS se banta hai isliye deleted name refresh/timeframe/symbol par wapas nahi aata; `remove_owner` saare series/markers/rays/labels clear karta hai (verified `PlotStore.remove_strategy`). (3) icons: naya asset `vayren_3_action_toolbar.svg` (eye/eye_off/settings/delete, currentColor) — Qt `QSvgRenderer` element-id render, Slint `IndicatorIcon` vector `Path` parity (SVG feature nahi, font glyphs hate). Files: panel/dialog/widget/tests(Qt 15), slint_market_host+bootstrap+host-test, market.slint+market.rs(Rust 112)+lib.rs+market_host.slint+render_market.rs(Rust 9). `language_retention.json` me dialog entry (TEMPORARILY_RETAINED); gate me Qt-subclass ban tha isliye dialog class nahi, `build/read_indicator_settings` functions hain.
VERIFIED: Rust `cargo test -p vayren-shell --lib` 112/112 (naya `settings_popup_semantics`), `cargo test -p vayren-market-view` 9/9 (pixel-click: eye→vis wire, settings→no wire, delete→rm wire, no source wire), `cargo fmt` clean; Python chart 228/228 + toolbar 15/15 + host 13/13 + strategy_plots 1/1; ruff/pyright/imports/structure/arch-gate PASS. PRE-EXISTING/NOT-MINE (isolate-proof): `validate_language_ownership` serde_json-in-vayren-core + `validate_imports` .kilo/worktrees path + `vayren-core` edits (concurrent stream ke uncommitted); frame-size host tests sirf reverse-order combined run me fail (existing chart-conftest pollution — `test_chart_window.py` se bhi reproduce, standard order 28/28 green). OBR/OBR-Visual-Probe/Stochastic: eye/settings/delete isolated-verified. Local only, no commit/push.
**Last update (execution-rust-slice, 2026-09-17):** EXECUTION RUST SLICE — aakhri bada backend module, saari Python files untouched. Naya `rust/vayren-core/src/execution_engine.rs`: ExecutionEngine (create/transition/apply-fill/reconcile/snapshot/restore, exact error strings, epoch clock default, UNKNOWN-target "state unknown"), planner + `(0,1]` prefs, modes (arm table, 5 gates, env `"true"`-only, resolve + downgrade reasons), lifecycle table (11 states, `live==RUNNING`), check_live_readiness (11 reason strings, live-verified), Ledger fold (partial-reduce avg=fill-price + pro-rata commission — backtest Position math se deliberate alag, probe-pinned), reconcile fns + verdicts, latency percentile/segments. Naya `execution_events.rs` (16 events + pipeline bus test). `lib.rs` +2. REUSED untouched: order_state table, execution models, risk_engine::py_float. Parity live probe se (`Temp/opencode/exec_probe.py`): engine paths, ledger numbers (99.0/100249.0/249.0), reconcile strings, modes/lifecycle. Do parity catch test me mile (CREATED-stamp + reconcile table-bypass — with_state semantics).
VERIFIED: Rust 190/190, fmt clean; Python 274 (exec+risk+backtest+bus+market) + 284 (data+strategy) = 558 green. BACKEND CORE COMPLETE: bus/market/data/backtest/risk/execution sab ke Rust twin ready, Python production authority barkarar. Baaki: broker SDKs (Python-owned), UI Slint, strategy/AI (permanent Python). Perf future phase. Local only, no commit/push.
**Last update (risk-rust-slice, 2026-09-17):** RISK RUST SLICE — decision-orchestration migration, saari Python files untouched. Naya `rust/vayren-core/src/risk_engine.rs`: 18-check `evaluate` (order/names/reasons byte-identical, duplicate-memory approved-only), `within_session` + `clock_sane` (ISO parse, fail-closed), `py_float` (repr-exact reason numbers: `5.0`, `1e+16`, `nan`), models (Policy/Request/Check/Decision, Python defaults). REUSED untouched: auto-generated `risk.rs` kernel (in-process call, no duplicate math), `kill_switch.rs` (engine takes `kill_halted` bool — composition test me engaged switch deny prove karta hai), `vy_risk_kernel` FFI (koi change nahi). RISK ka koi EventBus traffic nahi (manifest) — bus no-op deliberate. Parity live probe se (`Temp/opencode/risk_probe.py`): 22-case battery exact (NaN qty → qty+position deny jaise Python, boundary `<=`/`>=` pass, SELL+0-capital pass, DUP memory, KILL deny).
VERIFIED: Rust 177/177 (6 naye), fmt clean; Python 274 (risk+backtest+execution+bus+market) + 284 (data+strategy) = 558, sab green. Python RiskEngine/KillSwitch abhi bhi production authority. Perf out of scope. Local only, no commit/push.
**Last update (backtest-rust-slice, 2026-09-17):** BACKTEST RUST SLICE — computation core migration, saari Python files untouched. Gaps closed in `backtest.rs` (additive): `close_signal` (runner opposite-signal exit ke liye missing tha), `open_position` view, `into_trades`. Naya `backtest_engine.rs`: `execute_bars` loop-twin (warmup feed, SL/TP-priority close bar khata hai, signal entry/exit, END close; strategy injected `SignalSource` trait — strategies Python-owned §2, isliye loop mechanics port hua, strategy nahi) + `slice_indices` + `assemble_curve`/`assemble_report` (metrics kernels reused, zero duplicate math). Naya `backtest_events.rs` (5 events + request defaults + lifecycle bus test). `lib.rs` +2 lines. Parity live probe se (`Temp/opencode/bt_probe.py`): 3 trades field-by-field 1e-9 (SL/SIGNAL/END, qty/pnl/commission/r_multiple).
VERIFIED: Rust 171/171 (8 naye), fmt clean; Python backtest 37/37, risk+bus+market 94/94, strategy 105/105, execution 143/143. Python `execute_bars`/runner/worker/UI/execution-history abhi bhi production authority hain. Perf out of scope. Local only, no commit/push.
**Last update (data-processing-rust-slice, 2026-09-17):** DATA_PROCESSING RUST SLICE — pure-compute migration, saari Python files untouched (koi big-bang nahi). Naya `rust/vayren-core/src/download.rs` (queue job-builder + priority/order, sweep chunk-windows + `forward_sweep` + sentinel/abort/head-zero rules, coverage decision + LISTING_START `<=` rule, calendar math, ts normalise/parse, candle normalise + volume clamp, worker date-validation exact strings, provider error codes + canonical intervals) + naya `data_events.rs` (8 data events + worker reporter-mapping constructors + bus round-trip + sweep→progress bus flow). `lib.rs` +2 lines. Ground truth live Python probe se (`Temp/opencode/data_probe.py`): chunk counts, sweep windows, queue jobs/reasons/order, trading-day counts, normalise vectors, live SCAN (PARTIAL 43 rows 1.71%, boundary-suppress, ghost NOT_STARTED).
VERIFIED: `cargo test -p vayren-core --lib` 163/163 (17 naye), `cargo fmt --check` clean, Python `02_data` 179/179 (3 Qt UI files excluded, untouched), market+bus 73/73. KNOWN ODDITY (preserved, fix nahi): UI `DownloadRequest`/`CoverageRequest`/`CancelDownload` bus par publish karta hai lekin bootstrap worker ko kabhi subscribe nahi karta — request path production me dead hai, sirf tests worker ko direct chalate hain; manifest `events_consumed` ka ulta. Isko jodna behavior change hota, isliye migration me nahi chheda. Perf out of scope (task order); `throttle.py` pehle se Rust me tha, dubara nahi banaya. Local only, no commit/push.
**Last update (market-data-rust-slice, 2026-09-17):** MARKET_DATA RUST SLICE — pure-compute migration, Python production path untouched (koi big-bang nahi, koi Python file change nahi). (1) `market.rs` additive: poora `TIMEFRAME_LADDER` (11 entries) + `timeframe_seconds` (case-insensitive + `90m`/`2D` generated parse) + `timeframe_name`/`generate_label`/`available_timeframes` — purana `Timeframe::to_seconds` sirf 5 labels janta tha (30m/45m/2h/4h/1W missing tha, real gap); `SymbolQuote::from_bar` (price=close, change_pct=return_pct); `fetch_window` (`(limit+1)*ratio`, None passthrough) + `tail_bars` (`bars[-limit:]`, `limit=0` quirk preserved+pinned). (2) Naya `market_events.rs`: 8 market events (LoadSymbol/TimeframeChanged/ListSymbols/ListTimeframes + SymbolsListed/TimeframesListed/QuotesLoaded/DataLoaded) + loader-semantics handlers (fetch-fail → no publish/false) + `QuoteDeduper` (identical universe no-op, fail par guard unset = retry). `lib.rs` me 1 line (`pub mod market_events;`).
VERIFIED: `cargo test -p vayren-core --lib` 146/146 (13 naye market/market_events tests, vectors live Python se liye), `cargo fmt --check` clean, Python `03_market` 67/67 (untouched). BENCH (honest, no hype): 60k rows par buckets EXACT match, lekin rust-backed path (301ms) pure-Python dict oracle (61ms) se slow — bottleneck Python-side parsing/marshalling hai, kernel nahi; agli jeet timestamp-parsing ko Rust me le jane se aayegi, kernel se nahi. `Bar::is_valid`/`Symbol` NOT Python parity hain (additive, concurrent stream ke) — chheda nahi. Local only, no commit/push.
**Last update (eventbus-rust-parity, 2026-09-17):** RUST EVENTBUS PARITY — `unsubscribe` gap closed (untracked `rust/vayren-core/src/event_bus.rs` par kaam, Python authority untouched, koi big-bang nahi). `subscribe` ab opaque `HandlerId(u64)` deta hai (`Box<dyn Fn>` identity-compare nahi ho sakta isliye), `unsubscribe::<E>(id) -> bool` (Python ke `ValueError` ki jagah `false` — Rust me exceptions nahi); ordering/exact-type-dispatch/panic-isolation same. 3 naye parity tests (`unsubscribe_removes_handler`, `unsubscribe_unknown_id_returns_false`, `unsubscribe_keeps_remaining_handlers_in_order` — `01_core/core/tests/test_event_bus.py` ke mirror).
VERIFIED: `cargo test -p vayren-core --lib` 133/133 (event_bus 9/9), `cargo fmt --check` clean, Python `test_event_bus.py` 6/6 (untouched). Python `EventBus` abhi bhi single authority hai (bootstrap wiring same); Rust bus future pure-Rust modules ke liye ready kernel hai, FFI swap is phase me nahi. Local only, no commit/push.
**Last update (dead-code-delete-2, 2026-09-17):** DEAD CODE DELETE — user request par 2nd sweep (~128 lines). Removed: `dt_str` (data/calendar.py), `fmt_range` (data/reporter.py), `_auth_header` (fyers/live_auth.py), `_median_gaps` (chart/models/timeframe.py), `ensure_default` + `ensure_builtin_strategies` (strategy/language/storage.py) + unke `language/__init__.py` export entries, `strategy/README.md` stale line, bootstrap stale comment; `web/src/App.tsx` se TODO placeholder useEffect + dead `onStrategySelect` wiring + empty handlers + chart-placeholder div (45→28 lines). `LEGACY_OBR_CODE/NAME` + `DEFAULT_CODE/NAME` rakhe (tests/bootstrap me live). `ir` params (`create_snapshot`/`replay_execution`) NAHI chhede — 16 live callers wala compat shim hai, hatana API break hota.
VERIFIED: ruff format+check clean, imports/structure PASS, suites: lab_source_actions+timeframe+execution 29/29, calendar+fyers_live+fyers_auto+version_control 59/59, describe 3/3. Local only, no commit/push.
**Last update (fyers-auto-auth, 2026-09-17):** FYERS FULLY AUTOMATIC LOGIN — ZERODHA-EXPERIENCE PARITY (desktop `vayren.py` v5.3.0 reference). Naya `02_data/data/provider/fyers/auto_auth.py` (`FyersAutoAuthEngine`: saved-session probe → official vagator TOTP+PIN API flow (`send_login_otp` → `pyotp` TOTP → `verify_otp` → `verify_pin` → Bearer `/token` → auth_code → `validate-authcode` → store → validate; 2 attempts, auth log, secret-free messages) — Zerodha `AuthEngine` ka step-by-step mirror, Selenium ki zaroorat nahi kyunki FYERS apne login endpoints deta hai; koi page scraping/CAPTCHA touch nahi). Zerodha files ZERO touch. `FyersCredentials` + schema me `client_id`/`totp_secret` (layered store→`VAYREN_FYERS_*` env); triple na ho to exact missing piece + interactive-browser fallback (capture automatic rehta hai). `BrokerSpec.auto_authenticate` hook + manager `_try_auto_auth` (worker thread, AUTHENTICATING status; `None` = Zerodha legacy path identical); factory closure wires real FYERS auto-auth; startup/check par missing/expired session auto-connect, unreachable par seedha DISCONNECTED. BONUS REAL BUGFIX: FYERS edge stdlib `Python-urllib` UA par bare-403 deta hai — `_UrllibTransport` ab har request par browser UA bhejta hai (loopback test pinned), warna saare token/profile calls galat diagnose hote.
VERIFIED: NEW 13 tests (9 auto-auth incl. full payload-shape/payload-order/Bearer/appIdHash asserts + UA loopback probe, 4 manager hook incl. exploding-hook safety) + schema-order updates; suites: 02_data 234/234, 09_broker 215/215, manager 23/23; ruff/format/pyright mere files clean; imports/structure PASS (language/arch-gate fail sirf concurrent stream ki untracked `indicator_settings_dialog.py` par — untouched). REMAINING EXTERNAL (user ke paas, ek baar): FYERS portal me TOTP 2FA on karo + secret note karo; App ID/Secret + Client ID + TOTP secret + PIN SYSTEM→BROKERS me save karo (ya env vars); phir VAYREN RUN → FYERS auto-login → CONNECTED, koi copy/paste nahi. Pehle real-credential run par vagator field names confirm ho jayenge (defensive parsing + honest errors hain). Local only, no commit/push.
**Last update (rust-core-migration-batch-1, 2026-09-17):** RUST CORE MIGRATION — BATCH 1 (numeric kernels, ~30% coverage). Migrated modules: (1) `kill_switch` (engage/disengage/query, JSON persist, 3 tests); (2) `execution` models (Position ledger math w/ fill accounting — open/average/close/flip/realize, Order vocabulary — BrokerOrder.advance w/ order_state transition validation, Intent/Plan/Fill/Account, Side/OrderType enums, 16 tests); (3) `throttle` (API rate limiter, monotonic wait/ready/reset, 5 tests); (4) `chart_math` (viewport coordinate transforms — bar_to_x/price_to_y/x_to_bar/y_to_price, pure numeric, 12 tests). **90/90 Rust tests pass** (`cargo test -p vayren-core --lib`). Python layers untouched — ये bridges future phase mein banenge. Constitution §5/§13 invisible migration: sirf directly-related numeric slices, koi repo-wide sweep nahi. `language_retention.json` update pending (files abhi exist karte hain — delete phase alag). Modules: `rust/vayren-core/src/{kill_switch,execution,throttle,chart_math}.rs` + `Cargo.toml` serde_json dep + `lib.rs` registration. Local only, no commit/push.
**Last update (universal-broker-auth-fyers, 2026-09-17): UNIVERSAL BROKER AUTHENTICATION — FYERS AS SECOND VENUE (Zerodha flow byte-identical). Naya `09_broker/broker/auth.py` (universal contract: `AuthCapability` 13 values, `AuthField`, `ConnectionState`, `AuthErrorCode`, `AccountIdentity`, secret-free `AuthResult`, `BrokerAuthContract` protocol, `mask_secret`/`schema_keys`/`describe_schema`; stdlib-only, exported from `broker/__init__.py`). Naya venue `02_data/data/provider/fyers/` (stdlib `urllib` only, koi SDK dep nahi): `live_auth.py` (official FYERS v3 OAuth — `generate-authcode` URL → auto browser open → localhost `127.0.0.1:9475/vayren/fyers-callback` par `AuthCodeCapture` auth_code+state-CSRF-check → `validate-authcode` (`appIdHash=sha256(app_id:secret)`, once in-memory) → `FyersSessionStore` (`vayren:fyers:session`, OS vault/file fallback) → read-only `profile` validate; expired codes -8/-15/-16/-17 ya HTTP 401 = LOGIN_REQUIRED), `session_adapter.py` (read-only `FyersSessionAdapter`: connect/health/account/funds/positions/open_orders — `place_order` EXIST HI NAHI KARTA, trade impossible), `credentials.py` (layered store→`VAYREN_FYERS_*` env, `repr` secret-free), `adapter.py` (`FyersProvider`: EMPTY capability set — history `PROVIDER_UNAVAILABLE` fail-closed, apna schema `app_id`/`secret`/`redirect_uri`/`pin`). Naya `09_broker/broker/adapters/fyers/` (identity/caps/record, injected factory — transport import nahi). `BrokerSpec` me venue-neutral fields (`key_field`/`secret_field`/`redirect_uri_field`/`config_key_map`, Zerodha defaults = purana behavior); `BrokerManager` ab spec keys se baat karta hai (koi `if broker ==` branching nahi) + per-broker `callback_url` (top-level compat rakha); bootstrap selected-broker URL prefer karta hai. `factory.py` me `_seed_fyers()` + `fyers_management_spec()` (auth-only `venue_register` — UBL registry me `fyers-live` venue BANTI HI NAHI, isliye order-routing impact zero); `BrokerManager._default_specs` = zerodha+fyers. Zerodha files ZERO behavior change (sare defaults purane literals).
VERIFIED: NEW 63 tests (auth 7, fyers-adapter 10, fyers-live-auth 17 incl. real loopback capture+state-mismatch+secret-free asserts, fyers-provider 9, manager +6 incl. real-spec no-network wiring) + updated alias-prefix test; suites: 09_broker 215/215, 02_data 225/225, 00_app 301/302 (1 fail `test_download_panel_is_embedded_in_main_window` PRE-EXISTING — mera koi diff nahi us file me, purani memory me stash+isolate proof), selection/CLI 21/21; ruff+format clean, pyright mere files 0 errors (full-repo 6 baaki concurrent stream ke: bootstrap 2128/2848 + lab_source_actions x4 — untouched), imports/structure/language/arch-gate PASS. REMAINING EXTERNAL (user ke paas): FYERS dashboard me app banao + redirect `http://127.0.0.1:9475/vayren/fyers-callback` register karo + SYSTEM→BROKERS me App ID/Secret save karke CONNECT dabao (FYERS login/OTP browser me user karega, baad ka sab automatic — koi copy/paste nahi). FYERS history/download + trading future phases (explicit out of scope). Local only, no commit/push.
**Last update (watchlist-scroll-direction-fix, 2026-09-17):** NATIVE MARKET WATCHLIST SCROLL INVERSION FIXED (root cause, not UI workaround). Symptom: wheel UP moved list DOWN and vice versa — sirf native Slint Market watchlist mein (Qt `SymbolListWidget` mein koi custom wheel code nahi, wahan direction hamesha natural thi). ROOT CAUSE: `slint_market_host.py::wheelEvent` ne `-delta.y()` forward kiya tha — ye negation `market.rs::WheelZoom` ke `ZOOM_STEP.powf(steps)` (Qt ke `**-steps` se deliberate divergence, steps>0 = zoom OUT) ko compensate karne ke liye thi taaki wheel UP = zoom IN rahe, lekin wahi negated `PointerScrolled` delta Slint `ScrollView` (watchlist/download/log/popup) ko bhi milta tha → list scroll ulta. FIX (3 files, chart behavior byte-identical): (1) host ab natural `+delta.y` forward karta hai (baaki 4 hosts — live/system/research/portfolio — se parity); (2) `market.slint` chart `scroll-event` mein SIRF `wheel-zoom` ke liye `-steps` (steps<0 = zoom-in contract preserved; `price-zoom` untouched — uska Rust kernel `PRICE_ZOOM_STEP.powf(-steps)` pehle se Qt-parity mein tha, host fix ne uska pre-existing inversion bhi theek kar diya); Rust `MarketAction` semantics + `market.rs` unit tests untouched. (3) `render_market.rs` zoom test `-120` → `+120` (ABI delta ab natural: UP = positive = zoom IN; OUT full-history par capped hota isliye repaint ke liye IN chahiye).
VERIFIED headless ABI probe (`Temp/opencode/watchlist_scroll_probe.py`, 200-row universe): DOWN 2000px → T117→T167 (exact +50 rows), UP 2000px → wapas T117 (exact −50, symmetric), top/bottom overscroll clamp-stable (consecutive frames identical), watchlist-region scrolls se ZERO chart zoom/pan actions, tap-select alive throughout. Suites: market-view 11/11, shell-market 34/34, host 13/13, Qt watchlist 26/26; ruff/pyright/language-ownership clean. PRE-EXISTING failures NOT mine (untracked concurrent files): `validate_imports` SyntaxError `02_data/.../fyers/session_adapter.py` + `cargo fmt` diff `rust/vayren-core/src/market.rs` (dono `??` untracked, untouched). NOTE (follow-up, out of scope): `slint_strategy_lab_host.py` mein wahi `-delta.y` negation hai aur `lab.slint` mein koi custom scroll handler nahi → Lab ke ScrollViews bhi inverted honge; task scope (sirf Watchlist) ke bahar isliye untouched.
**Follow-up (chart-zoom parity, same day):** User ne report kiya chart zoom ulta lag raha hai. Diagnosis: mera Python-host change turant live ho gaya tha, lekin app `rust/target/release/vayren_market_view.dll` load karta hai jo file-lock (`Access denied`) ke kaaran purana reh gaya tha → transient mixed state (naya host + purani Slint = chart inverted). Fix: kuch revert nahi kiya (list fix intact); release DLL dobara build kiya (13m40s, success) taaki dono layers consistent hon. VERIFIED release DLL par pixel-geometry se (`Temp/opencode/wl_release_zoom_check.py`, 160-bar view): UP (+120) → runs 260→232, mean width 3.23→3.62 = zoom IN (pehle jaisa); DOWN (−120) → 260→262, 3.23→3.21 = zoom OUT. User ko app restart karna hoga (purana DLL running process mein loaded hai). Local only, no commit/push.
**Last update (strategy-lab-remove-transaction-cost, 2026-09-17):** STRATEGY LAB (NATIVE SLINT) SE "TRANSACTION COST" UI ELEMENT POORI TARAH HATAAYA — toolbar ab UNIVERSE → TIMEFRAME → DATE RANGE → CAPITAL (4 groups, stretch spacer reflow, koi broken gap nahi). Touched (sirf UI slice, 5 files): `rust/vayren-shell/ui/lab.slint` (TRANSACTION COST VerticalLayout block + `LabHeader.cost` field + dead `config-labels` property + stale "costs are a fixed disclosure" comment), `rust/vayren-shell/src/lab.rs` (`LabConfig.cost`, `fingerprint()` 4-field, `LabView.cost`, `project()`, `apply_snapshot_json` read, test fixture), `rust/vayren-shell/src/shell.rs` (2 bindings/fixtures), `rust/vayren-strategy-lab-view/src/lib.rs` (binding + snapshot fixture JSON), `00_app/app/services/slint_strategy_lab_host.py` (snapshot `config["cost"]` producer line 224-226 — sirf display string, engine params untouched). ENGINE UNTOUCHED: `slippage_pct`/`commission_pct` config + backtest/commission/slippage calcs + Qt lab advanced-costs panel (`test_run_config_has_fixed_costs`, "slippage" advanced text) sab as-is.
VERIFIED (isolated HEAD worktree + patch, concurrent stream ko touch kiye bina): cargo build clean (Slint 1.17.1), `cargo test -p vayren-shell -p vayren-strategy-lab-view` green (109 lib incl. lab fingerprint/projection tests + 1 snapshot test + render suites), `rustfmt --check` clean, Python `test_slint_strategy_lab_host.py` 10/10 + ruff clean. Offscreen render probe (temp test, worktree-only): lab screen 1600x900 + 1280x800 rasterize OK, PPM evidence `Temp\opencode\labshots\patched\` (+toolbar_top.png) — 4 labels aligned, COST text kahin nahi. NOTE: main-tree `cargo build` is waqt concurrent stream ke uncommitted changes se fail hota hai (market.slint me SpinBox/stroke-linecap jo vendored Slint 1.17.1 reject karta hai + market.rs E0308) — ye mere changes nahi, maine un files ko chhua tak nahi; HEAD par meri patch ke saath sab green hai. Gotcha: worktree me stale slint codegen ne E0063 diya tha → `cargo clean -p` se fix (22GB stale artifacts). Local only, no commit/push.
**Last update (dead-code-cleanup, 2026-09-17):** LEGACY QT WORKSPACE DEAD CODE REMOVED. AST orphan scan (non-test source: module imported OR public symbol referenced) pe 3 aisi files mili jinhe production kabhi construct nahi karta (native Slint viewport canonical): `00_app/app/ui/{brokers,live,portfolio}_workspace.py` (~3071 lines). Proof: in teen ke saare production references sirf comments/docstrings mein the; `app/ui/__init__.py` khud likhta tha "intentionally NOT re-exported". Deleted + unke 3 direct-widget test files (`test_{brokers,live,portfolio}_workspace.py`) + 1 zero-byte mangled-name junk artifact (`90_brain/migration/evidence/.../pre_wire/scripts__migration__agent__oracles____init__.py`).
REAL DEPENDENCIES FIXED (na tode): `chart_window.py` — dead `live_workspace`/`brokers_workspace` params + storage + addWidget blocks + dead `show_live()` removed (koi caller nahi; bootstrap `show_slint_live` use karta hai, `show_brokers` already `_slint_system_host` route karta tha); `bootstrap.py` — `live_workspace=None` kwarg hataaya; `test_navigation.py` — dead imports + 2 negative `findChildren(<DeletedClass>)` tests hataaye; `test_responsive_geometry.py` — dead imports + `_live_state()` helper + 4 dead tests (Research/TopNavBar tests kept). Stale comments/docstrings fix: `bootstrap.py` (5 jagah), `top_nav_bar.py`, `slint_live/portfolio/system_host.py` ki lineages, `app/ui/__init__.py`, `module_contracts.md`, `broker_layer_design.md`, `language_retention.json` (3 entries: 2 removed, brokers → MIGRATED; `qt_workspace_allowlist` se 2 hataaye).
VERIFIED: ruff 0 NEW errors (2 pre-existing I001 `live_trading_service`/`paper_service` — untouched), imports/structure/language-ownership (603 files, pehle 606) validators sab PASS. PRE-EXISTING failures NOT mine (stash+isolate proof, baseline par bhi same): `test_overlay_renderer.py::test_paint_time_tag_centered_on_crosshair` (test-order pollution, alone passes) + `test_runtime_integration.py::test_download_panel_is_embedded_in_main_window`. Lesson: pehla orphan-scan galti se repo nahi opencode ka Temp folder padh raha tha (ROOT hardcode karna pada). Local only, no commit/push.
**Last update (market-obr-plotting-parity, 2026-09-16):** LEGACY OBR ↔ SLINT MARKET EXACT PLOTTING PARITY (real-data forensics, both bugs fixed + regression-tested). Two production defects found by a real-data probe that runs the canonical `D:\VAYREN_STRATEGIES\OBR.py` on real SQLite (ZEEL/WIPRO/RELIANCE/TCS × 15m/30m/1h) and diffs the legacy Qt `PlotOverlay.paint_overlay` output against the Slint bridge→`market::project()` geometry over the SAME viewport:
(1) **`_run_strategy_plots` early return** (`00_app/app/bootstrap/bootstrap.py`): `if not raw: return` dropped EVERY live OBR plot — OBR emits only universal PlotEvents (plot_ray/plot_marker: REF HIGH/LOW, BUY/SELL/EOD/NO-TRADE) and zero `plot()` series, so `get_chart_series_with_owner()` is always `{}`. Verified production: before fix store_total=0 (no OBR on the chart at all), after fix store_total=8395. Removed the return so series + PlotEvents both flow. (The earlier memory note described this as landed; runtime proved it was not — now it is.)
(2) **`PlotStore.query_visible` spanning scan** (`04_chart/chart/renderer/plot_renderer.py`): a point plot (MARKER) anchored just below `first` did `break`, terminating the before-window scan so RAY/HORIZONTAL_LEVEL records with slightly lower anchors whose coverage reaches INTO the viewport were dropped (left-edge sliver) — Slint culls by `end >= first` and drew them, so the two disagreed exactly at the viewport left edge. Point plots can't span → changed `break` to `continue`.
VERIFIED PARITY (12/12 symbol×timeframe): rays legacy==slint EXACT (94/94 15m, 184/184 30m, 340/340 1h; 0 diff both directions), markers EXACT (66–258 per combo, 0 diff), viewports Qt==Rust identical, EYE OFF → 0/0 rays+markers, symbol switch (ZEEL→WIPRO) 0 stale price-level overlap (no-clear control leaks 270 → production `_on_chart_ready_lab` clear is load-bearing), timeframe switch regenerates (ZEEL 30m 276→1h 514 rays, cap=5 preserved), zoom/pan: sub-window record sets ⊆ full-window, prices viewport-independent (logical coords). Production end-to-end (real Bootstrap, offscreen): OBR ON 144 rays + 100 markers w/ EOD P&L labels + cap=5; native host renders.
Tests: `04_chart/chart/tests/test_overlay_integration.py::test_query_visible_includes_spanning_ray_past_left_edge` (sliver regression), NEW `00_app/app/tests/test_strategy_plots.py` (OBR-shaped strategy ingests PlotEvents with no chart series). ruff format/check clean, pyright 0 errors, imports/structure/language-ownership validators PASS. PRE-EXISTING failures NOT mine (proved by stashing/isolating my edits): `test_overlay_renderer.py::test_paint_time_tag_centered_on_crosshair` and `test_runtime_integration.py::test_download_panel_is_embedded_in_main_window` (concurrent workstream's in-progress LIVE/BROKERS/portfolio disconnect). Local only, no commit/push.
Prior (market-timeframe-switch-fix, 2026-09-16): MARKET TIMEFRAME SWITCHING FIX (only broken handoff): `ChartWindow.on_chart_ready` now force-pushes the snapshot to the native Market host (`_slint_market_host.refresh_now()`) so TimeframeChanged→DataLoaded→ChartReady fresh bars (15m 61676 / 30m 32079 / 45m 22213 / 1h 17278 / 2h 9878 / 4h 4944 on WIPRO, verified real SQLite) reach the Rust ViewModel synchronously with the toolbar highlight (not the 330ms poll). VERIFIED real app offscreen: 15m→30m→45m→1h→2h→4h→30m bars/snapshot tails differ, volume/OHLC from new bars, symbol preserved, symbol-switch keeps timeframe (RELIANCE 30m). Prior (market-one-side-free-pan, 2026-09-16): MARKET chart one-side FREE PAN (`[CANDLES] [LARGE EMPTY SPACE]`); shell market 26/26, market-view render_matrix 8/8, Python market-host+navigation 26/26, arch/language/structure/import PASS.

**When to read:** Every new session start, before deciding what to build next.
**Last update (backtest-directional-rust-slice, 2026-09-18):** BACKTEST MIGRATION SLICE 1 (user-ordered, smallest-first per §13 — poore 21 ek saath nahi). Gap analysis: positions/simulator/journal/metrics/loop/events sab ke Rust twin pehle se ready (`backtest.rs`/`backtest_engine.rs`/`metrics.rs`); asli gap = `engine/directional.py` (side/symbol filter + recompute). Naya `rust/vayren-core/src/backtest_directional.rs`: `indices_for_side`/`indices_for_symbol`/`grouped_indices` (single-pass, requested order, missing→empty)/`split_sides`/`subset_series`/`derive_curve_report` (assemble_curve+assemble_report reuse, zero duplicate math) + 9 parity tests. Ground truth live probe (`Temp/opencode/directional_probe.py`): LONG net 50/pf None/sharpe None, SHORT net 10/pf 1.5/sharpe 11.230360578039203, AAA net 80/sharpe 224.28923773524892, ZZZ zero-report — sab 1e-9. Deliberate difference (documented): invalid side par Python ValueError vs Rust `Side::from_str` None (no exceptions); None-base caller-side. Python authority untouched (koi .py change nahi, retention states same). `lib.rs` +1 line.
VERIFIED: `cargo test -p vayren-core --lib` 199/199 (190 existing + 9 naye), `rustfmt --check` clean (sirf nayi file; concurrent files untouched), Python directional+engine+runner 16/16, imports/structure PASS. Language-ownership wahi 1 pre-existing serde_json error (concurrent stream, stash-proven). Baaki BACKTEST slices (replay/simulator/validation/models/runner-orchestration jo Python strategies ki wajah se glue rahega) agle turns me. Local only, no commit/push.
**Last update (lab-library-cleanup, 2026-09-18):** USER REQUEST — Strategy Lab library cleanup: `D:\VAYREN_STRATEGIES` me OBR ke alawa saari 41 junk strategy files delete (A/A Copy/BatchParityCTA/Dup*/EmptyTest/IntegrationStrat/LiveObrShaped/OBR-Visual-Probe/PathCheckSMA/Renamed/SMA*/SvcObr/TestStrat/Untitled/VC_*×20/WorkerBatchSMA — sab 16/09 ke test leftovers). Pehle full backup `C:\Users\visha\AppData\Local\Temp\opencode\strategies_backup_2026-09-18\` (42 files, OBR hash-verified identical). Library ab `['OBR']` only, record intact (id cf7d7645…, 39671 bytes); `strategy_versions/` dir maujood hi nahi tha, research experiments/runs history untouched (delete nahi manga tha). VERIFIED: lab_source_actions+storage_layout 16/16 (OBR integrity/snapshot incl.). Local only, no commit/push.
**Last update (dead-code-delete-3, 2026-09-18):** DEAD CODE DELETE 3rd sweep (~390 lines net, 14 files, zero behavior change). Method: AST orphan scan (repo ROOT, relative imports resolved) → 4 whole-file candidates, all KEPT (entry point / test fixtures / contract template); symbol scan (129 def-only candidates) → repo-wide proof-grep incl. tests/docs → 24 deletes with zero live refs. Removed: plot_renderer `_paint_markers` (paint_overlay inlines the path)/`clear_owner` (one-line delegate)/`is_empty` (zero py callers); chart_window `show_research` (nav routes to show_slint_research)/`toggle_bottom`+`_bottom_visible`; `attach_worker` (ctor injects worker)/`clear_log`/`set_collapsed`/`attach_bus` (bus always None); research slice — engine `ResearchReport`+`create_dataset`+`generate_report`, governance audit slice (`AuditRecord`+`_audit_path`+`append_audit`+`read_audit`), storage discovery/intelligence slice (4 fns+2 lineage helpers+2 imports), `_hash`, `StatsSummary`, `get_version_graph`; sample-strategy factories `create_obr_sell_v10`/`create_obr`/`create_sma_crossover` (compiler instantiates CLASSES directly, external OBR.py imports only PythonStrategy+MarkerType). KEPT with reason: LEGACY_*/DEFAULT_*/ir (live callers), tools_toolbar mode group (docstring-retained for later phases), `clear_active`/`clear_results` (adjacent concurrent indicator/lab work), Bar/strategy-base helpers (external user strategies), documented contracts, `set_regime` (RESTORED after delete — concurrent stream's test_slint_market_host calls it; lesson: re-grep AFTER deleting, concurrent edits land mid-task).
VERIFIED: ruff format+check clean, imports/structure PASS, suites — strategy+execution 248/248, backtest 80/80, chart+nav+ranking 265/266, app 304/306. PRE-EXISTING/NOT-MINE (stash-proof, baseline identical): `test_configure_table_conventions` (Qt section-size 24 vs 22, order-pollution, alone passes) + `test_download_panel_is_embedded_in_main_window` + language-ownership serde_json (concurrent rust stream). Local only, no commit/push.
**Last update (migration-recount-2026-09-18):** MIGRATION COUNT CORRECTION — purana "34,136 lines baaki" galat tha (twins+glue sabko baaki gin liya tha). Code-derived audit (har .rs ke docs-scope vs har retained .py): authority-MIGRATED 340 lines (3 files), twins-ready 3,377 lines (bus/registry/throttle/market-models+loaders/download-kernels/backtest-loop+engine/risk-engine+kill/chart-slice, parity-tested, Python authority barkarar), forever-glue 11,455 lines (policy: re-export/manifest/event-vocab/Qt-paint/SQL-IO/threads/SDK/strategy-orchestration), TRUE REMAINING sirf ~157 lines (execution normalizer 129 + backtest validation 28) + UI replacement target 19,147 Qt lines (Slint shell ~25k lines, in progress). Method: `Temp/opencode/twin_map.py` + function-level AST splits (session/readiness 52, runner/loop 152, journal-helpers 12).
**Last update (approved-2.1K-engines, 2026-09-18):** APPROVED ~2.1K ENGINE MIGRATION 100% TWIN-COMPLETE — LiveSession pipeline (live_session.rs 21 tests + normalizer.rs 8 tests, live-probe parity incl. overflow branches), BacktestRunner orchestration (backtest_runner.rs 13 tests + backtest_validation.rs bitmask kernel 4 tests, fills the retention-claimed-but-missing module + concurrent FFI vy_backtest_validate_form now resolves), HistoricalDownloadEngine flow (download_engine.rs 13 tests, reuses download.rs kernels, exact strings/outcomes). Zero Python behavior change (5 retention reasons updated, states same); 1-word additive Clone derive on ContractView (required). VERIFIED: cargo 277/277, Python session+runner+engine 28/28, language/imports/structure/arch-gate PASS, rustfmt clean (own files). HONEST BOUNDARY: migration-manifest `verify` stays BLOCKED on 4 units by design — canonical verdicts need production FFI cutover (strategies/venues stay Python = new architecture, explicitly out of scope) and sqlite stays per policy; manifests untouched (no gaming). Pre-existing env quirk noted: pip editable install points at VAYREN-UI (shadows imports on partial pytest runs — use explicit chapter PYTHONPATH). Local only, no commit/push.
**Related:** `architecture.md` (map), `module_contracts.md` (APIs).

**Last update:** 2026-09-12 - RESET ZOOM ACTION (native shell chart slice, router-routed Slint+Rust, zero Python): `migration route` verdict tha split (Slint trigger + Rust viewport state; chart.viewport.math BLOCKED rehna chahiye tha isliye Python forbidden raha). Naya `rust/vayren-shell/src/viewport.rs` — `ChartViewportZoom` state (default 1.0, clamp [0.1, 20.0], NaN→default fail-safe, `reset()`, `label()`), 4 pure unit tests. Slint `app.slint` me chart toolbar (zoom label + Reset Zoom VButton variant 1) jo `reset-zoom` callback emit karta hai; `shell.rs` me `apply_zoom`/`wire_zoom` (single-state `Rc<RefCell>`), `main.rs` wiring; headless UI test me 250%→Reset→100% + idempotency assert. `cargo test -p vayren-shell` 10/10, `cargo fmt --check` clean, arch gate meri files par PASS (4 violations sirf concurrent stream ki Qt files me — §7 as designed), Python files zero touch. Note: Slint tree par concurrent design-system stream live kaam kar raha tha (VayrenDesign/VButton unke uncommitted changes) — meri additions uske upar consistent, build+tests green. Local only, no commit/push. Prior (same day): ARCHITECTURE ROUTER + LANGUAGE ENFORCER (repo-level, not prompt-only): `migration route` (request→domain→rust/slint/python/split/DECISION-REQUIRED), generated `90_brain/migration/architecture_manifest.json` (§21), wrong-language change gate `scripts/validate_architecture_gate.py` + `migration gate` (canonical-unit Python additions, new Qt UI, unretained Rust-domain files = FAIL; bridges/oracles/tests/retained glue pass; debt counter), background worker `migration background` (PID lockfile + stale-steal, dirty-tree skip, Slint exclusion), `migration task-report` (§25 self-check). Wired L2/L5/L6: Makefile `validate-architecture`, CI step, pre-commit hook. Evidence: 38 router/gate/report/worker tests + 698-test scoped suite green, all validators PASS. Found+fixed real bug class en route: `os.kill(pid,0)` on Windows queues delayed SIGINT — worker liveness now uses ctypes OpenProcess (no signals); subprocess calls pinned to UTF-8. Gate honestly FAILs on concurrent stream's new Qt widgets (spec §7 working as designed). Local only, no commit/push. Prior (same day): STRATEGY LAB UI PRESENTATION REBUILD (complete rewrite, presentation-only, zero trading/backtest logic changed): Rebuilt `00_app/app/ui/strategy_lab_workspace.py` completely from scratch into a professional institutional quantitative trading terminal aesthetic. Preserved all state management, event wiring, result ownership (`_capture_owner`, `_owner_matches`, `_refresh_staleness`), BUY/SELL/COMPARE tri-mode and directional derivation, batch execution handling, stock ranking table integration, and public API. Visual hierarchy overhauled: compact Strategy Command Center top bar (`LabTopBar` 42px) with `TOP_RUN_QSS` sticky run mirror, `ExperimentContextBar` orientation strip (`H_CONTEXT` 34px), clean 4-column `BacktestRunPanel` configuration grid with dynamic 4↔2 reflow, 3-tier `MetricsTiles` KPI hierarchy (verdict pill, hero Net P&L `FS_HERO`, tier-2 quality metrics, tier-3 risk metrics), unified scrolling page (`_page_scroll`), full-width analytical tabs (`PERFORMANCE` with first-class stock ranking, `TRADES` with dense trade blotter, `EQUITY`, `DRAWDOWN`), and `_CompareView` with `_StockComparisonBoard` and `_StrongerBanner`. Verification: Pyright 0 errors/0 warnings, Ruff lint/format clean, 60/60 `test_lab_workspace.py` tests PASSED, 19/19 `test_lab_integration.py` + `test_stock_ranking.py` PASSED, responsive verification passed across all 5 target resolutions (1920×1080, 1600×900, 1440×900, 1366×768, 1280×720) in both empty and populated states, import & language ownership validators PASSED. Prior (same day): AUTONOMOUS MIGRATION AGENT (`scripts/migration/agent/`, 22 tests): BLOCKED units ko dependency order me sandbox-analyze → KernelSpec IR → Rust generate → repair loop → differential parity → shadow → promote-on-green/rollback-on-red. Exact order pehle nikala (event_bus → download → storage → risk → session → viewport). Agent ne `risk.engine.evaluate` genuinely migrate kiya: AST se 13 pure scalar checks extract (5 orchestration Python me), `rust/vayren-core/src/risk.rs` + FFI + `native_checks.py` bridge, `engine.py` surgical patch (18 checks order intact, details byte-identical), frozen oracle + golden(29) + fuzz(150) parity PASS, shadow 60/60, RUST_CANONICAL. Baaki 5 honest BLOCKED (glue/Qt/venue-scope refusals). Live: 7 RUST_CANONICAL + 5 BLOCKED. Gates: ruff/pyright/cargo-fmt clean, risk 21/21, migration+agent 56/56, validators PASS. Pre-existing failures untouched. Local only, no commit/push. Prior (same day): STRATEGY LAB — INSTITUTIONAL QUANT WORKSTATION REDESIGN (presentation-only, engine/calcs untouched): full UX/IA/visual-hierarchy rebuild of the Qt Strategy Lab, all pre-existing contracts preserved. `app/ui/lab_theme.py` = single token source (palette + FS_* type scale 11/12/13/14/16/18/19/24/26, SP_* + H_TOPBAR/H_CONTEXT/H_TABS/H_STRIP, QSS fragments STICKY_BAR/SECTION/TOP_RUN/STOP/TAB, helpers `label/section_title/metric/body/semantic`). New **Strategy Command Center** top bar (brand + breadcrumb + Save/Compile + outlined sticky run mirror `TOP_RUN_QSS`, never a second dominant button) + **ExperimentContextBar** (`OBR | BUY/LONG | 527 STOCKS | 15m | 02 Jan→12 Sep | ₹10L | COMPLETE`, `set_compact` drops dates/capital). `BacktestRunPanel` = BACKTEST CONFIGURATION: `UNIVERSE|TIMEFRAME|DATE RANGE|CAPITAL` grid + `Advanced ▾` + ONE primary `▶ RUN BACKTEST` (`set_view_mode` retitles to RUN BUY/LONG / SELL/SHORT / ALL; `set_busy`→`⟳ RUNNING…`; `arm_stop`→`■ STOP`). **Responsive grid reflow**: `grid_host` horizontal policy `Ignored` + `resizeEvent` re-lays 4↔2 columns via `_natural_width(cols)` — right panel min width 808→318 (real fonts) / 1090→566 (empty font DB), so the library column keeps its 280px and `test_three_columns_present` passes in every collection order. `MetricsTiles` = BACKTEST RESULT (verdict pill from `interpret_result()` → PROFITABLE/UNPROFITABLE/MIXED/INSUFFICIENT DATA + note, hero Net P&L `FS_HERO`, tier-2 WIN RATE/PF/EXPECTANCY/SHARPE, tier-3 MAX DD/TRADES/AVG TRADE, `_vals` 8 keys unchanged). `watchlist_multiselect` summary mode (`_CHIP_LIMIT=12`: ≤12 chips, >12 single "527 stocks selected" line) + MANAGE SYMBOLS popup (live count, search, Select All/Clear All, scroll list). `stock_ranking` = discovery engine + drill-down (sticky header, `cellClicked`+`currentCellChanged` arrow keys, 26px rows, primary cols SYMBOL/NET P&L/RETURN, `_rank_tint` restrained signal, `_SelectedStockPanel` with PERFORMANCE/TRADES/EQUITY/DRAWDOWN tabs + own scroll region, `selected_symbol`/`select_symbol`, public `rows()` accessor). **COMPARE = genuine multi-stock workspace**: new `_StockComparisonBoard` (6-column cap, `_COMPARE_ROWS` NET P&L/RETURN/WIN RATE/PF/MAX DD/SHARPE/TRADES, exactly one coloured winner cell per row, `◆ LEADER` only on an unambiguous ≥2-row win, empty states SELECT STOCKS TO COMPARE / NO COMPARABLE RESULTS) fed from `StockRankRow` (single `derive_symbol_results` pass — never a second scan). `06_backtest/backtest/ui/analytics_views.py`: `_HoverChart` base → EquityCurveView (labelled axes, dashed START CAPITAL baseline, START/END markers, crosshair readout) + DrawdownView (`_drawdown_profile` → max DD / longest underwater run / recovered, all read from stored `drawdown_pct`). Verification: repo-external harness (`%TEMP%/vayren_verify/harness.py`) renders 5 resolutions × all states (READY/RUNNING/aggregating/error/COMPLETE/SELL/COMPARE/drill-down/advanced, universe 1/10/100/527) → `HARNESS CLEAN`. Tests: lab 39, stock_ranking 18, watchlist 8, trade_row_click 5, 06_backtest 54; **full suite 1640 passed / 1 skipped / 0 failed — NO ignore list needed**. The long-standing `05_strategy/strategy/tests/test_obr_pine_parity.py` collection `NameError` is FIXED: line 48 called `strategy_dir()` but never imported it (a leftover from removing the old `D:\VAYREN_STRATEGIES` literal, per `storage.py::_library_root`'s docstring) — added `from strategy.language.storage import strategy_dir`; the module now skips cleanly when the OBR record is absent (`~/.vayren/strategies/OBR.py`). NOTE: the autonomous migration agent re-wires `risk.engine.evaluate` periodically, and a full run overlapping a wire shows ~6 transient failures in `scripts/migration/agent/tests/` (analyzer/oracle/spec assert the UNPATCHED engine) — re-run before believing them; 23 s later the same dir reported 19 passed. The concurrent workstream's two `brokers_workspace.py` architecture gates (:137 `if broker_id == "zerodha"`, :971 `{"id": "zerodha", "name": "Zerodha"}`) are **FIXED**: the venue label is now data-driven (`VENUE_SUBTITLE` in the zerodha adapter → `BrokerSpec.extra` → snapshot `venue_subtitle` → UI), and the add-dialog choices come from the snapshot, so the widget stays registry-free with no literal and `test_brokers_workspace`'s "Kite Connect" assertion was NOT weakened. ruff/format/pyright 0 on every touched file (pyright needs `--pythonpath <system py311>` or PySide6 reports as missing); structure + imports + language-ownership (583 files) + migration (12 units) validators **all PASSED**. Also fixed the migration agent's recurring repo inconsistency: its `restore()` orphaned the retention entry it had added, because `RETENTION_PATH` was missing from `wire_files` in `scripts/migration/agent/pipeline.py` — added it (verified `snapshot`/`restore` round-trip the JSON byte-identically), and removed the current orphan `07_risk/risk/native_checks.py` entry. `risk.engine.evaluate` remains BLOCKED (python authoritative) — the migration itself is unfinished, only repo consistency is restored. INCIDENT (not ours): concurrent external process mangled `00_app/app/ui/portfolio_workspace.py` (2 collapsed-newline syntax errors at lines 80/360, mtime 09:39) — syntax restored — and gutted `.git/` (missing `refs/`, pack `.idx` present but `.pack` gone) — **REPAIRED** by recovering 31 files from the Windows Recycle Bin (parsed `$I` metadata with stdlib): pack `pack-b42306449ec47302e75c50b0ba6bdeaace6cd162.pack` + `.rev`, 28 loose objects, real `refs/heads/main` + `refs/remotes/origin/HEAD`, then `git update-ref refs/tags/v1.14.0 7bc01969979d9a89b9d958059750ac3c8b71866a`. Result: HEAD = main = `74b2d43`, 16 tags, `fsck` clean. Local only, no commit/push. Prior (2026-09-12) - AUTONOMOUS EVIDENCE-DRIVEN MIGRATION SYSTEM (new `scripts/migration/` + `scripts/validate_migration.py`, 12 behavioral units, 34 tests): retention-entry declaration ab migration proof nahi — 17-state machine (DISCOVERED→…→MIGRATED + REGRESSION), semantic dependency graph, AST scanner (560 files, 210 Rust-owned, 19 bridges, 0 orphans, 0 unused Rust), differential parity engine (order 206/206, metrics/aggregate/mode golden+fuzz PASS, explicit float tolerances), versioned golden sets, seeded fuzz, shadow execution (60/unit, redacted telemetry), explicit promote gate, rollback (drill-proven: REGRESSION→read-only gate FAIL→fresh-evidence recovery→re-promote), quarantine/finalize guards, SQLite schema discovery/migration plans, planner (READY/BLOCKED + exact unblocking action), report (`90_brain/migration/report.json/.md`). Live state: 6 RUST_CANONICAL (order_lifecycle, drawdown, equity_curve, sharpe, aggregate, mode — parity+shadow fresh, prod on Rust path) + 6 BLOCKED with reasons (risk.engine.evaluate → unblocks execution.session.lifecycle; viewport/storage/download/event_bus need targets). `make check` + CI me `validate-migration` (verify: evidence refresh + gate). Gate: migration PASS, language/structure/imports PASS, migration tests 34/34, full suite 28/29 (05_strategy/test_obr_pine_parity collection NameError `strategy_dir` — PRE-EXISTING concurrent-workstream half-edit, untouched; same 3 pre-existing ruff + 3 pyright errors in 00_app/05_strategy files). Local only, no commit/push. Prior (2026-09-11): v1.14.0 REPLACED (local): purana tag (f73c6f3) delete → fixes commit `68d7e98` ("release: v1.14.0": data-dir default + strategy-dir default + storage legacy-layout + test_storage_layout) → naya tag v1.14.0 → `68d7e98`. `release.py` same-version re-release support nahi karta (version-bump no-op → "pattern not found"), isliye release.py wali mechanics (add -A → secret scan clean → commit → tag) manual script se. --no-push (local only; origin/GitHub par abhi bhi purana v1.14.0 hai — push ke liye `gh auth login` fix + explicit push command chahiye). Gate note: `ruff check .` mein 3 PRE-EXISTING errors (live_trading_service/paper_service I001, test_obr_pine_parity F821) — release se unrelated. Prior: LANGUAGE ENFORCEMENT HARD GATE (migration root-cause fix): validator ab baseline-allowlist nahi - per-file tracked enforcement. New: 90_brain/ownership_policy.json v3 (16 rules, first-match order, AI/provider/native-bridge/Python-execution-adjacent explicit; UI dirs incl data/ui+backtest/ui; exec/broker + live_trading_service ab classified, koi silent skip nahi). validate_language_ownership.py rewrite: classes[] docs-only, baseline never read, har Rust-owned Python file ko files[] entry (state+reason+migration_target+migration_condition) chahiye else HARD FAIL; AST no-reintroduce + Rust workspace checks retained; ruff+pyright 0. language_retention.json v2: 210 per-file entries (187 glue + 23 UI/broker incl concurrent brokers_workspace.py). Constitution section 4 contradiction fixed (gradual ENFORCED migration, blanket exemption prohibited). AGENTS.md me enforcement section. Tests: 12/12 intentional violations (new-Python-in-Rust FAIL, valid-Rust PASS, strategy.rs-in-Rust FAIL, dropped-retention FAIL, invalid/incomplete retention FAIL, migrated-but-exists FAIL, migration-complete PASS, MIGRATION_REQUIRED warning, gate-wiring OK). Validator: 520 files PASSED. Baseline/inventory ab historical only (koi code read nahi karta). Local only, no commit/push. Prior (2026-09-10) - 2026-09-10 — PERFORMANCE SCREEN HIERARCHY REDESIGN (presentation-only, calc untouched): BACKTEST PERFORMANCE title + scope context; ranking primary (direction chip, Rank-by dropdown, search, 9 cols incl Return %, PF semantics, selected-stock details + captioned sparkline); duplicate mini-equity removed; explicit equity captions; 527 acceptance PASS; +7 tests; local only, no push. Prior (2026-09-10) — TOOL RAIL PREMIUM SVG ICONS (user complaint: tool rail icons sahi visible nahi): **ROOT CAUSE (Qt gotcha — document!)**: `QSvgRenderer.render(painter, targetRect)` DPR-pixmap par targetRect ko **logical coordinates** mein leta hai — purane code ne `QPixmap(32/64)@DPR 2/4` par target = device size diya → icon 2-4× oversize render → sirf top-left quarter visible (download icon POORA invisible tha, watchlist sirf box+stub dikhta tha — user complaint isi se thi). FIX (`tools_toolbar.py`): `_svg_icon_pixmap` ab `QRectF(0,0,ICON_SIZE,ICON_SIZE)` logical target render karta hai (64px@DPR4 supersample = crisp 16px); `_pixmap` fallback ab `fill(transparent)` karta hai (pehle uninitialized opaque garbage bg); dono icons lucide-style premium redesign (24 viewBox, stroke 2, round caps): watchlist = layout-list (2 rows rounded-square+line), download = tray-arrow; fallback painters match. Verify `Temp\opencode\verify_icons.py` (ink 0.12-0.24 @16px logical, 4/4 OK); 47 chart tests green, ruff/pyright 0. DEBUG NOTE: isolated `<path>`/`<line>` "not rendering" conclusion GALAT tha — sab primitives render hote hain jab target rect logical ho; bisect karte waqt DPR coordinate bug hi culprit tha. Prior (2026-09-10) — SLINT UI SLICE-1 (vayren-shell egui→Slint, user directive ne UI tech egui→Slint change kiya): native UI ab **Rust + Slint** — `ui/app.slint` application shell (7-workspace nav rail, Alt+1..7, status bar, VAYREN dark palette `ui/palette.slint`), BrokerPanel migrated surface (broker identity/env badge/health/LIVE/capabilities/blockers — same honest view-model semantics, backend facts only), non-migrated screens honest PENDING-MIGRATION card; naya `src/shell.rs` (screen state + projection, headless-tested via `i-slint-backend-testing` `init_no_event_loop` — UI tests ek sequential test mein, platform global hai); eframe/egui repo se GONE (Cargo.lock 0 egui crates); `build_rust.py` ab workspace-wide (cdylib + shell bin build + `cargo test --workspace`) — shell ab gate mein; constitution/AGENTS/architecture/retention/audit/validator egui→Slint strings + inventory regen (rust-slint target); shell tests 6 green, build_rust EXIT=0, fmt/ruff/pyright/structure/imports clean, release bin 10.6MB desktop smoke OK; pytest 1592 passed/6 failed — failures SIRF concurrent live-trading stream ke touched files mein (test_lab_workspace, test_stock_ranking), is slice se zero coupling; language validator ke 3 failures bhi unke naye live_*.py files (DATA_PROCESSING) — untouched by design. Forensics tool BROKEN mila (2 crashes: stale-task partition mismatch + total=0 ZeroDivisionError auto-report path) — stale task ka events.jsonl quarantine kiya (`.corrupt-quarantine` rename), tool fix alag task hai. Local only, no commit/push. Prior (2026-09-10) — BUG-HUNT FIXES (9 bugs, sab local, tests green): (1) **PaperBroker/SandboxBroker SELL cash sign** — `settle`/`_fill` ab side-aware: BUY debit (notional+commission), SELL **credit** (notional−commission); SELL ab cash-limited bhi nahi (no margin engine — proceeds credit hote hain); pehle sells bhi capital kam karte the → capital drain/negative → sells stuck. (2) **Backtest SHORT close_end slippage ulta tha** — `runner.py` dono paths (execute_bars + legacy) ab SHORT end-exit par `close + slip` (buy-back adverse), LONG `close − slip`; pehle shorts ko favorable fill milta tha → short PnL inflated. (3) `positions.try_close(exit_signal=True)` bhi side-aware slip (SHORT `+slip`). (4) **Downloader heartbeat thread leak** — `_start_heartbeat` ab `threading.Event` return karta hai, run end par `stop.set()` se thread khatam; `EngineLock.heartbeat()` missing lock file ko **recreate nahi** karta (ghost lock fix). (5) **sweep empty-DB boundary** — `all_chunks_zero` ab empty DB (before+after None) par bhi True (note: boundary write engine mein `earliest_now is not None` guard — truly-empty symbol ke liye date nahi hoti, retry-per-pass behavior waisa hi). (6) **Obr exit params wired** — `exit_hour`/`exit_min` ab `time_exit` mein use hote hain (default 15:15 unchanged); `ref_index`/`c4_thresh` abhi bhi unused (semantics invent nahi kiye). (7) `BacktestRunner.run` per-strategy `try/except` — ek kharab strategy ab poori multi-strategy run crash nahi karti (error string errors list mein). (8) EventBus handler log `getattr(handler, "__name__", repr(handler))` — partial/callable objects par crash nahi. (9) `create_sma_crossover` "Min volume" label mapping. Verify: 239 targeted tests green, ruff+pyright 0 errors. KNOWN-LEFT (deliberate): `db_path` symbol-sanitize collision (M&M/MM → same .db; fix existing data files orphan karta — migration needed), OBR `ref_index`/`c4_thresh` dead params, market close 12:40 quirk (documented intentional). Prior (2026-09-10) — LAB WORKSTATION REDESIGN (speed-first, calc untouched): compact 2-row config (792×234 → 872×109), ONE progress indicator (topbar counts only; buttons morph RUN→■ STOP), state machine READY→RUNNING→AGGREGATING→FINALIZING→COMPLETE, real STOP for batches (coordinator.cancel, results kept), ranking viewport-following (fixed 220 → 160/221/330 @768/1080/1440p), compact metric cards, dead mini-equity index fix (itemAt(1)→real EquityCurveView); prior perf opts preserved (caps, async history, LOD, single rebuild; finalization ~8s, tabs ≤133ms); +4 tests; app suites 94 green, ruff/pyright clean; local only, no push. Prior (2026-09-10) — POST-BACKTEST DISPLAY PERF (UI finalization ~125s → ~9s, results identical). Prior (2026-09-10) — BACKTEST BATCH PERF (527 stocks 18m28s → ~1m, 14.94×, bit-identical). Prior (2026-09-09, other workstream) — (1) startup maximized (2) 1400 candles visible (3) dev-log deleted; NOTE (2026-09-10 bugfix): `scripts/patch_research.py` DELETED (was dead no-op, gate green now) — unrelated, left alone. NOTE (2026-09-10 bugfix): `test_full_event_flow_reaches_window_rendered` passes now (1547 green full run) — is fix se unrelated.

**Current release:** `v1.5.2` (pyproject `1.5.2`, tag `v1.5.2` → commit `HEAD`). Previous tag `v1.5.1`. Changes since `v1.5.1`: OBR chart fix — REF HIGH/LOW horizontal ray (generic `PlotOverlay` `extend="session"` sparse→horizontal, dense→connected), stale `BacktestCompleted` overlay gate, `ChartReady` dedent + `_on_chart_ready_lab` repopulation + `indicator_added` lifecycle, compile feedback + param dedup, `after_time`/`plot` helpers.

## 1. Ye kya hai?

Ye document AI ko batata hai ki **abhi platform kahan hai** â€” kya bana, kya baaki. Naya session shuru karte hi ye padho.

## 2. Cheezein Kahaan Hain
| scripts/ | `scripts/forensics/` — coding-time forensics (measurement only, stdlib). CLI: `python scripts/forensics/__main__.py` → mark/run/sweep/status → report/aggregate/list. **Ab koi task-start/task-end nahi — sessions REAL agent lifecycle se auto open/close hote hain** (see forensics session model row). Traces `.forensics/tasks/<id>/events.jsonl` (gitignored). Phases 14, intervals non-overlapping, accuracy MEASURED/INFERRED/UNKNOWN. `scripts/validate_imports.py` now has `--json` |
| forensics session model | **Sessions automatic: task = ek coding-agent turn** — `agent.py` opencode ke apne log (`C:\Users\visha\.local\share\opencode\log\opencode.log`, env override `VAYREN_FORENSICS_AGENT_LOG`) se turn start/end nikalta hai: `message=process session.id=<ses> messageID=<msg>` = turn start, `message="exiting loop"`/`message="stream error"` = turn end, consecutive process = same turn. Pehla invocation session kholta hai **TASK_START = turn ka real start**; naya turn ya turn exit dikhe → close REAL exit timestamp par + final sweep + **completed task ka report automatically print** (auto-report). Task id = `turn-<msg_id>` (fallback bina log: `session-<ts>`, open at first invocation, idle-close `session_idle_ms` 900s). **TOTAL = TASK_END − TASK_START, hamesha poora window** — TASK_START/TASK_END immutable raw events. Window partition: anchors = koi bhi event with phase (run_end chhodkar); gaps > idle-threshold (default 120s, report flag `--idle-threshold`) → UNKNOWN (INFERRED); sub-threshold gaps persist phase; pre-first-marker → UNKNOWN (accuracy UNKNOWN); wrapped run exact duration + tail. Open task → window closes at analysis moment (ek baar resolve). `analyze_task` assertion: covered_ms != elapsed_ms → ValueError (no double counting / no dropping). Report: `CODING TIME FORENSICS` header, `Total elapsed:`/`Measured:`/`Unknown:` col 22, phase table (30/7/5, sirf evidence wale phases + hamesha UNKNOWN + TOTAL rows), `PARTITION CHECK: measured + unknown == total (diff 0ms)`, **`TOP TIME CONSUMERS` top 3 (UNKNOWN included)**. **watch_mod = phase CODE_GENERATION** (file writes, ts = mtime window mein clamp); **`_close_session` TASK_END pehle likhta hai phir sweep** (clamp ke liye). `format_duration` hamesha `Xm Ys` (`Xh Ym Zs`). Tests: `scripts/forensics/tests/` (14) — synthetic controlled clock + agent log fixture (real log kabhi touch nahi; **dual-module gotcha: `forensics.agent` vs flat `agent` ALAG objects — fixture flat modules patch karta hai**, `recorder.now_iso` monkeypatch se clock control); `conftest.py` scripts/ + scripts/forensics/ dono sys.path par; **ROOT import-time binding gotcha fixed**: recorder/watcher `store.ROOT` call-time attribute access karte hain. pyproject testpaths includes scripts/forensics/tests. Limitation (by design): pehli invocation se pehle ki file edits UNKNOWN mein jaati hain (snapshot open par hota hai) |


| Module | Chapter | Andar kya hai |
|---|---|---|
| app | `00_app/app/` | `App` (`--data-dir`, `--limit` default **None**), `Bootstrap` (`limit=None`) â€” **Part 4: `components` + `system_model` properties, `_build_architecture()` startup par real market/chart manifests register karta hai (live instances as implementations) + SystemModel build** â€” old `services` registry (names) + nayi `components` (capability lookup) dono coexist; subscriptions sirf yahan, `AppLifecycle`, `__main__.py` |
| core | `01_core/core/` | `EventBus`, `Event`, `AppStarted`, logger, `Registry` + **Universal Foundation**: `contracts/` (ComponentId, ComponentVersion, CapabilityId, CapabilityDecl, CapabilityContract, BehavioralRules, ComponentContract, Health, ComponentManifest incl. `capabilities_consumed`, validate_manifest), `registry/` (`CapabilityRegistry` â€” capability â†’ multiple implementations, `find(prefix)`; `ComponentRegistry` â€” manifest + implementations register, discovery, `summary()`), **`system/` (Part 2 â€” System Intelligence Layer)**: `ComponentGraph` (deps/dependents incl. optional, transitive, `find_cycle()`), `CapabilityGraph` (providers/consumers, `unresolved_consumers()`), `EventGraph` (producers/consumers, unproduced/unconsumed), `DataFlowModel` (inputs/outputs, workflow data paths), `Workflow`/`WorkflowStep`/`WorkflowRegistry` (id pattern `^[a-z][a-z0-9_]*$`, step ids unique), `change_impact.py` (`analyze_change` auto-dispatch â€” capability `.` / workflow / component; risk: HIGH = indirect dependents ya workflows, MEDIUM = direct dependents/consumers, LOW = kuch nahi), `snapshot.py` (`build_snapshot` â€” components/capabilities/events/workflows/gaps JSON-ready + `render()`), `system_model.py` (`SystemModel` â€” queryable facade over registry + workflows: find_component/capabilities/find_implementations (asli objects registry se)/find_dependents/find_consumers/find_workflows/analyze_change/snapshot), **`ai/` (Part 3 â€” AI Engineering + Evolution Layer)**: `intent.py` (IntentKind/Intent/`classify`/`parse_intent`/`validate_intent`), `plan.py` (Plan/PlanChange/PlanRisk/Rollback + `validate_plan` + `risk_rank`/`plan_risk`), `plan_validator.py` (`Policy` + `PlanValidator` â€” deterministic policy enforcement: protected components, forbidden capabilities, max risk, allowed change kinds, rollback threshold), `change_simulation.py` (`simulate_plan` â€” Part 2 analyze_change se affected components/capabilities/workflows + required regression tests + estimated risk), `sandbox.py` (`Sandbox` â€” strict stage lifecycle PLANâ†’SANDBOXâ†’TESTâ†’BENCHMARKâ†’VALIDATEâ†’APPROVEâ†’DEPLOY, no skips, `deploy()` = recorded decision only, invalid plan approve nahi ho sakta), `boundary.py` (`ActionKind` â€” 9 allowed + 6 forbidden (execute_trade/bypass_risk/delete_production_data/modify_protected_system/deploy_unvalidated/override_contract), `AiBoundary` fail-closed), `providers.py` (`AiProvider` ABC + `OfflineProvider` default + `AiProviderRegistry` â€” select() None = AI optional), `memory/engineering.py` (`EngineeringMemory`/`EngineeringEntry`/`Decision` â€” searchable decisions), `memory/performance.py` (`PerformanceMemory`/`PerformanceRecord` â€” measured facts only, â‰¥1 metric required, best/average/summary), `optimization.py` (`OptimizationStudy` â€” benchmark/compare/recommend, **`adopt()` hamesha raises** = guarded self-optimization), `context.py` (`ContextBuilder` â€” deterministic context sections for providers). **Motto: "AI proposes. VAYREN validates. Deterministic runtime executes."** |
| market | `03_market/market/` | `Bar` + `SymbolQuote`, `SqliteCandleDatabase` + `OhlcvCandleDatabase`, `CandleRepository` + `SymbolRepository` (+ `get_quotes`), `MarketDataLoader` + `SymbolListLoader` + `TimeframeListLoader` + `QuoteLoader`, `timeframe/` package (ladder + aggregation), **`manifest.py` (Part 4) â€” `market_manifest()` production manifest** (storage, 4 capabilities, events, dependency core), events: `LoadSymbol`, `ListSymbols`, `DataLoaded`, `SymbolsListed`, `TimeframeChanged`, `ListTimeframes`, `TimeframesListed`, `QuotesLoaded` |
| chart | `04_chart/chart/` | `ChartModel` (+ `timeframe`, `exchange`), `CrosshairValue`, `ChartEngine`, `CandleRenderer` + `TimeAxisRenderer` + `CrosshairRenderer` + `LabelRenderer` + `OverlayRenderer`, `CandleChartWidget` (axis strip + snapping crosshair + overlays), `WatchlistWidget` (+ inner `SymbolListWidget` â€” 2-line quote rows via `_SymbolRowDelegate`), `ChartToolsToolbar` (TradingView-style left rail — SVG-based nav icons: watchlist + download-engine via `_SVG_DATA` dict, `_svg_icon_pixmap` renderer; other tools QPainter fallback), `ChartWindow`, **`manifest.py` (Part 4) â€” `chart_manifest()` production manifest** (presentation, chart.render, consumes data.query.candles, deps core+market), `ChartReady`, `WindowRendered` |
| data | `02_data/data/` | **Historical download engine (Phase 6)** â€” `settings.py` (DownloadSettings frozen: chunks 200d, max_history 10y, retries 5, 429-stop @3, market 09:15â€“12:40 IST, lock stale 120s, default interval `15m`; NSE_HOLIDAYS 2024â€“26; no credentials here â€” provider package ke paas), `calendar/throttle/lock/models/logging_setup/symbols/reporter` (reporter protocol: Null/Recording), `storage/` (CandleDB â€” ohlcv/non_trading/history_boundaries, WAL, INSERT OR IGNORE, V9/V9.1 migrations; DatabaseScanner + `scan_one()`), `provider/` (contract.py â€” Provider protocol `available/symbols/fetch_candles(symbol,interval,start,end)/new_session/renew`, CANONICAL_INTERVALS `(1m,5m,15m,30m,1h)`, 7 normalized error codes, `ProviderError(msg, code)`, sentinels TOKEN_EXPIRED/RATE_LIMITED; factory.py â€” register_provider/build_provider dispatch on `settings.provider`; `zerodha/` package â€” adapter.py (ZerodhaProvider: KITE_INTERVAL_IDS, symbols, fetch_candles wrapper, lazy FetchEngine reset on new_session/renew), auth.py (token.json + Selenium TOTP login, `available()`, lazy SDK imports), fetch.py (FetchEngine 5 retries), instruments.py (resolver, cached), credentials.py (ZerodhaCredentials from_env + layered load_zerodha_credentials(settings, store) per-field: stored -> env -> none; Phase 6N credentials manager: credentials.py (CredentialField + ProviderConfigError), credentials_store.py (CredentialStore protocol; FileCredentialStore data_dir/credentials/vayren.<provider>.json; WindowsCredentialStore ctypes advapi32 CredWriteW/CredReadW/CredDeleteW, TargetName=vayren:zerodha, JSON blob, CRED_PERSIST_LOCAL_MACHINE; default_store win32->Windows else file, no keyring dep), manager.py (ProviderCredentialsManager(settings, provider, store=None) validate label-only/save/apply/test_connection/clear/reload/has_stored; provider duck-type display_name/credential_fields/build_credentials/reload_credentials; adapter 5 fields api_key+api_secret required, user_id/password/totp_secret optional; auth messages -> panel + env fallback mention) priority in-app -> env -> Not Configured; env instructions sirf Advanced -> Environment Variable Fallback collapsed; ui/credentials_dialog.py (ProviderCredentialsDialog + _SecretEdit masked/eye/re-mask, Test Connection generic success/failure texts, Save & Connect -> manager.save, Clear with confirm), status_view set_credentials_manager + post-dialog reload) â€” engine/UI se Zerodha internals ab GONE), `downloader/` (DownloadQueue PARTIAL-priority, forward_sweep 200d chunks + jitter, `HistoricalDownloadEngine.run_download/run_batch/scan_symbol/reset/set_reporter` — **provider required ctor param (Phase 6L), bootstrap `build_provider` se deta hai; canonical intervals only, `provider.symbols()` universe filter, `new_session()` per job, fetch_chunk = partial(provider.fetch_candles, symbol, interval)**), `events/` (8: DownloadRequest, CoverageRequest, CancelDownload, DownloadStarted, DownloadProgress, DownloadCompleted, DownloadFailed, DownloadCoverage), `worker.py` (DownloadWorker QThread â€” engine sirf yahan, op queue, signalsâ†’bus bridge), `ui/` (download_panel (sections, chips, quick ranges, plan, date defaults 2017->today, display dd MMM yyyy - 6I/6I-bis), status_view (state cards, provider card), log_view (LogPanel), historical_panel (HistoricalDownloadPanel side panel - Phase 6F; single main scroll - 6J), stock_checklist (Phase 6G)), **`manifest.py` â€” `data_manifest()`** (historical_data, capabilities download/coverage/status, deps sirf core) |
| risk | `07_risk/risk/` | `RiskPolicy`/`RiskRequest`/`RiskDecision` (named checks), `RiskEngine` (fail-closed, duplicate memory), `KillSwitch` (global/strategy/broker, JSON persisted), `SessionRules`/`clock_sane`, `risk_manifest()` |
| execution | `08_execution/execution/` | Live/paper sessions: `market_data/` (provider Protocol, ReplayProvider, normalizer), `runtime/` (contract inspector, lifecycle, driver, LiveSession), `planner.py`, `engine.py` (order state machine), `broker/` (adapter Protocol, PaperBroker, mode-gated factory), `portfolio/` (ledger, reconcile), `journal.py`, `replay.py`, `regime.py`, `adaptive/` (attention/memory/surprise/confidence, advisory only), `ml_interfaces.py` (Protocols + disabled defaults), `modes.py` (PAPER default, 5 live gates). LIVE_BROKER_INTEGRATION = NOT_CONFIGURED |
| brain | `90_brain/` | Permanent knowledge â€” code chhune se pehle padho |

## 3. Verified Facts (is session mein pakke kiye)

| Fact | Detail |
|---|---|
| Bus synchronous hai | Poora chain ek publish mein khatam |
| Default data dir restored (2026-09-11) | `00_app/app/__init__.py` DEFAULT phir pre-v1.11 jaisa: env `VAYREN_DATA_DIR` → legacy `D:\ZerodhaTradingData` (exists to wahi, 527 DBs verified, 360ONE 42,683 candles) → fallback `~/.vayren/data`. `test_main.py` 5/5 PASS, ruff clean |
| Strategy library restored (2026-09-11) | Same pattern: `--strategy-dir` default env → legacy `D:\VAYREN_STRATEGIES` (OBR.py verified) → `~/.vayren/strategies`. ROOT CAUSE 2: `storage.strategy_dir(data_dir)` hamesha `<dir>/strategies` colocate karta tha → legacy top-level `OBR.py` invisible + khaali `strategies/` subdir banata tha. Fix: dir mein khud `*.py` hon to use-as-is, else colocate. Naya `test_storage_layout.py` (2 tests). 50 tests PASS (layout+main+lab+paper), ruff clean |
| Subscriptions ek jagah | Sirf `00_app/app/bootstrap/bootstrap.py` |
| Asli data | `D:\ZerodhaTradingData` â€” 527 DBs, har stock ka apna file |
| Asli schema | Table `ohlcv` (`candle_time` TEXT PK, open/high/low/close REAL, volume INTEGER) â€” koi `symbol` column nahi |
| Asli granularity | AMBUJACEM.db = 61,601 rows @ **15m** (09:15..15:15, 25 bars/day, deltas 900s) |
| Timeframe detection | `CandleRepository.detect_timeframes` â€” base = mode of row deltas; `available_timeframes` = ladder entries jo base ke multiple hain. AMBUJACEM â†’ `('15m','30m','45m','1h','2h','4h','1D','1W')` (1m/3m/5m excluded). **Kabhi cached nahi** |
| Aggregation | Session-anchored intraday (session start = mode of **first-bar-per-day** â€” partial windows se robust), daily = midnight, weekly = Monday. OHLCV raw rows se exact; aggregated bar ke bar_size pe `"30m"` jaisa label |
| Query pattern | Sirf timeframe change par 2 queries (sample 5000 detect + `(limit+1)Ã—ratio` window fetch); `bars[-limit:]` partial oldest bucket drop karta hai. **`limit=None`** (default ab) = full sample + full fetch, koi drop nahi |
| Load-all semantics | `LoadSymbol.limit` / `TimeframeChanged.limit` ab `int \| None = None` â€” default = **poori SQLite history** (TCS 61,676 rows, 2016-06-09 â†’ 2026-06-10). Explicit `--limit N` hi cap karta hai. DB layers (`fetch_candles`) `limit is None` pe ascending full scan â€” `LIMIT ?` NULL binding **kabhi nahi** (SQLite `datatype mismatch`) |
| Initial viewport | Fresh lifecycle (first open / naya symbol / timeframe change) → `_initial_count(total)` = `max(MIN_VISIBLE_BARS, min(INITIAL_BARS=1400, total, max_visible_bars()))` — **latest readable bars** (1400 ya viewport limit jo chhota ho) right margin pe, poori history loaded rehti hai. Density (2026-09-09): `MIN_CANDLE_SLOT=1.0px`, `max_visible_bars()=min(chartWidth/1, MAX_VISIBLE_CANDLES=1800)` — zoom-out/same-series/trade-focus/resize sab isi se clamp; pan count-preserving; Y visible-only; volume same window; PlotStore viewport query. `_fit_all_count` **hata diya** (Phase 5F fix). Same symbol+timeframe reload → incremental follow-latest re-anchor / manual-pan shift (waisa hi). `reset_view` bhi density-capped latest. `_log_data_range` har load pe first/last ts log karta hai |
| TimeframeChanged flow | UI â†’ `TimeframeChanged` â†’ `MarketDataLoader.on_timeframe_changed` â†’ aggregation â†’ `DataLoaded` â†’ chart update (chart bina touch) |
| Timeframe persistence (Phase 5J) | Selected timeframe **chart/session ka state hai, stock ka nahi** â€” symbol switch par kabhi reset nahi hota. `ChartWindow._current_timeframe` single source of truth: `on_chart_ready` â†’ `model.timeframe`, `_on_timeframe_selected` â†’ explicit selection, initial `None`. Symbol click: `_current_timeframe` hai â†’ `TimeframeChanged(symbol, timeframe, limit)` (existing aggregation path); nahi hai (pehli load) â†’ `LoadSymbol` (base bars). Har case mein `ListTimeframes` bhi. `ChartEngine`/loader/repository untouched â€” at-or-below-base timeframe repository ka existing fallback (plain fetch) handle karta hai |
| ChartModel | `timeframe`/`exchange` fields (Phase 5A) â€” engine infer/default karta hai; market ka aggregation bar_size isse override nahi karta |
| Axis layout | `CandleChartWidget._chart_rects()` â†’ `(chart, volume, axis)`; `TIME_AXIS_HEIGHT = 24` |
| Window layout | `ChartWindow` â†’ QSplitter 4 items: **tools rail (0) | watchlist (1) | collapse strip (2) | container (3)**; container QVBoxLayout: TimeframeToolbar strip (stretch 0) + chart canvas (stretch 1). Splitter handles = clean separators; `setSizes([40, 220, 28, 1004])`. **Final horizontal order: TOOLBAR â†’ WATCHLIST â†’ CHART** â€” tools rail extreme left (fixed 40px, splitter item 0), watchlist uske turant right, chart container baaki saari jagah (pan/zoom/scroll se rail kabhi move nahi hota). Chart header (`_paint_header` band: `SYMBOL Â· tf Â· exchange` + OHLC) + timeframe strip dono chart area ke andar. `ChartWindow(widget, watchlist, toolbar, tools, bus, limit=None)`. **`OptionsPanel` removed (empty dark strip) â€” chart freed space mein expand hota hai** (1280px: tools 40 | watchlist 220 | chart 1018). **Watchlist collapse (Phase 6B — sirf watchlist, tool rail hamesha visible)**: `_watchlist_visible` dedicated state + `toggle_watchlist()`/`watchlist_visible`; watchlist header `Â«` button (existing icon) + collapse strip (`WATCHLIST_COLLAPSE_WIDTH=28`, `Â»` button, splitter item 2 = watchlist ki jagah, sirf hidden mein) dono toggle; collapse = sirf watchlist hidden (selected symbol/scroll/quotes intact), chart 220px+ expand; restore = original sizes. Splitter count 4 hamesha, koi animation nahi, ctor unchanged, `setCentralWidget(splitter)` (wrapper nahi). **TradingView-style nav (Phase 6D)**: collapse strip + header `Â«` + `ChevronToggleButton`/`chevron_toggle.py` SAB hata diye — ab **Tool Rail = permanent navigation**: `_active_panel: str | None` SINGLE state (`None | "watchlist" | ...`, koi multiple booleans nahi), `toggle_panel(panel_id)` (`active == clicked ? null : clicked`), `active_panel` property, `_apply_panel_state()` (panel visible iff active, rail button checked sync, sizes open `[40,220,rest]` / closed `[40,0,rest]`); splitter ab 3 items `[tools | watchlist | container]`; rail ka FIRST icon = watchlist nav button (kind `watchlist`, 30x30, checkable, mode group ke bahar, `watchlist_clicked` signal, tooltip `Watchlist (All Stocks)`, vector list glyph `_draw_watchlist`, checked = teal pill + ACCENT_TEXT icon; cursor checked default unaffected); doosre icons abhi untouched (future panels ke liye `toggle_panel` ready); closed state chart = w − 40 − 1 (3 items, hidden middle → sirf 1 handle, empirical); watchlist header ab sirf watchlist/+/â†©/â‹¯ + stretch |
| Side panels (Phase 6F) | `HistoricalDownloadPanel` (data/ui/historical_panel.py — ex data_window.py): **embedded side panel** in ChartWindow splitter item 2 `[tools | watchlist | download | container]`, ctor `download: QWidget | None` (chart data import nahi karta — bootstrap inject karta hai); header `Historical Download` + `×` close button (`close_requested` signal — bootstrap `toggle_panel("download")` se jorta hai); `_apply_panel_state` download branch: sizes `[40, 0, 220, rest]` open / `[40, 0, 0, rest]` closed; panel EXACTLY 220 (minHint 211). **Splitter handles (empirical, 4 items)**: 1 hidden + 1 visible panel → chart = w − 40 − 220 − 2 (2 handles); dono hidden → w − 40 − 1 (1 handle). `start()` mein `data_window.show()` REMOVED — koi separate "VAYREN — Historical Data" window nahi. `DownloadPanel` compact: From/To vertically stacked (side-by-side 2×114px fit nahi hota tha), `_COMPACT_STYLE` buttons 11px pad 2/5 — minHint 211 ≤ 220; status value labels wordWrap. **Multi-stock checklist (Phase 6G)**: single SYMBOL input hataya — `StockChecklistWidget` (data/ui/stock_checklist.py): search QLineEdit ("Search stocks...", clear btn, case-insensitive substring via `item.setHidden`) + bar `[Select All][Clear All]...stretch...Selected: X` + QListWidget (NoSelection, mouse tracking, per-pixel scroll, **min 120, UNCAPPED + stretch-absorbing (Phase 6H)** — `_LIST_MAX_HEIGHT`/`setMaximumHeight` hataya, `addWidget(list, 1)`; `_ChecklistList` sizeHint rows×26 cap 16 — form pref 487→419; download_panel `addLayout(stocks_box, 1)`, historical_panel `addWidget(panel, 3)` — empirical: stretch 1 clamped @minHint 415, 3 = list 278 @1080 / 428 @1280, status stretch 0, log 1; @760 list floor 120 (5r), 10–15 rows sirf tall windows par), slim scrollbar); `_ChecklistDelegate` 14×14 rounded checkbox (selected = Highlight fill + HighlightedText check, hover = AlternateBase, row 26px) — **`option.palette` kabhi mat lo, `self._owner.palette()`** (QSS list ka palette app-default rebuild hota hai). API: `set_symbols`/`symbols`/`is_selected`/`selected_symbols` (universe order)/`selected_count`/`select_all` (hidden bhi)/`clear_all`/`selection_changed`. Download/Coverage = **first selected stock**, empty → no-op; `set_busy` checklist disable. Wiring: bootstrap `SymbolsListed` → `historical_panel.on_symbols_listed`. **Panel-width war sikhaya (empirical)**: offscreen font DB EMPTY (har family wide fallback = 2× widths) → probes/app-tests `QFontDatabase.addApplicationFont("C:/Windows/Fonts/segoeui.ttf")` load karte hain (QApplication ke BAAD, pehle nahi — access violation); QSS widget ka layout QStyleSheetLayout = box-model min, size overrides ignore karta hai — `Minimum`-policy items ko box min; **`Preferred` policy (ShrinkFlag) se box min bypass**; `_SlimLabel`/`_SlimButton` (minSizeHint width 0) + `_WrapLabel` status values → form minHint 204 ≤ 220,  **Phase 6I (console redesign)**: header subtitle 'Market Data Acquisition'; SectionLabel sections; checklist chips (limit 6) + set_selected; quick ranges 1M/3M/6M/1Y/MAX; Download Plan live estimates (trading days via NSE_HOLIDAYS, rows = days x bars/day x stocks); status cards (run/complete/failed/coverage) + perf timer + provider card (raw env names sirf Advanced detail); LogPanel collapsible; interval label 60minute -> 1h. **6I-bis (date range)**: default FROM 2017-01-01, TO = currentDate (dynamic), display 'dd MMM yyyy' (01 Jan 2017 / 17 Aug 2026), internal/emitted ISO yyyy-MM-dd WAHI; _DATE_EDIT_STYLE box-model min-width 0 + ::drop-down subcontrol (text kabhi icon ke peeche nahi); FROM/TO full-width equal (Expanding policy + wrapper margins 0); plan Date Range wordWrap (hamesha poori date, kabhi truncate nahi). **6J (single scroll)**: POORA panel content (header + DownloadPanel + StatusView + LogPanel) EK QScrollArea mein (widgetResizable, NoFrame, h-scrollbar AlwaysOff) = panel ka SINGLE main scrollbar; DownloadPanel ka inner QScrollArea HATA (ab ek flow column, actions flow mein); stocks list apna internal scrollbar rakhta hai; short viewport -> main scrollbar, sab reachable (Provider Status/Advanced/Engine Log/Footer); tall -> content stretches, list grows
| Chart tools rail | `ChartToolsToolbar` (04_chart/widgets/tools_toolbar.py) — TradingView-style vertical rail, `TOOLBAR_WIDTH=40`, `BUTTON_SIZE=30`. **EXACTLY 2 icons rendered (Phase 6K — minimal sidebar)**: FIRST = watchlist nav button (kind `watchlist`, checkable, mode group ke bahar, `watchlist_clicked` signal, list glyph, **default checked**) + SECOND = download nav button (kind `download`, checkable, mode group ke bahar, `download_clicked` signal, download glyph, watchlist ke seedha neeche y=40); phir `addStretch(1)` = empty rail space. Cursor mode button (Phase 6E ka teesra icon) AUR internal 1px separator `__init__` se hataye (6K) — koi extra item nahi, koi "More"/Settings/Help nahi. Baaki tool-group definitions (`_MODE_TOOLS`…`_SETTINGS_TOOLS`), `_draw_*` drawer functions, `_icon`/`_pixmap`/`_ICON_CACHE`, `_mode_button`/`_tool_button`/`_separator`, `_mode_group` SAB retained (architecture delete nahi hua — aage phases render kar lenge). QIcon states: Normal/Off=`TEXT`, Normal/On=`ACCENT_TEXT` (dark on teal pill), Disabled/Off=`PLACEHOLDER`. QSS widget-level palette-roles only — `:hover` midlight, `:pressed` mid, `:checked` highlight pill. **Qt gotchas (empirical)**: (1) QVBoxLayout WITHOUT trailing stretch extra vertical space items ke beech spread karta hai (PySide6 6.11.1 is machine) — `addStretch(1)` last item zaroori; (2) Windows 150% DPI top-level `show()` width quirk (117) content-independent — top-level width asserts hidden-resize se karo, in-window (child) tests hi ground truth hain; (3) splitter setSizes symmetric 220/220 — side-panel switch par chart width SAME rahti hai (chart sirf dono panels closed par badhti hai) — chart-width test asserts `>=` (minHint squeeze order-dependent). **Phase 5P: splitter item 0 — extreme left, watchlist se pehle** |
| UI theme (Phase 5K) | `chart/theme.py` ka `APP_STYLE` â€” single palette-only QSS, `ChartWindow.__init__` mein `setStyleSheet(APP_STYLE)`. 4px radius, buttons transparent + hover `palette(midlight)`, checked/pressed `palette(mid)`, **active/checked = `palette(highlight)` pill** (timeframe buttons, selected menu item), framed rounded QMenu, hairline `QSplitter::handle` (bg `palette(mid)`). `ChartWindow` splitter `setHandleWidth(1)`. Watchlist row styling **`SymbolListWidget` ke apne QSS mein rehta hai** (widget-level sheet ancestor ko override karta hai): separators `palette(midlight)`, hover `palette(alternate-base)`, selection rounded highlight, slim 8px scrollbar. Chart header: backdrop band `OverlayRenderer.HEADER_BAND` (16,20,24,110) widget mein cached brush se; OHLC change segment `CandleRenderer.BULL/BEAR` color (`LabelRenderer.paint_left` ka optional `text_color`); "Symbol" label muted `palette(placeholder-text)`. Koi hardcoded QSS color nahi â€” sirf palette roles |
| Institutional UI (Phase 5M) | **Fixed dark terminal palette** â€” `chart/theme.py` `APP_PALETTE` (cool near-black `#101418` family, teal `#26a69a` accent, muted `#8a93a6`/`#5d6778`). **CRITICAL Qt behavior**: QSS `palette()` roles **application palette se resolve hote hain** â€” `ChartWindow.__init__` `QApplication.setPalette(APP_PALETTE)` karta hai (window par bhi). Plain-QWidget QSS backgrounds/borders ke liye **`WA_StyledBackground` zaroori** (TimeframeToolbar â€” iske bina toolbar border kabhi render nahi hota tha). Header strip: solid `STRIP_BG #141922` + hairline, **two-tone unframed text** (`paint_symbol_info`: symbol bright bold + meta muted; `paint_ohlc`: O/H/L/C letters muted, values light, bull/bear change). Watchlist: dense rows (3px/8px, radius 2), selection = teal fill + bold + dark text, 6px scrollbar, uppercase `SYMBOL` table header, 24px icon buttons. Race fix: `on_timeframes_listed` `_current_timeframe` re-apply karta hai (TimeframesListed ChartReady ke baad aata hai â†’ active button ab hamesha sahi). Renderer contracts unchanged (call counts + args pinned). Pixel verification: `C:\Users\visha\AppData\Local\Temp\opencode\ui_pixels.py` (15 checks) |
| Watchlist quotes (Phase 5N) | **Real quote snapshot**: `SymbolRepository.get_quotes(symbols)` â€” har symbol ke DB ka **sirf latest candle** (`fetch_candles(symbol, 1)` = `ORDER BY candle_time DESC LIMIT 1`), `SymbolQuote(symbol, price=close, change_pct=return_pct, timestamp)`. Change % = chart header wali `(closeâˆ’open)/open` semantics. Missing file â†’ skip. **Measure**: ~1 ms/symbol; 527 stocks â‰ˆ **0.4â€“0.55 s ek baar at startup**, phir cached â€” **kabhi re-query nahi** (kabhi per-repaint/per-mousemove DB query nahi). Flow: `SymbolsListed â†’ [window.on_symbols_listed, quote_loader.on_symbols_listed]` (subscription order = watchlist pehle) â†’ `QuoteLoader.get_quotes â†’ QuotesLoaded â†’ window.on_quotes_loaded â†’ watchlist.set_quotes`. `QuoteLoader._last_symbols` guard: identical universe = no-op (initial `None` â€” pehla listing hamesha publish). Row rendering = `_SymbolRowDelegate` (QStyledItemDelegate, quote item ke `UserRole` mein): line 1 symbol DemiBold left + price `,.2f` right, line 2 change % 11px bull `#26a69a`/bear `#ef5350` right; bina quote = symbol-only centered; **selected = midlight bg + 2px teal accent edge + `Text` color** (HighlightedText `#0b0f13` midlight par invisible tha â€” kabhi use nahi), hover alternate-base, hairline sab rows. `set_quotes(527)` = 0.72 ms; sort rebuild+reattach 4.84 ms; quotes survive sort/watchlist switch (`_refresh_list` re-attach). Company name/icons/markers **kahin exist nahi karte** â€” hamesha omit (kabhi invent nahi). Real-data pixel check: `Temp\opencode\verify_watchlist.py` (8 checks, bull+bear verified) |
| Chart painting (Phase 5L) | **Static-layer pixmap cache**: `_static_pixmap` = grid + candles + axis ek widget-sized QPixmap (key: model id, first, last, price range, volume_max, size); `paintEvent` = blit + live header + crosshair overlays â†’ crosshair/hover frames kabhi bars nahi scan karte (~0.93 ms/frame vs 10 ms pehle). `_window_stats()` single-pass (low/high/volume_max) cached per (model, window, price_manual) â€” `_price_range` auto-fill bhi cache se. Cache invalidation: model/window/price/size key mein hain; `set_model`/`reset_view` explicit clear. **Grid-cache tests ke contracts preserve**: `paint_grid` sirf cache rebuild par, header (symbol/OHLC) har paint par LIVE (static mein bake nahi â€” tests isko pin karte hain). Pan/zoom frames static rebuild karte hain (~1.6 ms) |
| Timeframe aggregation (Phase 5L) | `aggregate_bars`: **`strptime` band** (locale lookup per call = 2.1s/58k rows!) â†’ `datetime.fromisoformat`; buckets single-pass running max/min/sum accumulators (pehle per-bucket generator re-scans). 1D limit=500: 1849 â†’ 542 ms; 1W full: 2316 â†’ 633 ms. Baaki cost = SQLite fetch 48k rows (~170 ms) + Python agg loop (~160 ms) ka honest floor â€” SQL-side aggregation (GROUP BY) = DB architecture change, out of scope. `infer_timeframe` ab sampled median gap (`_INFERENCE_SAMPLE=2048`) â€” 60k-bar load par 102 ms â†’ ~1 ms. `ChartEngine` `_ascending` check: sorted bars par sort skip (85 â†’ 14 ms). `WatchlistWidget.set_symbols`: identical set = no-op + batched `addItems`/updates off â†’ symbol clicks par 7 ms/527 items â†’ 0.002 ms. **Real data scale**: `D:\ZerodhaTradingData` 527 stocks, ~60k rows/stock, 15m base |
| Crosshair | `CrosshairRenderer` + snap to candle (`CrosshairValue`) + crosshair labels. **Permanent header** (Phase 5I fix): `_paint_header` har `paintEvent` mein top bar paint karta hai â€” `SYMBOL â€¢ timeframe â€¢ exchange` (`paint_symbol_info`) + **latest bar** ka `paint_ohlc` (`model.bars[-1]`, O/H/L/C + change + %, change = closeâˆ’open, pct = `bar.return_pct` â€” real data, koi fake nahi). Crosshair se **bilkul independent** â€” visibility kabhi crosshair par depend nahi karti; model change (symbol/timeframe) par automatic update (paint model se derive hota hai). Crosshair overlays ab sirf: right price label (crosshair Y) + bottom time label (crosshair X). **Bottom time label (2026-09-16 restyle)**: `LabelRenderer.paint_centered` ab flat TradingView-style coordinate tag hai — solid teal `#26a69a` fill, dark `#0b0f13` centered text, **square corners (no radius), no border**, compact fixed height (`TAG_MIN_HEIGHT=16px`, `TAG_VPAD=2`), axis strip mein vertically centered, crosshair X par centered with bounds-clamp (left/right edge par poori box visible rehti hai). Format **ISO** (`OverlayRenderer._format_timestamp`): intraday (m/h) `YYYY-MM-DD HH:MM` (e.g. `2026-05-25 12:15`), daily/weekly/monthly `YYYY-MM-DD` — purana abbreviated `Tue 04 Aug '26` / `Week 32 2026` / `Aug 2026` format hata diya. Price label (`paint`) + OHLC (`paint_left`) unchanged (framed rounded). Top bar crosshair dirty-rect se bahar |
| Free pan (Phase 5E) | `CandleChartWidget` drag ab **2D pan** karta hai â€” horizontal (time window via `_first`/`_last`) + vertical (price range, span/zoom unchanged via `_price_manual`). Pointer `grabMouse()`/`releaseMouse()` = capture. 2-finger touch ab vertical bhi pan karta hai. Zoom kabhi nahi badalta â€” sirf viewport position |
| Context menu + Alt+R (Phase 5F) | `CandleChartWidget` right-click par **single-action QMenu** (`â†© Reset chart view`, shortcut `Alt+R`) â€” ek hi `QAction` widget par `addAction` (WindowShortcut â†’ poore window mein `Alt+R` chalta hai). Dono paths â†’ `reset_view()`: viewport-only fresh-chart restore (`_initial_count` + `_anchor_first` + `_price_manual=None` + follow-latest) â€” data/symbol/timeframe kabhi nahi chhute. `contextMenuEvent` suppress karta hai default menu. Menu Qt se band hota hai (selection/Escape/click-outside) |
| Watchlist panel (Phase 5H) | `WatchlistWidget` â€” pure UI, `SymbolListWidget` ko compose karta hai. Row 1: `Watchlist â–¼` selector (InstantPopup menu = watchlist names, active checked) + `+` (naya khali watchlist `Watchlist N`, uspe switch) + `â†©` (reset chart view â†’ `reset_requested` â†’ `ChartWindow` â†’ existing `widget.reset_view()`) + `â‹¯` (menu: New watchlist / Remove watchlist; All Stocks remove disabled) + stretch. **Phase 6D: header chevron `Â«` button HATA diya — panel toggle ab tool rail ke pehle icon se hota hai** (`collapse_requested` signal bhi removed). Row 2: `Symbol` label + `Sort by â–¼` (Name Aâ†’Z / Zâ†’A â€” real sorting). Row 3: stock list. Watchlists session-local in-memory `dict[name â†’ symbols]`, seeded `"All Stocks"` = asli symbol universe (`SymbolsListed`). Signals: `symbol_selected(str)`, `reset_requested()`, `collapse_requested()`. Properties: `watchlists`, `active_watchlist`, `symbols`, `current_symbol` |
| Provider boundary (Phase 6L) | **Engine Zerodha/KiteConnect se anjaan**: `data.provider.contract` (sentinels TOKEN_EXPIRED/RATE_LIMITED + ProviderError + Provider protocol: available/instrument_map/instrument_token/fetch_session/renew) = engine ka sirf provider import. `ZerodhaProvider` (data/provider/zerodha.py) = SIRF yahan broker knowledge: wraps AuthEngine/InstrumentResolver/FetchEngine; `auth` ctor param = FakeAuth injection (tests); resolver lazy+cached; AuthError->ProviderError (string preserved); `fetch_session()` = fresh FetchEngine per call (429-counter semantics preserved). `factory.build_provider(settings)` `settings.provider` pe dispatch (dead config field ab functional), `register_provider` future providers ke liye. `data.provider.__init__` contract-only — core import broker-free: fresh interpreter mein `import data.downloader.engine` -> sys.modules mein na kiteconnect na data.provider.zerodha/factory/auth/fetch/instruments (subprocess isolation test). Engine `_instrument_map`/`_instrument_token`/`_resolver` hataye; error strings `authentication failed: {exc}` / `⚠ Authentication failed: {exc}` byte-identical; renewal exception -> `_abort` (waise hi). Kite interval ids (`minute`/`5minute`...) settings mein aur NSE calendar abhi bhi untouched (data/UI concern). Phase 6N: adapter credential surface (display_name Zerodha, credential_fields 5, build_credentials trims, reload_credentials -> new AuthEngine), manager/store/loader as per data row; ctor ab layered load_zerodha_credentials. Engine chunking/queue/sweep/retry/resume/dates/progress/SQLite 100% same — tests wahi, sab green |

## 4. Workflow â€” AI Agent Ke Liye

```
1. 90_brain/*.md padho (minimum: architecture,
   event_catalog, module_contracts, ai_memory) + ARCHITECTURE_CONSTITUTION.md
2. Module contracts ke hisaab se implement
3. make check (lint + format + typecheck + test + validators)
4. 90_brain/ai_memory.md update
```

## 5. Open Items â€” Baaki Kaam

| Item | Status |
|---|---|
| **Portfolio institutional redesign (2026-09-12)** | Done - `00_app/app/ui/portfolio_workspace.py` rewritten (same public API/state-dict, zero broker logic, zero invented values): PORTFOLIO header + Refresh w/ real "Updated HH:MM:SS" stamp + refresh_requested signal; account status panel (CONNECTED w/ masked XXXX1234 + env/mode/lifecycle vs NOT CONFIGURED guidance); 8-KPI strip w/ hierarchy (TOTAL EQUITY 26px primary; AVAILABLE/INVESTED/TOTAL P&L; TODAY/UNREALIZED/REALIZED/RETURN, semantic P&L colors, "Unavailable" when unconfigured); PERFORMANCE panel (trades/wins/losses/win-rate from real pnl, honest NO PERFORMANCE DATA empty state — equity-curve chart intentionally omitted: no time-series in state dict); PORTFOLIO STATUS gates (Trading/Market Data/Account Sync/Risk Engine via GateRow); POSITIONS 9-col table (SYMBOL/SIDE/QTY/AVG/LTP/INVESTED/VALUE/P&L/ALLOC%) + open-count + flat-vs-not-configured split states; ALLOCATION horizontal bars (exposure/equity, top-8, N/A-guarded) + POSITION DETAIL (row-click → symbol/side/qty/avg/value/P&L/alloc, no navigation); ORDERS/FILLS tabs (6-col orders incl. TIME, 5-col fills, NO ORDERS vs ORDERS UNAVAILABLE split); PORTFOLIO RISK (exposure sum, largest position, concentration, drawdown/volatility only from `risk_metrics` else N/A + RISK UNAVAILABLE empty) + health badge (NOT CONFIGURED/BLOCKED/CONCENTRATED ≥50% rule/HEALTHY). `test_portfolio_workspace.py` 4→17 tests, responsive-geometry portfolio case green. ruff/format/pyright/imports/structure clean on touched files. 00_app suite 247/248 — 1 fail `test_lab_workspace::test_compare_board_caps_columns_and_reports_scope` is PRE-EXISTING (zero portfolio refs; concurrent-workstream compare-board cap 4-vs-6, untouched). Follow-ups: position/order search+filters, P&L period selectors (need equity-history capability), multi-account selector (needs selection-surface support). Local only, no commit/push. |
| **Centralized broker layer + automatic Zerodha auth (2026-09-11)** | Done - SYSTEM → BROKERS is the single source of truth: `BrokerManager` (app service, one background worker thread, Qt signals only) owns config/auth/state; `BrokersWorkspace` pure view (cards, checks, configure/login/disconnect/remove, masked keys); `BrokerStatus` enum in UBL (`09_broker/broker/status.py`); official Kite flow automated around the user step in `02_data/.../zerodha/live_auth.py` (SDK login URL → auto browser open → localhost callback captures request_token one-shot → automatic exchange → CredentialStore session → read-only validate; expired token = LOGIN_REQUIRED, never network-error, never retried). CONNECTED registers the authenticated `zerodha-live` instance (StaticPlugin) so every LiveSession resolves the same adapter; LIVE consumes via existing blockers + no-silent-fallback + START probe (all green). Future broker = one `BrokerSpec` (see `broker/management.py` + `data/provider/factory.py:zerodha_management_spec`). Security scan: zero hard-coded credential literals, zero committed token.json/.env; secrets never in logs/UI/snapshots (masked key only). +26 tests (9 auth incl. real loopback callback, 11 manager, 6 workspace); gate green (ruff/format/imports/structure/language PASS, pyright 0). OBR untouched (canonical record read-only; repo obr.py/sma.py/lab-workspace edits are the concurrent workstream's, not this task). REMAINING EXTERNAL: user Kite app redirect URL `http://127.0.0.1:9474/vayren/callback` + api_key/api_secret saved once in BROKERS + daily one-click login (user performs Zerodha 2FA; everything else automatic). |
| **STRATEGY LAB empty-center run guard (2026-09-12)** | Done - fixed the live-screenshot bug (empty center + populated stale results): ROOT CAUSE was unguarded run entry points — topbar RUN / Ctrl+Enter / compare run buttons emitted `run_backtest` with no strategy open, and bootstrap `_current_strategy_id()` silently fell back to `defs[0]` (another strategy), so results populated under an empty center. Fix (workspace presentation only, engine/bootstrap untouched): `_has_open_strategy()` (center-stack index) guards `_emit_run`/`_on_panel_run`/`_emit_run_buy/sell/all`; blocked runs show an inline message (topbar status line / panel error) instead of emitting, `_pending_side` untouched, hint clears on strategy open. +2 regression tests; lab 60/60, 00_app 269/269, ruff/format/pyright/validators clean. No commit/push. |
| **STRATEGY LAB screenshot fixes (2026-09-12)** | Done - fixed 2 confirmed defects from the live THELEELA screenshot + investigated the 3rd: (1) header RUN button stuck RED after batch — `_set_run_busy(False)` restored text but never the stylesheet; now restores `TOP_RUN_QSS` on idle (STOP red is in-flight only). (2) double-tick "✓ ✓ COMPILED" — bootstrap passes "✓ COMPILED …" into `show_compile_result` which prepends another "✓"; normalized at the single choke point (also fixes the batch "✓ ✓ n/n symbols" line). (3) missing config grid in user's shot does NOT reproduce on current code — verified with real Segoe UI fonts + 527 symbols at 1920 (grid fully rendered: summary+Manage, timeframe, dates, capital). Likely cause: user's running instance predates these workspace changes (uncommitted local edits); asked to re-run from this workspace. (Side note: `QFontDatabase.addApplicationFont` before `QApplication()` segfaults offscreen — scripts must create app first.) +3 regression tests (idle-style restore, glyph normalization, grid populated); lab 58/58, 00_app 267/267, ruff/format/pyright/validators clean. No commit/push. |
| **STRATEGY LAB real-app visual rebuild (2026-09-12)** | Done - implemented the HTML reference direction natively in Qt (HTML untouched, no engine change): ROOT CAUSE of collapsed result was the vertical `_root` QSplitter cramming content minimums (~1000px+) into 644–824px viewports — children overflowed and painted over each other (overlapped KPIs, clipped Net P&L). Fix: `_main` splitter now holds sidebar + `_page_scroll` (QScrollArea); page flows center → stale banner → results dock → analysis, scrolling instead of squeezing. MetricsTiles never hides KPI tiers anymore (`set_compact` folds only the secondary wins/losses line); tier grids got breathing room + self-managed reflow in their own resizeEvent (workspace-level width reads raced viewport settling); ranking/journal/equity/drawdown pages got minimum heights (360/300/300/300). Stale banner restyled from full-bleed amber block to a subtle bordered box with amber left edge + quiet RUN AGAIN (~52px). Removed dock border-top (separator cleanup) and dead collapse-animation code (`_animate_max`/`_results_height`). Ownership/staleness model fully preserved (banner/compare-notice/owners/fingerprints untouched). Tests: above-fold test rewritten as scroll-reachability + no-overlap (old test encoded the rejected fit-in-viewport assumption); +4 layout regression tests (KPI no-clip at 720p, section no-overlap, banner compact, page-scrolls-at-600px). Lab 55/55, 00_app 264/264, ruff/format/pyright/validators clean; offscreen renders verified at 1920/1600/1440/1366/1280 + empty + compare (config 2-col reflow confirmed at 1280). Note: no-strategy keeps the neutral READY/-- dock (pinned by `test_results_visible_with_ready_state`; shows zero stale data). One flaky `test_download_icon...` in combined chart batch (passes alone + full file green; chart code untouched). No commit/push. |
| **STRATEGY LAB state-consistency redesign (2026-09-12)** | Done - presentation-state only, engine/calcs untouched: per-side result ownership fingerprints (strategy+symbols+timeframe+dates+capital+costs+params; last-run universe preferred, single-symbol/config/live-selection fallback; unknown params never cry wolf) captured in `set_result`/`set_results`; single `_refresh_staleness()` renderer (NO_STRATEGY/READY/RUNNING/COMPLETE-current/COMPLETE-stale/FAILED-historical) fed by existing `config_changed`+`param_changed`+mode/strategy/run-state transitions. Stale sides get an amber `RESULTS OUT OF DATE` banner with one-click re-run (history kept, labeled — never silently current); failed runs with older results show `LAST RUN FAILED — SHOWING PREVIOUS RESULTS`; running/aggregating/finalizing hide the banner; strategy switch/empty-center/clear reset owners+results (no stale leakage; fresh dock stays READY + `--`). COMPARE labels currency on its own notice (dock hidden there). Metrics tiers reflow 4→2 cols under 620px (placement-tested; note `QGridLayout.rowCount()` never shrinks — assert via `itemAtPosition`). Ranking row height kept at 26px (deliberate: 40px+ would break pinned 10/14/18-row viewport contracts). +12 tests; lab 51/51, 00_app 260/260, ruff/format/pyright/validators clean on touched files; 48/48 offscreen visual checks (5 breakpoints, overlap/clipping/minimums). No commit/push. |
| **SYSTEM-BROKERS control-center redesign (2026-09-12)** | Done - presentation-only IA rebuild of `BrokersWorkspace` (Qt retained shell, no logic change): page header (SYSTEM / BROKERS + subtitle + primary + ADD BROKER) + per-broker status hero (icon+label+explanation, semantic states NOT CONFIGURED/CONNECTING/CONNECTED/DEGRADED/DISCONNECTED/ERROR + transient SYNCING) + BROKER HEALTH 6-capability grid (READY→✓, missing→— Unavailable, FAILED→⚠ with tooltip) + account/funds/positions/orders metric row + action bar (single PRIMARY: CONNECT when unconfigured/login-required, REFRESH when connected; SETTINGS/DISCONNECT secondary; Remove moved to More-menu, bootstrap confirms before erase) + collapsed CONNECTION SETTINGS (masked credentials only) + collapsed INTEGRATION DETAILS (callback URL + Copy, never prominent) + centered 1280px max-width scroll content + empty state + Add-broker selector dialog (future-venue ready) + step-based Connect progress dialog (authenticate→verify→connected, info-only). `BrokerManager` extended ONLY with safe read-only UI scalars (`account_id`, funds floats, open counts, `last_sync`, `can_refresh` — secrets never enter state/snapshot; auth/health/registry flows byte-identical). Bootstrap wires new `refresh_requested→submit_check`; remove-confirm copy aligned. +16 tests (13 workspace incl. hero/grid/primary/empty/dialogs/secret-hygiene, 3 manager details); 27/27 broker green, 00_app 247/248 (1 unrelated concurrent-workstream lab compare-board fail), ruff/format/pyright/validators clean on touched files. No commit/push. |
| **Performance screen hierarchy redesign (2026-09-10)** | Done - BACKTEST PERFORMANCE title + scope context ("BUY · LONG · 527 STOCKS ANALYZED"); ranking primary (direction chip, Rank-by dropdown synced with headers, search preserving global ranks, 9 cols with Return % from existing net_profit_pct, PF semantic colors, selected-stock details + captioned sparkline from existing per-symbol views, empty state); duplicate mini-equity removed (EQUITY tab canonical); explicit strategy/symbol equity captions; results dock 2:3 share. Numbers/formulas/sorts unchanged (single grouped derivation, same helpers). 527 acceptance PASS (publish 6.3s, order parity, spark parity, exact exports, tabs ≤123ms). Screenshot follow-ups: SHARPE stretch gap removed (columns pack left), perf context refreshes on selection change (selection relay signal); reported table/details overlap INVESTIGATED — geometry dumps + dark offscreen renders prove sequential stacking (no overlap; white band was unstyled-host artifact), widget hardened with own BG fill. +9 tests; blast radius green; ruff/format/pyright/validators clean. Follow-up: ranking viewport share raised (results dock 2:3 → 1:2; table 10/14/18 rows @768p/900p/1080p), details condensed into title line, sparkline 56–72px. No calc/OBR change. No commit/push. || **LIVE smoothness/perf pass (2026-09-10)** | Done - profiled FIRST (temp harness, deleted after): bottleneck = per-second UI-thread work, not strategy. Fixed: (1) `CandleRepository.detect()` one-shot (base+session in a single bounded fetch) + `get_candles_timeframe(..., detection=)` hint → SqliteTailProvider polls cache detection per symbol (steady-state poll = 1 bounded window fetch/symbol, no per-poll 5000-row sample scan; idle tail poll 5.2→1.4ms). (2) provider `last_close` map → `_refresh_mark` zero extra DB reads per tick (was get_candles(1) per symbol every tick). (3) validate() result cached on config tuple (idle snapshot was doing store scan + per-symbol timeframe detect + candle read every second) + chart-bars 500-bar backdrop cached (idle snapshot 37→~5ms warm). (4) activity drain cursor-only (dropped full-journal SIGNAL rescan) + persist cadence 10→30 ticks + activity drain every 2nd tick. (5) workspace `_render_chart` skips set_model when bars tuple identity+symbol+tf unchanged (zero chart pixmap rebuilds between candles; workspace refresh 12.5→5.8ms). Verified: 1604 pytest green, ruff/format/validators PASS, pyright 0 outside concurrent-workstream's test_stock_ranking (5 pre-existing errors, untouched file). OBR/broker/signal logic untouched — presentation/processing cadence only. |
| **Zerodha REAL-money LIVE layer (2026-09-10)** | Done - real Kite Connect execution path (SDK calls verified against installed kiteconnect 5.0.1, no fabrication). New in sanctioned SDK home `02_data/data/provider/zerodha/`: `live_trading.py` (TradingFace/BrokerAdapter-compatible: auth-verify, funds/positions/open-orders, tag=client_order_id idempotency + adopt + timeout-tag-resolve, whole-unit qty, MARKET/LIMIT, cancel/modify, poll-diff fill/reject/cancel stream, secrets never logged), `live_market_data.py` (throttled batched quote face), `live_activation.py` (env-gated `zerodha-live` registration + request_token exchange; history record untouched). New `BrokerFeedProvider` (execution: anchor-aware candle builder over injected face). Service: START-time venue probe (connect/health/account read-only), post-start mode assert (NEVER silent PAPER fallback), feed live/local labeling, ACCOUNT/EXECUTION/FEED gate rows. Arch-tests forced the placement (no SDK/creds/concrete imports under 09_broker adapters or app). +20 tests (16 venue incl. auth-fail/dedup/partial/cancel/reconnect + 4 bridge); gate green (ruff/format/imports/structure/language PASS, pyright 0 on touched files). Full suite: only concurrent-workstream fails remain (lab_workspace/stock_ranking half-applied edits + repo obr.py param edit 17:40 — NOT this task; canonical OBR record only read, never written). REMAINING EXTERNAL: VAYREN_ENABLE_LIVE_VENUE=true + VAYREN_ZERODHA_API_KEY + daily VAYREN_ZERODHA_ACCESS_TOKEN (browser login → request_token → exchange) + NSE equity symbols (F&O mapping out of scope) + operator LIVE confirm/arm. |
| **Slint UI slice-1 — application shell (2026-09-10)** | Done - UI tech decision egui→Slint (user directive, constitution strings updated). `rust/vayren-shell`: eframe removed, `slint 1.17` + `slint-build` + `i-slint-backend-testing` (dev); `ui/app.slint` (AppWindow: 104px nav rail CHART/LAB/RESEARCH/PORTFOLIO/LIVE/BROKER/DATA + Alt+1..7 FocusScope + status bar; top-level PendingCard + BrokerPanel components, in-out properties), `ui/palette.slint` (VAYREN dark global: #101418/#141922/#26a69a family), `src/shell.rs` (screen_title/screen_pending/env_kind/capability_rows/apply/select/wire/demo_snapshot + 2 tests; UI assertions ek hi test mein — Slint testing platform process-global), `build.rs`; view_model semantics UNCHANGED (honest health/LIVE derivation test-pinned). Migration state: broker = migrated surface; baaki screens honest PENDING MIGRATION (koi fake data/controls nahi). Gate: `build_rust.py` workspace-wide (shell bin existence check + `cargo test --workspace`), shell 6 tests green, fmt check 0. PySide6 app untouched (temporary coexistence, migration order follow karega — shell→nav→screens). Next slices: broker panel ko LIVE BrokerRegistry state se wire karna, phir Strategy Lab screen. |
| **Post-backtest display perf — 527 stocks (2026-09-10)** | Done - profiled FIRST (UI thread): blotter 57s ×2 (193k QTableWidgetItems each), history JSON 36.8s, equity paint 1564ms/paint, ranking rebuilds ×3, merge 1.9s + derives 2s. Fixes (presentation-only): TradeBlotter cap+refill + caption (same widget/API/tests), TradesView cap, history → QThreadPool w/ generation guard (same file/lines), `decimate_envelope` paint LOD (visually identical), single ranking rebuild + `batch_finalizing` → FINALIZING pill. UI finalization ~125s → ~9s (T2−T1 2.0s, T3−T1 11.1s, T5−T1 11.4s, tabs ≤133ms). 193,823 trades / 483 ranked / metrics / equity identical; repeat identical; memory flat 3 cycles; +5 tests; 180 touched-suite green; ruff/pyright/validators clean. Results, OBR, calc engine untouched. No commit/push. |
| **OBR strategy LIVE trading (2026-09-10)** | Done - Lab OBR executable on LIVE tab through the SAME compiled record (no logic copy; logic.on_bar shared with execute_bars). New: SqliteTailProvider (watermarked exactly-once closed candles, forming withheld, stale health, reconnect-safe, gap catch-up) + LiveTradingService (per-symbol LiveSession = independent OBR state, validate-then-start with exact blockers, PAPER default, LIVE confirm + arm, STOP halts ticks, live/ persistence, never auto-starts) + setup panel + POSITIONS table + bootstrap wiring (signals, Lab handoff, LIVE consent dialog). Proven: backtest==live parity, 1-candle-1-order, per-symbol isolation, reconnect no-dup, partial fills, STOP freeze, PAPER-never-venue, unarmed-LIVE-blocks. Real OBR v1.0: compiles, 0 blockers, PAPER RUNNING on RELIANCE 15m. +17 tests; gate green 1576 pytest, pyright 0. LIMITS: no real order venue in repo (LIVE fail-closed until TradingFace registered); candles from SQLite tail (no websocket); SUPPORTS_LIVE declared at composition layer (math untouched). NOTE 13:26: test_lab_workspace 2 fails (single_dominant_run_action, batch_progress_shows_counts) are a CONCURRENT workstream's half-applied edit (strategy_lab_workspace.py 13:22 connects _on_topbar_run which does not exist yet) — untouched by this task, do not fix here. |
| **Bugfix session - gate RED→GREEN (2026-09-10)** | Done - (1) dead `scripts/patch_research.py` deleted (stray `</ARG>` broke ruff/format/pyright/language-validator; old1/old2 strings absent in workspace = no-op, safe delete). (2) `test_batch_parity.py:209` None-guard (guarded ma/mb). (3) `CandleRepository` start/end forwarding fixed - fallback + limited paths dropped bounds, `get_candles(symbol, limit, start, end)` now forwards; temp-DB live-verified (fallback 8/8 in-range, aggregated 3/3 in-range, unbounded 20 intact). (4) untracked `test_batch_worker.py` lint: I001 auto-fix + E501 per-file-ignore (embedded strategy source, repo convention) + format. Gate: ruff 0, format clean, pyright 0, imports/structure/language PASS, 142 impacted green. |
| **Backtest batch perf — 527 stocks (2026-09-10)** | Done - measured bottleneck FIRST: data loading 90% of sequential run (aggregate ~70%, SQLite fetch ~25%); threads REJECTED (fetch 0.07× contention, agg GIL-bound 1.05×), bounded process pool 5.6×. `execute_bars` shared core (single+batch identical by construction; hoisted params/imports, running realized-PnL bit-identical) + `run_symbol_batch` (compile-once/worker, default 6 workers, deterministic symbol order, per-symbol error isolation, cancel-aware, inline fallback) in `backtest/runner.py`; `BacktestWorker` batch queue + `BatchEnqueued` (stock-level `batch_progress` only, no per-bar flood); coordinator single-batch drive (sequential chaining removed, `merge_results` unchanged); market `fetch_candles` optional start/end bounds + batch ±7d window (250/250 bound-parity SAFE incl. 1W/crash-month); lab UI `set_batch_progress` (`RUNNING BACKTEST… 123 / 527 · 23%`, pct-throttled). OBR.py ZERO touch. Acceptance (real OBR, 30m, 2022-24): baseline 1108.3s → optimized 74.2s = **14.94×**, 527/527 identical, 193823 trades identical, merged ranking identical, peak ~473MB/60-stock sample. Sizes 1/5/50/100 identical. +7 tests (`test_batch_parity` 5, `test_batch_worker` 2); full gate green (1550 pytest incl. smoke, pyright 0, ruff 0, imports/structure PASS). No new bus events, no new core files, no commit/push. |
| **Universal strategy-owned plotting (2026-09-08)** | Done - PlotEvent contract (10 types/9 markers/lifecycle/layers/deterministic IDs/versioning/serialization, text None default) + strategy plot API (7 emitters + update/remove, trading-separated, legacy mirror) + PlotStore (indexed viewport query, atomic batches, dirty region, telemetry, error isolation) + generic renderer (5 new glyphs, glyph-only style, layer batches, text cache, LOD, no strategy branches — source-scanned) + backtest/live/replay unification (`chart_plots`, bootstrap both paths); OBR trading frozen (71 parity green); +28 tests; gate green 1494 pytest, pyright 0. |
| **OBR NO-TRADE visual migration (2026-09-08)** | Done - owner-side 1-line in canonical record `_plot_obr_visuals`: `marker_square\|NO TRADE` → `marker_circle_x\|none` (glyph-only ⊗, exact bar 25 + ntEventVisPrice 101.24625 unchanged, single event, no duplicate); trading identical (72 parity green incl. new CIRCLE_X/text-None/exactness/negatives test); universal 28 green unchanged; universal infra ZERO touch. |
| **OBR legacy→universal migration (2026-09-08)** | Done - record `_plot_obr_visuals`/REF now universal-only (UP/DOWN_ARROW, SQUARE/DIAMOND exits, CIRCLE_X NT, REF rays ext visual-only, SL level CREATE→UPDATE→COMPLETE, all text None, exact bars/prices, PNL pills dropped); legacy `_marker`/`plot()`/meta writes removed; base `_mirror_legacy` deleted (was the second visible path); legacy `plot()` API intact for direct callers; trading identical (72 parity incl. rewritten universal visual asserts); universal 28 unchanged; 1495 pytest green, product chapters ruff/format/pyright 0. |
| **OBR BUY/SELL conflict fix (2026-09-08)** | Done - generic ownership suppression in TradeOverlay: result-owned MARKER bars (duck-typed, REMOVED/HIDDEN/malformed safe) cover same-bar execution pills; connection + focused detail + generic no-plot executions intact; no strategy names/semantics/renderer changes; +10 overlay tests + OBR runner→overlay e2e (pills 0, connection paints); 1506 pytest green, product chapters clean. |
| **OBR marking redesign (2026-09-08)** | Done - reference-style: tip-anchored triangles (tip exact price, screen-space only) + compact pills (+6/+2 pad, r2, same font/colors/layers); OBR entries owner text "BUY/SELL {price:.2f}" iff showEntryLabel else None, rest textless; dedup via bar-coverage (e2e: exactly 1 pill cluster); trading identical; +3 universal tests; 1509 pytest green, product chapters clean. |
| **OBR exact-match style (2026-09-08)** | Done - EOD blue up-arrow + "EOD {price} {signed pnl}" on TIME exits only (new generic TRIANGLE_BLUE primitive, proven-necessary); SL exits SQUARE→DIAMOND textless; pills solid marker-color + white text; UP pills below / DOWN above; entries/NT/REF/SL-level unchanged; trading identical; 1510 pytest green, product chapters clean. |
| **OBR exact reference visual (2026-09-08)** | Done - SL zero chart visual (all SL plot code removed from record; engine/SL/PnL intact) + EOD only on non-entry-bar TIME exits + same-bar TIME→diamond + generic muted-bars channel (base/runner/result/directional/overlay, renderer untouched); §29a/b/c green; 1516 pytest green, product chapters clean. |
| **Screenshot pill fix (2026-09-08)** | Done - "OD" clipping root-caused to measure/draw font mismatch (fixed: marker font for whole draw call) + edge pill clamp in `_layout_marker_pills` (optional bounds, honest displaced flag); OBR/record/contract untouched; +2 universal tests; 1518 pytest green, product chapters clean. |
| **RESEARCH LAB workstation (2026-09-08)** | Done - hypothesis-first lab: command bar, 17 metrics (real drawdown/Sharpe/Sortino/payoff; CAGR/vol/exposure/turnover honest N/A), 8-col signal/trade inspectors + trade detail (MFE/MAE N/A), robustness/validation via real engine, data-quality NOT CHECKED, reproducibility trace, comparison NO RESULT, real-action log; +8 tests (15 green); gate green 1466 pytest. ZERO new files, OBR untouched. |
| **OBR ref/extend params (2026-09-08)** | Done - refIndex=1/3/4/5 moves engine+visuals (test-pinned); extendBars param 5/1/20 visual-only, engine default aligned 5 (never read by logic); teal/red REF rays pinned; +10 tests (71 green); gate green (1458 pytest, pyright 0); live smoke identical 6 trades. ZERO new core files, markers untouched. |
| **OBR chart cleanup (2026-09-08)** | Done - REF/SL short visuals only: renderer `bars:N` ray cap (REF=5) + SL `none` exact span; cross-series deterministic pill de-collision (glyph exact, priority BUY/SELL>EXIT>NT); +9 tests in existing parity file (62 green); gate green (1449 pytest, pyright 0); live smoke identical 6 trades. ZERO new core files, trading unchanged. |
| **OBR chart visuals (2026-09-08)** | Done - adapter plots engine-event series (BUY/SELL/SL-line/EXIT-SL+PNL/EXIT-TIME+PNL/NT, exact bars+values, style meta); PlotOverlay additive marker_up/down/square glyph+pill branch (line path identical); +12 tests in existing parity file (53 green); gate green (1440 pytest, pyright 0); live smoke 6/6 SIGNAL-closes, trading unchanged. ZERO new core files. |
| **OBR Pine 1:1 parity (2026-09-08)** | Done - canonical `D:\VAYREN_STRATEGIES\OBR.py` audited vs verbatim Pine rules; engine LAYER 0-12 untouched; adapter: same-bar entry+exit suppression + conditional IST attach; `test_obr_pine_parity.py` 41 green; gate green (1597 pytest, pyright 0); live smoke AMBUJACEM 6/6 SIGNAL-closes. Desktop `OBR.txt` EMPTY (no Pine file to diff). Repo `obr.py` RSI strategies unrelated/untouched. |
| **Release tooling (2026-09-07)** | Done - `scripts/release.py --version X.Y.Z` (bump→stage→secret-scan→commit→tag→push→gh attempt, ~30s; disposable-copy proven). `make release VERSION=x`. Gate stays pre-release + CI. |
| **FINAL quality pass (2026-09-07)** | Done - no future phase. Product audit → `ui_kit` shared widgets; LIVE rail scroll-safe + visible gate reasons + row reuse + proportional splitter + sized tables; Research service + workspace (real engine); Portfolio workspace (shared state, honest funds); nav/window/bootstrap wiring; responsive harness (5 sizes green); dead-UI sweep; +61 tests. Full gate green. |
| **FINAL language migration (2026-09-07)** | Done - no future migration phase. Rust workspace `rust/` (`vayren-core` cdylib std-only + `vayren-shell` egui broker panel); migrated: order lifecycle table, backtest metrics kernels, timeframe aggregation kernel (parity-proven, Python math deleted, zero duplicate authority); `core.native` loader + thin bridges; ownership validator (baseline 491, anti-reintroduction AST, crate hygiene) in gate+CI; Qt UI proven-retained, strategy/AI/research Python-owned, retention manifest carries proofs. Full gate green. |
| **FINAL broker architecture (2026-09-07)** | Done - no future phases. UBL frozen (identity, credential lifecycle w/ broker-scope-expiry, health); market-data full lifecycle + malformed guard; AccountSnapshot identity + funds→risk mapping + LIVE funds validation; engine snapshot/restore + checkpoint idempotency + RECONCILING startup path; SAFE/WARNING/BLOCKED verdicts + funds reconcile + journaling; timeout/backoff/reconnect policies; skeleton reference adapter + registration proof; 12-step activation ceremony (pure verifier); journal causal chain w/ broker+env; 20 architecture proofs. **1344 tests**, 28/28, gate green. LIVE = NOT_CONFIGURED. Remainders EXTERNAL ONLY: broker account, credentials, permissions, KYC, endpoint activation, operator LIVE consent |
| **Phase 25 UBL M8 (2026-09-07)** | Done - production adapter framework on M6/M7 tree: tri-state `CapabilityStatus` + `surface_status()` (bool compat kept); `broker/credentials.py` (key-ref, env-aware, redacted) + `broker/health.py` (8 generic states, legacy wrappers, health≠LIVE); network denylist + SDK boundary (validator-enforced); `MODIFY_PENDING`/`MODIFIED` additive; optional `idempotency_key`; subscribe/unsubscribe; generic harness `test_adapter_contract.py` (38) + arch proofs → 15; Zerodha history-only; UI unchanged (honest). **1308 tests**, 28/28, gate green. LIVE = NOT_CONFIGURED. Baaki: real-broker venue + credentials + account confirmation (M9+) |
| **Phase 24 UBL M7 (2026-09-07)** | Done (scoped) - bootstrap historical face seedha UBL registry se (build_provider caller hata, def retained); `register_provider`/`register_adapter` retired (tests/bench direct `BrokerRegistry.register`; duplicates ab fail-closed `DuplicateBrokerError`); `DownloadSettings.provider` documented derived (INVARIANT = selection.name); arch tests +5 (M7-A–E) + `00_app` branching scan; +20 `test_m7_direct_ubl.py`; gate green; LIVE = NOT_CONFIGURED. Retained (blockers documented): `build_provider`, `resolve_broker`, `Provider`, `BrokerAdapter`, transports (Zerodha transport M5-decision se `02_data` mein). Baaki: UBL trading-resolve + credentials model, phir full retirement |
| **Phase 21 UBL M4 (2026-09-06)** | Done - `FileSelectionStore` (atomic JSON, fail-closed, no secrets) + `BrokerSelectionService` (CLI→persisted→compat-default precedence, registry-validated) + `--broker` CLI + bootstrap derives `settings.provider` from selection + capability-absent → `unavailable_provider` fail-closed + LIVE pill from selection (trading-capable only) + data-panel registry-backed selector. **1188 tests**, 28/28, gate green. Paper/Sandbox/gates untouched. Baaki: M5 adapter move, M6 funds, M7 shim retirement |
| **Phase 20 UBL M1-M3 (2026-09-06)** | Done - `09_broker/broker/` (vocab/capabilities/faces/registry/selection) + delegation shims in `data.provider.factory` + `execution.broker.factory`. One registry, one selection contract, unified error/capability vocabulary; sentinel identity + PAPER downgrade + gates preserved; **1152 tests**, 28/28 partitions, gate green. Baaki: M4 UX + engine wiring, M5 adapter move, M6 funds, M7 shim retirement |
| **Phase 19 UBL audit (2026-09-06)** | Design-only - `90_brain/broker_layer_design.md` ready for implementation: dual boundary audit + D1-D8 findings + 09_broker contract design + migration M1-M7 + required tests. Zero product code. Decision pending: M4 selection UX |
| **Phase 18 speed engine (2026-09-06)** | Done + PUSHED - 2 local commits (`ed68c28` Phase-18 + `a3a5d65` execution stack) aur tag `v1.9.0` sab `origin` par live; worktree clean. `scripts/speed/` + dashboard + 31 tests → **1117 total**, 27/27, gate green. GitHub Release OBJECT pending (`gh` token invalid) |
| **Phase 17 LIVE workspace (2026-09-05)** | Done - LIVE tab is a real execution-control workspace (status bar, mode selector, readiness gates + reasons, backend-gated ARM, kill banner, strategy/position/risk panels, shared chart widget, orders/fills tables, PnL strip, reconcile, broker panel, filterable event stream). Pure view over injected state; 22 tests incl. real PaperService E2E render |
| **Phase 15 live-readiness (2026-09-05)** | Done - REAL_BROKER_UNSPECIFIED (Zerodha data-only, no execution venue anywhere; nothing invented). SandboxBroker + credentials (store-only, redacted) + 5 named gates on existing primitives + account confirm + explicit arming (LIVE submit gate) + ReadOnlyBroker + retry/rate-limit/clock utilities + contract conformance suite + redaction tests + --check-live arming/clock/env lines + sandbox benchmark + SDK-boundary validator rule. LIVE fail-closed, impossible to trigger accidentally |
| **Phase 14 sandbox (2026-09-05)** | Done - SandboxBroker (identity, credential-gated, scripted fills, disconnect sim) + credentials (store-only values, redacted) + 5 named gates on existing primitives + account confirm (sandbox never LIVE) + --check-live (no orders possible) + session stream-generalized (paper parity intact). E2E + 13 failure sims + reconcile-block + paper/sandbox + replay parity. Benchmarks recorded. LIVE = NOT_CONFIGURED (correct) |
| **Phase 13 --paper (2026-09-05)** | Done - PaperService composes existing layers headlessly (no Qt/threads); real CLI E2E 40 events/12 signals/11 fills/PnL +2822/CLEAN; --live fail-closed RC=2; checkpoint recovery + idempotent shutdown; 9 tests A-L; bootstrap benchmark ~42ms |
| **Live execution layer (2026-09-05)** | Done - 07_risk (fail-closed gates, kill switch, 17 tests) + 08_execution (paper sessions, broker boundary, portfolio, journal/replay, regime, adaptive). PAPER default, LIVE 5 gates, broker integration NOT_CONFIGURED. Parity measured. Baselines: dispatch ~932k ev/s, risk ~0.017ms, paper 60-candle ~3ms. Next: bootstrap wiring, live venue, execution UI |
| **AEOS Phase 3 (2026-09-05)** | Done — same-command replays (n=1-4 by cost): validation median 2.37×/mean 2.60×/540s saved; biggest SMALL 4.33×, min REFACTOR 1.01×, zero regressions; end-to-end NOT MEASURED (repeats = theater); bottleneck = agent work ~94.5% of wall (validation only ~5.5%); no new optimization passed §18 (xdist rejected on risk, parallel rejected 0.9×); utilization 89→91; human 0 |
| **AEOS-X Phase 2 (2026-09-05)** | Done — 9/10 corpus executed+recorded (MIGRATE obsolete: IDs already in adapter), Qt ROOT CAUSE (live QThread workers at exit) fixed by conftest autouse fixture → pytest 925 RC=0 + run_tests 25/25. `benchmark.py impact/replay/begin` live; impact median 53.5s vs full 81.8s (1.5× operational); parallel validation REJECTED (0.9× measured). Utilization 85→89. Human interventions: 0 |
| **AEOS velocity baseline (2026-09-05)** | Done — `scripts/benchmark.py` + `benchmark_corpus.md` (10 real specs + protocol) + `benchmark_scoreboard.md` (generated) + `benchmark_runs.jsonl`. Gate medians: pytest ~82s, pyright ~23s, rest <2s. Pyright full-scope enforced (22→0, 1 genuine TradeRecord bug fixed). BENCH-TEST-01 executed (gate-coverage test). Qt teardown abort measured 7/9 RC-captured runs (pre-existing, harness reports `flake-teardown`, root-cause deferred). AGENTS.md fast-path bullet added |
| **Validation gate (2026-09-05)** | Done — `pytest` default 914 tests (92 restored + coverage/test additions), validators 7 domains, `ruff check`/format/pyright 0 (pyright scope CLOSED this turn: chart/strategy/backtest enforced, 22→0). 00_app partition + full-suite Qt teardown abort pre-existing (clean HEAD par prove; harness reports `flake-teardown`). `run_parameter_sensitivity` ab `variant_executor` injection leta hai (`backtest.runner.run_variant_backtest`); `if TYPE_CHECKING:` imports validator-exempt (runtime graph enforced) |
| **Coding-Time Forensics (session capture)** | âœ… Done (2026-08-15) â€” TOTAL = TASK_END âˆ’ TASK_START (diff 0), immutable start/end events, gaps â†’ UNKNOWN (kabhi "AI thinking" nahi), report TOTAL/MEASURED/UNKNOWN rows + PARTITION CHECK, 9 naye tests (controlled timestamps) â†’ **638 total**, make check green. **Baaki**: koi production optimization nahi (user directive â€” measurement only) |
| **02_data Historical Download Engine (Phase 6)** | âœ… Done (2026-08-14) â€” ported engine + storage/provider/downloader packages, 8 events, DownloadWorker, native UI (panel/status/log - ab main window ke side panel mein, Phase 6F, multi-stock checklist Phase 6G, checklist height fix Phase 6H), `data_manifest()` registered in `_build_architecture`, services `data_engine/data_worker/data_window`, 97 naye tests â†’ **619 total**, make check green. **Baaki**: asli Kite download ka live QA (real credentials ke saath), coverage/status UI polish, download history/persistence |
| **Provider decoupling (Phase 6L)** | âœ… Done (2026-08-17) â€” engine broker-free: contract + ZerodhaProvider + factory (`settings.provider` dispatch), sentinels contract mein, bootstrap `build_provider`, engine behavior 100% same (tests wahi green), subprocess isolation test. **686 total tests**, gate green. **Baaki**: Provider B (user directive — abhi nahi); Kite interval ids + NSE calendar settings/UI mein rehte hain (engine unse anjaan); live QA abhi baaki |
| **Provider Credentials Manager (Phase 6N)** | âœ… Done (2026-08-17) â€” in-app configure (ProviderCredentialsDialog: masked secrets + eye + re-mask, Test Connection generic ✓/✕, Save & Connect, Clear with confirm), secure storage (Windows Credential Manager ctypes fallback file store — keyring dependency nahi), layered per-field store→env→none, env instructions Advanced mein collapsed, status bina restart refresh (post-dialog reload), secrets kabhi logs/UI error mein nahi. **737 total tests** (+43), gate green. **Baaki**: real device par CredMan live QA; TOTP/user/password optional rehte hain (required sirf API Key/Secret); live download QA |
| Load-all bug fix | âœ… Done (Phase 5C) â€” data poori load hota hai (2016â†’2026, 61k+ rows), `limit: int \| None = None` end-to-end; Phase 5F fix ke baad initial viewport latest `INITIAL_BARS` dikhata hai (fit-whole-dataset hata diya). App default `--limit` None; explicit cap abhi bhi kaam karta hai |
| Universal Foundation Part 2 | âœ… System intelligence layer done (2026-08-14) â€” `core/system/`: ComponentGraph, CapabilityGraph, EventGraph, DataFlowModel, Workflow/WorkflowRegistry, change impact (`analyze_change` + RiskLevel HIGH/MEDIUM/LOW), `SystemSnapshot` (`build_snapshot` + `render()`), `SystemModel` facade over `ComponentRegistry` + workflows (172 core tests, sab green). **Baaki**: `vayren --describe` CLI, per-chapter manifests (`market/components.py` style) â€” **âœ… manifests Part 4 mein production ho gaye** (`market/manifest.py`, `chart/manifest.py`), declarative composition root â€” **âœ… Bootstrap hi composition root hai (Part 4: `_build_architecture` add hua)**, workflow **execution engine** (abhi sirf declarative model hai), health polling, config source |
| Universal Foundation Part 3 | âœ… AI engineering + evolution layer done (2026-08-14) â€” `core/ai/`: Intent â†’ Plan â†’ PlanValidator (Policy) â†’ ChangeSimulation â†’ Sandbox (strict lifecycle, deploy = record) + AiBoundary (fail-closed) + EngineeringMemory + PerformanceMemory + OptimizationStudy (`adopt()` hamesha raises) + AiProviderRegistry (OfflineProvider default, AI optional) + ContextBuilder (deterministic provider context). Core tests 172 â†’ **512, sab green**, ruff/pyright 0. **Baaki**: real provider adapter (LLM) kabhi add ho to registry se â€” runtime boundary fixed rehta hai |
| Universal Foundation Part 4 | âœ… Runtime integration done (2026-08-14) â€” **market + chart real manifests production mein** (`market/manifest.py`, `chart/manifest.py`), Bootstrap `_build_architecture()` par live service instances register karta hai + `SystemModel` build (startup par ek baar). Old `services` registry untouched, EventBus 0 changes, ek hi runtime. Integration tests 10 naye â†’ **522 total**. **Measured**: architecture build = 0.134 ms (2.05% of 6.54 ms startup), capability lookup 2.3 Âµs, dispatch 0.3 Âµs, snapshot 0.024 ms. POC ab production manifests use karta hai (single source of truth). **Baaki**: `vayren --describe` CLI, baaki components ka incremental migration (same pattern), workflow execution engine |
| Timeframe selector UI | Phase 5B ne market side ready kiya (`TimeframeChanged`/`ListTimeframes` events + detection) â€” ab UI (selector dropdown/combo) aana baaki hai, wo `ListTimeframes` publish karke list le aur `TimeframeChanged` publish kare |
| Chart par daily/weekly bars ka visual QA | `1D`/`1W` bars midnight timestamp pe â€” desktop par axis labels check karo |
  | Crosshair tooltip/OHLC readout | âœ… Done (Phase 5A + bugfix) â€” crosshair snaps to nearest candle; bottom time label centered at crosshair X (ISO format: intraday `YYYY-MM-DD HH:MM`, daily/weekly/monthly `YYYY-MM-DD`; flat teal tag, square corners, dark centered text, edge-clamped), right price label centered at crosshair Y, symbol info + OHLC single-line in top bar. Time label multi-line bug fixed |
| Zoom/pan/resize ka visual QA | Live boot verified â€” asli desktop par haath se chart interactions check karo |
| `data/vayren.db` + `seed_sample_db.py` | Ab unused â€” Phase 1 legacy. Hata sakte hain jab chahe (tests independent hain) |
| Watchlist panel | âœ… Done (Phase 5H + rebuild) â€” `WatchlistWidget`: header `Watchlist â–¼ + â†© â‹¯` + separators + `Symbol / Sort by â–¼` row + styled stock list. **Rows ab real market info dikhate hain (Phase 5N)**: symbol + price (line 1), change % bull/bear (line 2) â€” real SQLite quotes, ek baar load, cached. Company name/icons/markers app mein exist nahi karte â†’ hamesha omit (kabhi invent nahi). Filter row [S][G][T] / bottom bar [Grid][Edit][â‹¯] â€” koi existing controls nahi â†’ banaye nahi. Sirf list scroll karta hai (header/sort fixed) |
| Options section | Removed â€” empty 56px dark strip watchlist aur chart ke beech mein tha, user ne screenshot mein target kiya. `OptionsPanel` widget + ctor param + splitter item + tests sab delete (chart expand ho gaya freed space mein). Indicators/drawing tooling aage kabhi aaye to **naya module** (purana kabhi expand nahi hota) |
| Credentials | `ZerodhaCredentials.from_env()` â€” `VAYREN_ZERODHA_API_KEY/SECRET/USER_ID/PASSWORD/TOTP_SECRET` env se, **auth time par** hi padhte hain; kabhi settings/events/logs/UI/SystemModel mein nahi; repr redacts |
| Data domain (Phase 6) | `02_data/data` = historical downloader (write path; market = read path). Engine library-only (CLI nahi). Console â†’ Reporter protocol; hard exit â†’ returns; SIGINT â†’ `cancel()`/`reset()`. Engine **sirf worker thread** mein; worker signals â†’ `bus.publish` main thread (bus thread-safe nahi); subscriptions sirf Bootstrap. Data paths sab `<data_dir>` (env `VAYREN_DATA_DIR` / `--data-dir`; app default `D:\ZerodhaTradingData`): `token.json`, `logs/`, `symbols.csv` (missing â†’ DB-derived fallback, default interval 15m). Coverage scans **bina engine lock** (deliberate) |
| Engine renewal semantics | Pehli sweep failure â†’ `_auth.renew()` ek baar â†’ retry ek baar; doosri failure â†’ `{"ok": False}` bina `error` key + `_abort=True`; sirf renewal **exception** â†’ `{"error": "auth"}` |
| CandleDB quirks (test-pinned) | non-date strings verbatim store (upsert count); `ohlcv` NOT NULL (corruption rows sirf legacy DBs mein); `load_symbols` missing CSV â†’ FileNotFoundError (resolve_symbols fallback); CSV cols `trading_symbol,interval`; scanner DOWNLOAD_COMPLETE = poori target window covered (earliest â‰ˆ target_start AND latest â‰ˆ now) |
| Hatchling + `.gitignore` gotcha | Hatchling `.gitignore` ko wheel exclude spec mein merge karta hai. Bare `data/` pattern = kisi bhi depth ka data dir exclude â†’ `02_data/data` wheel + editable `.pth` se silently gayab (import fail). Fix: `/data/` root-anchored. Editable reinstall ke baad `02_data` `.pth` mein aana chahiye |
| 527 stocks ka UX | Watchlist ab header row ke saath hai â€” search/filter aur watchlist persistence aage ke phase (architecture.md Future dekho) |

## 6. Known Oddities (Koi Problem Nahi)

| Cheez | Detail |
|---|---|
| Offscreen Qt warnings | Sirf headless mein aate hain, desktop par nahi |
| Em dash title | `VAYREN â€” SYMBOL` â€” console mein `Ã¢ï¿½ï¿½` dikhe to sirf codepage; GUI sahi dikhata hai |
| Antialiasing off | Time-axis aur crosshair lines crisp rakhne ke liye shapes pe `Antialiasing=False` (text pe `TextAntialiasing=True`) â€” blended pixels 0 |
| Offscreen grab unreliable | Widget pe `grab()` offscreen mein crash/flaky â€” widget tests pixel-free (state + monkeypatch); pixel checks sirf QImage pe |
| Partial last bucket | 25 bars/day (odd) â†’ last 30m bar sirf 15:15 ka hota hai (genuine data state, TradingView jaisa) |
| 15:15 bar | Day ka 25th bar â€” session-anchored buckets isse apne aap handle karte hain |

## 7. Future

Naya kaam shuru karo toh pehle `90_brain/architecture.md (Future)` dekho â€” kaunsa phase, kaunsa chapter.

> Brain state bataata hai: kya bana, kahan hai, kya baaki. Update karte rehna.


## Market crosshair/cursor offset — ROOT fix (2026-09-16)
Slint Market ka horizontal crosshair cursor se neeche tha (bottom par ~83px @1280x720, ~128px @1920x1080). ROOT CAUSE: `rust/vayren-shell/ui/market.slint` pointer Y ko **full plot height** (price grid + volume strip) ka fraction bhejta tha, jabki legacy Qt (`CandleChartWidget._chart_rects/_price_from_y`) + saari Slint rendering (candle/overlay/marker/crosshair `× grid-h`) + Rust `HoverMoved(price = high - fy*span)` price range SIRF **price grid** (85%) mein maante hain. Render `hover_y × grid-h` hone se line hamesha `0.85 × cursor-Y` par padti thi — proportional, resolution-dependent offset. FIX (1 file, 4 denominators, koi logic change nahi): `plot.height → plot.grid-h` in `px-fy`, `plot-h-px`, `price-anchor-fy`, `drag-start` Y. X mapping pehle se exact tha (plot.width domain dono taraf; sirf intentional candle snapping, half-slot max). Rust/Python/backend/markers/zoom-direction/pan-formula/styling untouched. Verify: headless mapping proof `C:\Users\visha\AppData\Local\Temp\opencode\crosshair_mapping_proof.py` — 3 resolutions × 4 DPI, NEW offset 0.0px, snap+price == legacy; Python chart crosshair/viewport 62/62 + slint_market_host 12/12 PASS. NOTE (pre-existing, is task se unrelated): concurrent workstream `rust/vayren-shell/src/market.rs` ko mid-refactor chhod gaya hai (Settings/SourceIndicator arms bina enum variants → lib compile red; golden zoom/pan tests bhi usi drift se fail) — crosshair fix se independent, chhua nahi. Local only, no commit/push.


## Packaging
- EXE build: `.venv/Scripts/pyinstaller scripts/assets/vayren.spec --noconfirm` -> build/dist/Vayren/Vayren.exe; icon scripts/assets/make_icon.py se regenerate hota hai.

## Constitution
- `ARCHITECTURE_CONSTITUTION.md` (repo root) ADOPTED hai — naya code hamesha ownership table ke hisaab se language choose karo; legacy Python ko preserve rakho, big-bang migration MANA hai.

## Dead Code Cleanup (2026-09-12)
Import-graph scan se unreferenced files delete kiye:  2_data/data/logging_setup.py (koi caller nahi),  6_backtest/backtest/optimizer.py (orphan), scripts/_fix_r11.py (one-shot done), scripts/bench_execution.py, scripts/bench_migration.py, scripts/language_audit.py (superseded), 10_research/ (empty scaffold), strategy_lab.html, SCAN_REPORT.md. Retention entries + alidate_structure.py whitelist update kiye. All validators + touched-module tests pass.

## Permanent Global UI System — foundation slice (2026-09-12)
Mission (permanent Apple-grade + institutional-terminal design system) ka foundation slice — big-bang nahi (constitution s11), isliye: (1) `UI_DESIGN_SYSTEM.md` repo root par = permanent source of truth (tokens s3 lab_theme values = canonical: BG0 #070B10…WARN #DDAA45, FS_* 11–26 floor 11, SP_* 2–24, H_*/RADIUS/ROW_HEIGHT; layout/responsive/DPI/state/table/chart/number/nav/keyboard/a11y/motion/perf/ownership/audit rules + s13 incident log + s15 agent rule). NO ONE-OFF UI = defect. (2) Slint target-arch: `palette.slint` canonical tokens par (VayrenPalette+VayrenDesign globals, <length> typed — int se compile fail hota hai) + naya `components.slint` (VButton 4 variants/VBadge/VStatusDot/VSection/VKpi/VBanner/VEmptyState, tone 0–4); `app.slint` tokenized (9/10px→FS_LABEL, teal/red→pos/neg, PendingCard VBadge reuse). Shell 6/6 + build_rust EXIT=0. (3) Qt retained shell convergence — SIRF existing files (nayi Qt file = validator HARD FAIL): lab_theme self-drift (#008F84→ACCENT_DIM), ui_kit→SP_*/FS_*/label()/body() (PAD_LG 8=SP_MD; GAP 3/6 off-scale→4/8), top_nav_bar/trade_context/event_log/system_health/market_status tokenized, candle_chart_widget #5d6778→theme.PLACEHOLDER. 02_data/05_strategy/06_backtest/04_chart-widgets Qt drift CHHODA (data→app import = forbidden backward import; chart ke apne theme tokens; workspaces par concurrent stream ke uncommitted diffs — per-slice follow-up neeche). Verify: ruff/format/pyright 0, 4 validators PASS, chart 209/209, 00_app full (lab 60/60 + baaki suite) green, responsive-geometry 6/6. PRE-EXISTING (not ours): test_ui_kit::test_configure_table_conventions chart_window ke BAAD run par fail (defaultSectionSize 24 vs 22 — stash karke bina meri change ke reproduce kiya); DPI 125/150/175% matrix headless me possible nahi — manual gate baaki. Next slices: brokers/live/research/strategy_lab/stock_ranking/watchlist Qt drift (inventory is turn ke explore-task me), phir Slint screen migration order par. Local only, no commit/push.

## Strategy Lab premium rebuild — presentation slice (2026-09-12)
Mission (global premium institutional UI, Strategy Lab primary) ka presentation slice — REAL app par implement + render-verify. Architecture §1 enforced throughout: gate baseline 4 pre-existing FAILs (concurrent stream ke naye Qt widgets) — ZERO new violations (koi naya Qt subclass nahi; sirf existing classes ke methods/fields); validators (structure/imports/language-587/migration-12) ALL PASS; koi validator weaken nahi. (1) Empty-dock compaction (§20): no-strategy+no-results+ready par tab strip hidden + stack zero-height (maxHeight clamp — sizeHint sprawl fix) — pinned contract intact (results_open True, READY, NET PROFIT --; test_results_visible_with_ready_state untouched PASS). User-expand pin karta hai; strategy/result/mode transition restore karta hai. (2) Library taxonomy (§13): ALL/★/RECENT filter row + session-local favorites (menu toggle, star rows) + recent tracking (open order, cap 10) + No-matches vs No-strategies split empty states + select_name fallback. (3) Token sweep: strategy_lab_workspace.py me ZERO hex literals (BUY/SELL→t.ACCENT/t.NEG aliases, #04211E→ACCENT_DEEP, #FFFFFF→TEXT, painter→BG1/TEXT2/BORDER/TEXT) + ZERO px sizes (9/10→FS_LABEL, 11→LABEL, 12→SMALL, 13→TABLE, 14→BODY, 16→METRIC_SM, 24→DISPLAY, 28→HERO); _StrategyRow/library panels tokenized. (4) Identity line (§14): topbar crumb-meta "v1.0 · modified 11 Sep 26" — open_strategy(name, code, meta=None) backward-compat + bootstrap record metadata pass; unknown→empty (never invented). Behavior proof: REAL Bootstrap offscreen (QT_QPA_FONTDIR=C:\Windows\Fonts for real text) — OBR compile→registry→1-symbol RELIANCE 15m run→COMPLETE, renders verified 1920/1366 + QT_SCALE_FACTOR=1.5 (no overlap/clip; responsive-geometry 6/6). Numbers identical pre/post (UNPROFITABLE, Net P&L 1,85,236, 71/256 trades). Tests: lab 64/64 (4 new: taxonomy×3 + identity), lab-related 117/117, chart+strategy-ui 209+1skip, runtime-integration 11/11; test_results_reachable... ko open_strategy diya (compaction me zero-height ranking scroll-target nahi — intent-preserving). PRE-EXISTING (not ours): ui_kit defaultSectionSize order-fail (chart_window ke baad), bootstrap.py pyright 2 errors (stash-proved), DPI 125/175% + ultrawide manual gate baaki. Harness scripts Temp me (repo me koi mock nahi). Local only, no commit/push.

## MARKET WIPRO BUG � drain never delivered taps (2026-09-16)
Symptom: watchlist tap changed header but candles stayed old. ROOT CAUSE: `SlintMarketHost._drain_actions` passed a `create_string_buffer` (c_char_Array) to the `LP_c_ubyte` `next_action` ABI slot -> ctypes.ArgumentError on EVERY pump, silently swallowed by contextlib.suppress -> queued `select:X` never reached `apply_market_action` -> backend never reloaded. Header updated via optimistic local SelectSymbol; chart kept old bars. FIX (Python host only): view buffer as `(c_uint8*N).from_buffer` like the render path. Verified: native tap -> select:360ONE drained -> model+bars follow; WIPRO/WOCKPHARMA/ZEEL at 30m and 5m all PASS with OBR markers/rays. New regression test `test_drain_delivers_queued_select_to_sink` (13/13 host tests green). Note: 5m falls back to 15m base bars per legacy repository semantics. Local only, no commit/push.

## DEEP PYTHON/Qt AUDIT + MICRO-CLEANUP (2026-09-17)
109 Qt-touching Python files censused (AST spans + submodule evidence), 43-file prod-UI set measured EXACT = 22,909 LOC (repo total py 117,642 + rs 25,747 + slint 11,795 = 155,184). Reference census (prod vs test importers) + instantiation grep + chart_window mount analysis: pure-legacy zero-prod-use = live 979 + brokers 1036 + portfolio 1056 = 3071 lines (never constructed in prod; only frozen tests import them) — file deletion BLOCKED (test_navigation.py + test_live_workspace.py concurrent-frozen import them; chhuna mana hai). Visible Qt in prod = 2604 (TopNavBar + event_log/system_health/market_status + full download-panel chain, all mounted/toggleable). Baaki 17,234 = constructed-but-hidden state-holders/bridges/fallbacks (lab 4350 = Slint snapshot/action bridge; research 1737 = no-host fallback; chart stack 6485 = hidden-splitter state holders + fallback surface; panels/helpers rest). SAFE CHANGE (1 file, +7/−5): `app/ui/__init__.py` se LiveWorkspace/PortfolioWorkspace re-export hataya (attribute-style `from app.ui import X` users ZERO — repo-wide proven; tests module-path se import karte hain) → dono dead modules ab prod me kahin import nahi hote (import-edge proof re-run: sirf 4 test files). Verify: imports/structure/language(610)/arch/migration gates PASS, navigation+portfolio+brokers+ui_kit+geometry 58/58 PASS. Classification: PARTIAL (3071 lines test-only legacy remain, frozen-test-blocked). Local only, no commit/push.

## PRODUCTION CLEANUP — obsolete UI/scratch removal (2026-09-17)
SAFE cleanup only (migration already 6/6 Slint; no migration, no rewrite, no backend touch). DELETED 38 files / 806 lines / 0 added: `web/` React prototype 11 files × 532 lines (never wired — App.tsx me TODO, zero production refs, README ka "all new UI = TypeScript/React" rule CONSTITUTION §3/§10 ke khilaf, Slint Market screen ka stale duplicate) + `ray_probe.py` 21 + `scripts/render_broker_view.py` 141 (untracked scratch, zero refs, Rust render tests cover karte hain) + `.b1..b10`/`.t1/.t2` captured build/test logs 24 files × 100 lines + `.vayren-rt-verify/live/live_session.json` 12 (stale verify-run data, zero refs). PRESERVED: all 5 Qt workspaces (live/brokers/portfolio/research/lab — tests import karte hain; lab+research bootstrap me constructed state-holder/bridge hain; chart Qt widgets Slint hosts ke canonical state holders hain), saara Python strategy/research/AI/backend, saare Rust view crates + Slint screens + slint hosts. Verify: cargo check -p vayren-shell EXIT 0 (Slint compile incl.), imports/architecture/language-ownership(610)/migration-gate PASS, navigation + 6 slint-host suites 67/67 PASS, ruff format clean (ruff check ke 2 errors live_trading_service.py me PRE-EXISTING hain — file me zero diff). Concurrent-stream notes: `09_broker/...test_zerodha_adapter.py` (3+/1-) aur `rust/Cargo.lock` (+62 view-crate entries) ke diffs mere nahi — chhoda as-is. Local only, no commit/push.

## OBR INDICATOR TOOLBAR — ACTIVATED (2026-09-16)
Market chart ka OBR floating toolbar (eye/settings/source/delete) ab real Slint/Rust me fully functional hai — legacy Qt behavior 1:1 reproduce, koi invention nahi. Root causes mile: (1) IndicatorBarRow ke ⚙/{} Slint me literal no-op the (`/* backend no-op */`) — ab `settings-indicator`/`source-indicator` callbacks → `MarketAction::SettingsIndicator/SourceIndicator` (sirf existing row forward, unknown indicator → no wire) → `indicator:settings:/indicator:source:` wires → Python host visibility panel ke SAME `settings_requested`/`source_requested` signals re-emit karta hai jo Qt click emit karta hai (legacy handlers sirf log karte hain — koi settings/source surface legacy me exist nahi karti, isliye koi fake dialog/editor invent nahi kiya). (2) REAL blocker: chart ka pan TouchArea (`cta`) indicator bar ke BAAD declare tha + poora plot cover karta tha — Slint me later TouchArea hit-test jeetta hai, isliye bar ke 4 buttons UNCLICKABLE the (eye/delete "wired" hone ke baad bhi). Fix: indicator bar block ko `cta` ke baad move kiya — same coords, no layout change. (3) INDEPENDENTLY CONFIRMED the same ctypes drain bug jo concurrent stream ne WIPRO entry me fix kiya (`create_string_buffer` vs LP_c_ubyte → suppressed ArgumentError → koi action backend tak nahi) — is tree me `from_buffer` u8-view form me correct hai (13/13 host tests me unka drain test + mera settings/source test dono green). Visual: naya compact `IndicatorIconBtn` (20px, no padding bloat, hover/pressed/active states, tokens only) — buttons label ke bilkul paas (TradingView compactness), row height 28px unchanged, koi redesign nahi. Eye = existing `indicator:vis:` → `_indicator_visible["OBR"]` (levels/rays/markers hide, candles/volume untouched, session auto-save persist); Delete = existing `indicator:rm:` → OBR row hide + owner series cleanup (strategy file untouched); re-add = existing `indicator:add:OBR`. Verification: Rust `market_indicator_toolbar_buttons_queue_wires` (real C ABI: pointer→4 buttons→4 wires, 9/9 render green), `settings_and_source_forward_existing_rows_only` (shell 109/109), Python 13/13, release `vayren_market_view.dll` rebuilt (22:40), aur FULL-STACK real-app offscreen run 11/11 PASS (real cdylib + real CandleChartWidget: eye hide/restore, settings/source legacy signals, delete, candles intact, re-add, chaaro dobara). Gates: ruff/pyright/imports/structure/language-ownership ALL PASS. PRE-EXISTING (not ours): chart `test_query_visible_includes_spanning...` ('RAY' str-vs-enum, concurrent stream ka file), shell-bin link me transient pagefile E0786 (lib targets green). Files: rust/vayren-shell/src/market.rs, rust/vayren-shell/ui/market.slint, rust/vayren-market-view/{ui/market_host.slint,src/lib.rs,tests/render_market.rs}, 00_app/app/services/slint_market_host.py, 00_app/app/tests/test_slint_market_host.py. Local only, no commit/push.

## RELEASE v1.16.0 — 6-screen Slint migration + research engine (2026-09-17)
6/6 production nav Slint (MARKET/LAB/RESEARCH/PORTFOLIO/LIVE/SYSTEM viewports + 6 slint hosts + 6 view crates), research engine (compare/conditions/data_validation/fingerprint/report/statistics/walkforward), OBR toolbar/pan/drain fixes, migration router/gate advance. Release-prep fixes (scope: release content only): test_slint_market_host Qt-mirror noqa (N802/ARG005), zerodha-alias allowlist + third pinned display site (slint_market_host mirrors pinned Qt card text), rustfmt 3 new files. Gate ledger: structure/imports/language(612)/arch/migration-verify PASS; ruff+format PASS on release content; pytest 1819 passed — 8 PRE-EXISTING fails (HEAD-worktree/memory-proved: download-embedded, overlay time-tag, 6 migration-agent) + 0 new; pyright 2 + ruff I001 2 = tool-drift in untouched files (left as-is); Rust: 7 release DLLs + shell rebuilt OK, fmt clean, tests green (core 27, shell 109+3 integ, market-view 2+9, research 3, system 5, live 4, lab 1, portfolio 3; workspace-parallel link needs disk CI has). Scratch EXCLUDED from commit (.b*/.t* logs, ray_probe.py, render_broker_view.py, .vayren-rt-verify, .rust-gate*.log). Pushed main + tag v1.16.0 + GitHub release.

## HISTORICAL DOWNLOAD STOCKS SCROLL DIRECTION FIX (2026-09-17)
ROOT CAUSE: `_ChecklistList` (STOCKS list viewport) had no wheel handler — Qt default scrolled interior correctly (UP decreases offset, DOWN increases) but IGNORED wheel at top/bottom boundaries (probe: accepted=False at edges), so the event bubbled to the outer `HistoricalDownloadPanel` QScrollArea and scrolled Date Range/Interval/Download Plan instead (overscroll/glitch, outside-panel movement). FIX (scope: only `02_data/data/ui/stock_checklist.py::_ChecklistList.wheelEvent`): explicit natural mapping — content offset IS scrollbar value (min=top, max=bottom); positive delta (wheel UP / trackpad pixel+angle UP) → `value - delta`, negative → `value + delta`; trackpad `pixelDelta` 1:1 px else mouse `angleDelta/120 × wheelScrollLines × singleStep` px (same sign, fast wheel linear); clamp to [min,max]; ALWAYS `accept()` (even at edges/empty) so outer panel never scrolls. Search/Select All/Clear All/count/Date/Interval/Plan untouched; Watchlist untouched (direction already consistent, verified same sign). VERIFIED offscreen: UP 500→422, DOWN 500→578, fast 1-notch 578 < 4-notch 812, top UP stays 0 accepted outer 0, bottom DOWN stays max accepted outer 0, selection survives, filter+scroll ok, trackpad ±40 same direction, scrollbar in range. Tests: stock_checklist 15/15 + ui 22/22 + watchlist/chart-window green; ruff/format/pyright/imports/structure/language PASS. PRE-EXISTING (baseline-stash proved, not mine): `test_download_panel_is_embedded_in_main_window` + concurrent-stream fyers live-auth fail. Local only, no commit/push.

## STRATEGY LAB — + NEW FIX + OBR SOURCE RESTORE + EDITOR SOURCE ACTIONS (2026-09-17)
ROOT CAUSES (both proved, then fixed at the source — no UI-only hacks):
(1) **+ NEW created a vanishing in-memory draft**: `bootstrap._on_new_draft` called `open_strategy(name, "")` — an unsaved empty buffer, so the strategy never entered the storage-backed STRATEGIES list and was lost on restart. FIX: `+ NEW` now creates a REAL persisted strategy via `create_strategy` (which REJECTS existing names → can never overwrite OBR or any strategy) with the compilable `DEFAULT_CODE` template, then refreshes the library (`_refresh_my_strategies`) and selects it. Appears immediately, editable/saveable/compilable/runnable, survives restart.
(2) **OBR source was WIPED to empty**: `D:\VAYREN_STRATEGIES\OBR.py` record had `"code": ""` (a save of empty source over the existing record — the `is_update` path in `_on_save`). RESTORE: the authoritative verbatim OBR (39671 chars, "Full Pine 1:1 engine, LAYER 1→12") is preserved in the immutable version graph (version `d258c55b`, strategy id `cf7d7645-...`); restored the exact preserved source via the storage layer (no reconstruction, no logic change — OBR id preserved, compiles, real params: REF Candle Index + slippage trio). GUARD added so this class of wipe can never recur: `_on_save` refuses to save empty/whitespace-only source over an existing strategy (surfaces a clear error, never silent).
(3) **Copy/paste had disappeared**: the editor toolbar only had Save/Compile/RUN. Added a compact source-action bar (CODE tab only): **⧉ Copy All** (copies the COMPLETE `toPlainText()`, never just the visible selection — large sources intact), Select All, Paste, Undo/Redo (Ctrl+Z/Y), Find (Ctrl+F)/Replace (Ctrl+H), and **≡ Format** — a token-safe re-indent (`_reindent_python`) that rewrites ONLY leading whitespace from the Python token stream; strings/comments/code verbatim, and on any tokenization error leaves the source untouched rather than corrupt it.
EDITOR production state: line numbers, syntax highlighting, current/error-line highlight, h/v scrolling, unsaved-change indicator (`_dirty` + `_StatusChip`), compile diagnostics linked to line/column (`show_compile_result` → `set_error`), find/replace, undo/redo — all pre-existing and preserved.
TESTS (`05_strategy/strategy/tests/test_lab_source_actions.py`, 14): create→list→reopen round-trip, create never overwrites (OBR protection), save preserves record id across saves, duplicate independence, delete isolation, **Copy All = complete source not selection**, token-safe reformat (verbatim strings/comments, fixes bad indent, leaves broken source untouched), compile line-report, **OBR integrity** (restored source non-empty + compiles + SHA-256 matches preserved version snapshot), broken-strategy isolation (good strategy still compiles/compiles+duplicates while broken one fails).
VERIFIED: 14/14 new pass; lab suite (test_lab_workspace + test_lab_integration + 05_strategy) 169 passed/1 skipped + 1 PRE-EXISTING order-pollution fail (`test_pill_text_never_clipped_inside_own_pill` — passes in isolation, stash-proved, not mine). ruff+format clean on my files. NOTE — **concurrent workstream is actively editing this repo** (fyers broker + rust event_bus/market/registry + chart_view.slint + callback_url in bootstrap.py + this scroll-fix entry); my edits live in `strategy_lab_workspace.py` (purely +64), `code_editor.py` (purely +110), the new test file, and 2 well-separated hunks in `bootstrap.py`. NOT committed/pushed to avoid disturbing the concurrent stream's uncommitted work — awaiting go-ahead. Local only, no commit/push.
