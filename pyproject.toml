# ══════════════════════════════════════════════════════════════════════════════
# VAYREN — PROJECT CONFIGURATION
# ══════════════════════════════════════════════════════════════════════════════

[project]
name = "vayren-core"
version = "0.1.0"
description = "Event-driven desktop charting platform"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "Vayren" }]
readme = "README.md"

# ── Runtime Dependencies ──────────────────────────────────────
dependencies = [
    "PySide6>=6.7",
]

# ── Development Dependencies ───────────────────────────────────
[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "ruff>=0.6",
    "pyright>=1.1",
    "pre-commit>=3.0",
]

[project.scripts]
vayren = "app:main"

# ══════════════════════════════════════════════════════════════════════════════
# TOOL CONFIGURATION
# ══════════════════════════════════════════════════════════════════════════════

# ── Ruff (Linter + Formatter) ─────────────────────────────────
[tool.ruff]
target-version = "py311"
line-length = 100
indent-width = 4
extend-exclude = ["99_archive", "90_brain"]

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP", "B", "SIM", "ARG", "C4", "T10"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
line-ending = "auto"

[tool.ruff.lint.per-file-ignores]
"03_chart/chart/widgets/*.py" = ["N802"]  # Qt event handlers are camelCase by framework convention

# ── Pyright (Type Checker) ────────────────────────────────────
[tool.pyright]
typeCheckingMode = "standard"
include = [
    "00_app/app",
    "01_core/core",
    "02_market/market",
    "03_chart/chart",
    "scripts",
]

# ── Pytest (Test Runner) ──────────────────────────────────────
[tool.pytest.ini_options]
testpaths = [
    "00_app/app/tests",
    "01_core/core/tests",
    "02_market/market/tests",
    "03_chart/chart/tests",
]
addopts = "-v --tb=short"

# ── Coverage (Test Coverage) ──────────────────────────────────
[tool.coverage.run]
source = [
    "00_app/app",
    "01_core/core",
    "02_market/market",
    "03_chart/chart",
]

[tool.coverage.report]
exclude_lines = ["pragma: no cover", "def __repr__", "if __name__ == .__main__.:"]

# ══════════════════════════════════════════════════════════════════════════════
# BUILD SYSTEM
# ══════════════════════════════════════════════════════════════════════════════

[tool.hatch.build.targets.wheel]
packages = [
    "00_app/app",
    "01_core/core",
    "02_market/market",
    "03_chart/chart",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
