# analytics/services/ — Analytics Services

## What Is Inside

| Service | Responsibility |
|---|---|
| `PerformanceCalculator` | Computes Sharpe, Sortino, max drawdown, win rate, profit factor |
| `AttributionAnalyzer` | Decomposes returns into factor contributions |
| `ReportGenerator` | Assembles metrics + attribution + charts into a structured report |
