# research/training/ — Model Training

## What Is Inside

| File | Purpose |
|---|---|
| `trainer.py` | Training loop orchestration — data loading, model forward/backward, logging |
| `callbacks.py` | Training hooks (early stopping, learning rate scheduling, checkpointing) |
| `dataset.py` | Dataset and dataloader for market data |
