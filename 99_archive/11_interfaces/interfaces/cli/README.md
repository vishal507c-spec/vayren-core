# interfaces/cli/ — Command-Line Interface

## What Is a CLI?

A **CLI** (Command-Line Interface) is a text-based way to interact with the system. Unlike the API (used by programs), the CLI is used by humans in a terminal.

---

## What Is Inside

| File | Purpose |
|---|---|
| `shell.py` | Interactive REPL shell for ad-hoc commands |
| `trade.py` | Trade-specific CLI commands (place, status, cancel) |
