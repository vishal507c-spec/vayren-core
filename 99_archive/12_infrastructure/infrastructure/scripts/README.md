# infrastructure/scripts/ — Infrastructure Scripts

## What Is Inside

| Script | Purpose |
|---|---|
| `setup.sh` / `setup.ps1` | Development environment setup (Python venv, pre-commit hooks, Docker checks) |
| `seed_data.py` | Loads sample market data for local development and testing |
